# AI UI/UX 디자인 아카데미 소개 홈페이지 — 프로젝트 진행 문서

## 요약 — 다음 세션 필독 브리핑 (2026-08-12 작성)

아래는 이 문서 전체(1300줄+)를 시간순이 아니라 주제별로 압축한 것이다. 새 세션에서 이 프로젝트를 다시 열면, 아래 요약만 읽고 바로 감을 잡을 수 있게 정리했다. 시간순 상세 기록은 이 섹션 다음(`## 프로젝트 개요`부터)에 그대로 남겨뒀다.

### 이 프로젝트는 무엇인가
국비지원 UI/UX 디자인 학원의 소개・지원・멘토 매칭 사이트. 데모/과제 프로젝트지만 실제 백엔드(Supabase)까지 붙여서 "진짜 동작하는" 수준으로 완성했다. React 19 + Vite + react-router-dom 프론트엔드, Supabase(PostgreSQL + Auth + RLS, 전용 `academy` 스키마) 백엔드, Vercel 배포(https://ui-ux-course-site.vercel.app). 로컬은 `npm run dev` → `localhost:5173`. 사이트 안에서 "오늘"은 2026-08-12 근방으로 고정해서 모집 일정 등 데이터 정합성을 맞춰뒀다(실제 시스템 날짜와 다를 수 있음).

### 사용자가 일하는 방식 (중요 — 여기서 자주 어긋났음)
- **스크린샷 + 손그림으로 의도를 전달하는 경우가 많다.** 특히 레이아웃/애니메이션 요청은 말로 설명해도 내가 두세 번 잘못 짚은 적이 많았다(여정 그래프가 대표 사례 — "선이 안 이어진다"를 세 번 다르게 고쳐야 했음). 애매하면 바로 구현하지 말고, 실제로 재현되는 화면을 스크린샷으로 찍어보거나 좌표를 실측해서 확인한 뒤 손대는 게 결과적으로 더 빠르다.
- **"됐다"는 말을 믿지 않고 직접 확인해달라고 요구하는 편.** 코드 리뷰만으로 끝내지 말고 실제 브라우저 조작(Playwright)으로 검증하는 걸 기본값으로 여긴다. 특히 "실제로 저장되는지", "다른 사용자에게도 보이는지" 같은 건 반드시 실제 테스트로 증명해야 함.
- **매 작업 단위가 끝나면 "푸시 및 배포"까지가 기본 완료 조건이다.** commit → GitHub push → `vercel deploy --prod`까지 하고 200 OK 확인하는 게 습관화되어 있음. 대부분 먼저 요청하지만, 여러 작업을 이어서 할 때는 마지막에 한 번에 몰아서 해도 됨.
- **문서(보고서류)는 사람이 쓴 것처럼, 짧게.** 결함 보고서를 표・ID 체계로 정리했다가 "너무 AI가 정리한 것 같다"는 피드백을 받고 산문체로 다시 쓴 적 있음. 실제 과제 제출용 문서는 필요 이상으로 길게 쓰지 말 것(예: "PPT 1~2페이지 분량으로" 요청받음). 표는 꼭 필요할 때만.
- **오해했으면 바로 인정하고 재작업.** 우기거나 방어적으로 굴지 않고, 잘못 짚은 지점을 정확히 짚어서 정정하는 흐름이 잘 통했다(예: 유나 계정을 멘토로 착각했을 때, 페르소나 문서가 실제 시드 데이터와 안 맞았을 때 등).

### 반복적으로 쓴 작업 패턴 (다음에도 그대로 재사용할 것)
- **이미지 처리**: `reference/`(원본, git 제외)에 사용자가 원본을 넣어주면 → `npm install --no-save sharp`로 임시 설치 → 크롭/리사이즈 → `public/images/`에 저장 → `npm uninstall sharp`로 제거. 흔적 안 남기는 걸 원칙으로 함.
- **브라우저 자동화 테스트**: 이 환경엔 기본 브라우저 도구가 없음 → `npm install --no-save playwright`로 임시 설치(전역 캐시에 크로미움 있어 재다운로드 없음, 빠름) → 실제 시나리오(회원가입・글쓰기・상태변경 등) 재현해 검증 → 확인 후 `npm uninstall playwright`. `git status`로 package.json/lock 흔적 없는지 항상 재확인.
- **Supabase 마이그레이션**: 이 환경엔 Supabase CLI 연동이 안 되어 있음(프로젝트 링크・DB 비밀번호 없음). 새 테이블/정책은 `supabase/migrations/00XX_*.sql` 파일로만 작성하고, **사용자가 Supabase 대시보드 SQL 에디터에서 직접 순서대로 실행**해야 반영됨. 실행 전까지는 관련 기능이 동작 안 한다는 걸 항상 명시할 것.
- **`progress.md` 기록 관례**: 작업 단위마다 "무슨 요청 → 어떻게 처리 → 확인 필요한 것" 형식으로 이 파일 끝에 이어서 기록.

### 이 사이트의 기술적 특징 (다음에 비슷한 기능 만들 때 참고)
- `academy` 전용 Supabase 스키마 사용(다른 앱과 프로젝트 공유 중이라 `public` 스키마 대신). 새 테이블 만들 때마다 스키마 GRANT를 마이그레이션에 명시적으로 재실행해야 함(0002・0003 패턴).
- RLS는 "로그인 회원이면 대부분 가능"으로 의도적으로 단순화(민감정보 없는 데모라는 전제) — 실제 권한 구분은 화면(라우트)의 role 체크에 의존. **예외**: 커뮤니티 글/댓글의 수정・삭제만 "본인 또는 관리자"로 서버(RLS) 단에서도 강제.
- 계정 없는 시드 데이터(더미 지원자, 시드 게시글 등)를 넣을 때는 `user_id is null` 전용 INSERT 정책을 별도로 추가하는 패턴을 계속 재사용(0004에서 처음 나옴).
- **모든 Context는 로그인 세션이 바뀔 때 재조회하도록 `useEffect` 의존성에 `currentUser`를 반드시 넣을 것** — 세션 복원 전에 딱 한 번만 조회하면 로그인 직후 데이터가 비어 보이는 구조적 버그가 났었음(5개 Context에 동시에 있었음, 2026-08-12에 전부 수정). 새 Context 만들 때 처음부터 이 패턴으로 시작할 것.
- 테스트 계정 4개(비밀번호 전부 `123456`): `minji@example.com`(일반회원, 지원 심사중+지수 멘토 상담중), `yuna@example.com`(합격+은채 멘토 상담중), `jisu@example.com`(멘토), `admin@example.com`(관리자).

### 디자인 톤
배경 `#F7F8F6` / 카드 `#FFFFFF` / 텍스트 `#1C2321` / 포인트 딥그린 `#1F6650` + 골드 `#f5b83d`(마케팅성 CTA 전용, 지원/로그인 같은 기능 버튼은 그린 유지). Pretendard 폰트. "믿을 수 있는 성장 파트너" 컨셉 — 미니멀・신뢰감・안내형. 실제 데이터가 없는 곳(연매출, 가격 등)은 항상 placeholder임을 명시하는 관례.

### 지금 상태
- **완료**: 인증・지원・상담・비회원 콜백・커뮤니티・문의・학원일정 전부 Supabase 연동 + 실제 테스트 완료. 마이페이지, 파비콘, 문서 7종(기능명세서・DB스키마・프로젝트일정표・결함발견및수정보고서・페르소나・정보구조설계도・사용자시나리오) 전부 최신 상태.
- **의도적으로 보류(사용자 결정)**: 공지사항・강사진・멘토소개・후기・포트폴리오・파트너사・FAQ의 관리자 CRUD(정적 콘텐츠라 비용 대비 효용 낮다고 판단해서 보류), 발표자료 제작, 와이어프레임 원본 산출물.

---

## 프로젝트 개요
- **주제**: AI를 활용하는 UI/UX 디자이너 교육 과정 소개 홈페이지 + 정원 신청 시스템 + 멘토 자동 매칭 시스템. 국비지원(국민취업지원제도)은 핵심 정체성이 아니라 수강료를 낮춰주는 부가 혜택으로 다룬다(2026-08-04 브랜드 컨셉 정리, 아래 로그 참고).
- **기술 스택**: React + Vite (Frontend) / Node.js + Express + SQLite (Backend)
- **개발 스타일**: 기능 먼저 구현 후 스타일링, pages/components 폴더 분리, mock data는 별도 data.js, Context API로 상태관리, react-router-dom으로 라우팅, 단계별 구현 + 설명

## 사용자 유형 및 권한
| 유형 | 인증 | 역할 |
|---|---|---|
| 지원자/수강생 | 회원가입/로그인 | 과정 조회, 지원, 마이페이지, 멘토 상담 신청 |
| 멘토 | 로그인 (role 구분) | 배정된 상담 수락/거절/완료 처리 |
| 관리자 | 로그인 (role 구분) | 지원자·정원·멘토 매칭 관리 |

## 핵심 로직
- **정원 초과 방지**: 신청 시점 잔여 정원 체크 → 초과 시 자동 대기 상태
- **대기열 자동 승격**: 합격자 취소 시 대기 1순위 자동 승격
- **멘토 자동 매칭**: 상담 분야 일치 → 멘토 부하(진행 중 상담 건수) 최소 순 배정 → 매칭 실패 시 대기열 등록 + 관리자 수동 배정 알림
- **상태 전이**
  - 지원: `pending → confirmed / waiting → cancelled`
  - 상담: `requested → matched → in_progress → completed`

## DB 스키마
- **User**: id, email, password(해시), name, role(user/mentor/admin)
- **Course**: id, title, capacity, current_count
- **Application**: id, user_id, course_id, status, applied_at
- **Mentor**: id, user_id, specialties(태그), max_concurrent
- **Consultation**: id, student_id, mentor_id(nullable), category, content, status, requested_at

## 디자인 가이드
- 배경 `#F7F8F6` / 카드 `#FFFFFF` / 텍스트 `#1C2321` / 포인트 컬러 `#1F6650`(딥그린)
- 상태 뱃지: 대기중=그레이, 합격/매칭완료=딥그린, 취소=톤다운 레드(`#B24B3F`)
- 폰트: Pretendard 단일 사용 (제목 700 / 본문 400)
- 시그니처 요소: "실시간 잔여 정원" 뱃지

## 페이지 목록 (IA)
- 공통: 랜딩, 회원가입/로그인
- 지원자용: 과정 상세, 지원서 작성, 마이페이지(지원 현황/상담 현황 탭), 상담 신청
- 멘토용: 멘토 대시보드, 상담 상세/처리
- 관리자용: 관리자 대시보드, 지원자 관리, 정원 설정, 상담 매칭 모니터링

---

## 진행 단계

### 1. 기획 및 요구사항 정의 — 완료
- 프로젝트 목표, 핵심 기능 범위, DB 스키마 초안 확정

### 2. 사용자 리서치 — 완료
- 레퍼런스: HRD-Net, 재학 중인 학원 사이트, 부트캠프/채용 플랫폼
- 페르소나 3명 확정: 지원자(민지, 27세), 멘토(지수), 관리자(성훈)

### 3. 디자인 시안 — 완료
- IA 확정 (로그인 → 지원자/멘토/관리자 3개 영역)
- 핵심 와이어프레임 3장: 과정 상세, 마이페이지, 관리자 대시보드
- 디자인 가이드(컬러/폰트/상태색) 확정

### 4. 웹퍼블리싱 — 완료 (순수 HTML/CSS)
- 5개 페이지 시맨틱 마크업 + 반응형(640px/1024px) 구현
- 아코디언/탭은 JS 없이 CSS(`details/summary`, radio 트릭)로 구현
- 관리자 테이블 → 모바일 카드형 자동 전환
- 산출물: `ui-ux-course-site.zip` (index.html, course.html, login.html, mypage.html, admin.html + styles/)

### 5. 게시판 핵심기능 구현 (CRUD) — 완료
React + Vite로 전환하며 아래 순서로 단계별 구현. mock data(`src/data/`) + Context API(`src/context/`) 기반, 백엔드 연동 전 단계.

1. **React + Vite 프로젝트 세팅 — 완료**
   - Vite + React 스캐폴딩, `src/pages/`·`src/components/` 폴더 분리
   - 원본 HTML/CSS 산출물이 폴더에 없어, `progress.md`의 디자인 가이드(컬러/폰트/뱃지 색상)를 기준으로 CSS 변수 및 컴포넌트를 새로 정의
   - 디자인 컨셉 확정: "믿을 수 있는 성장 파트너" (신뢰감·차분함·안내형·미니멀), 딥그린 포인트 컬러 중심
2. **라우팅 구성 — 완료**
   - `react-router-dom`으로 `/`(홈) · `/course`(과정상세) · `/login` · `/mypage` · `/mentor`(멘토 대시보드) · `/admin`(관리자) 라우트 구성
   - 로그인/멘토대시보드/관리자는 아직 플레이스홀더 페이지
3. **지원(Application) CRUD + 정원 초과 자동 대기 — 완료**
   - `ApplicationContext`(Context API): 신청 시점 확정 인원과 정원을 비교해 `confirmed`/`waiting` 자동 결정
   - 과정 상세에서 지원, 마이페이지에서 내 지원 목록 조회 및 취소
   - mock 로그인 사용자(`src/data/users.js`) 1명으로 동작 — 실제 로그인은 6단계에서 연결
4. **관리자 상태 변경 시 대기자 자동 승격 — 완료**
   - 확정(confirmed) 상태였던 지원이 취소/변경되어 좌석이 비면, 같은 과정의 대기(waiting) 1순위를 자동으로 confirmed로 승격
   - 승격 로직은 마이페이지의 자기 취소·관리자 대시보드의 상태 변경 모두에서 공통으로 동작하도록 Context에 단일 함수로 구현
   - 관리자 대시보드에 전체 지원자 목록 + 상태 변경 UI 추가 (모바일에서는 카드형으로 자동 전환)
5. **멘토 상담 신청 + 자동 매칭 로직 — 완료**
   - `ConsultationContext`(Context API): 신청 시 분야 일치 → 부하(진행 중 건수) 최소 순으로 멘토 자동 배정, 매칭 실패 시 `requested` 상태로 대기
   - `/consultation`(상담 신청), `/mentor`(멘토 대시보드: 수락/거절/완료), `/admin`에 상담 매칭 모니터링 + 수동 배정 추가
   - `/mentor`에서 거절 시 다른 멘토로 자동 재매칭 시도
   - 마이페이지에 지원 현황/상담 현황 탭 구성 완료 (IA 요구사항 반영)
6. **회원가입/로그인 및 role 기반 권한 분기 — 완료**
   - `AuthContext`(Context API): mock 계정 로그인/회원가입/로그아웃, 로그인 상태 `localStorage` 유지
   - 테스트 계정(2단계 페르소나와 동일, 비밀번호 모두 `1234`): 지원자 minji@example.com · 멘토 jisu@example.com · 관리자 admin@example.com
   - `RequireAuth` 컴포넌트로 라우트 보호: `/mypage`(로그인 필요), `/mentor`(멘토 전용), `/admin`(관리자 전용)
   - `ApplicationContext`/`ConsultationContext`가 고정 mock 사용자 대신 실제 로그인 사용자 기준으로 동작하도록 전환
   - 헤더에 로그인 상태 및 역할별 메뉴(멘토 대시보드/관리자) 노출

### 5-1. 홈페이지 콘텐츠 보강 — 완료
5단계(핵심기능) 완료 후, 완성도를 높이기 위해 홈페이지 위주로 추가 구현. 실제 이미지/자료 수집 전이라 아래 항목은 전부 **placeholder** — 실제 데이터가 정해지면 해당 `src/data/*.js` 파일만 교체하면 됨.

- **메인 비주얼 슬라이더(`HeroSlider`)**: 5슬라이드, 자동재생(5초)/이전·다음/재생·정지, 폭 1440px로 확대
- **국비지원 안내 배너(`FundingBanner`)**: 홈 최상단, "국민취업지원제도 최대 100% 지원" CTA → `/course`
- **실적/수상 배너(`AchievementBanner`)**: 스크롤 진입 시 숫자 카운트업 애니메이션(`CountUpValue` + Intersection Observer)
- **지원 마감 카운트다운(`DeadlineCountdown`)**: 과정 상세 + 홈 정원 뱃지 옆 실시간 표시
- **기수(회차) 개념 도입**: 현재 4기 모집중 (과정 상세에 뱃지 표시)
- **수료생 포트폴리오**: `/portfolio` 전체 페이지(1~3기 필터, 4기는 아직 미수료) + 홈 미리보기 3개. 이니셜+그라디언트로 임시 썸네일 처리
- **과정 진행 흐름 그래프(`JourneyGraph`)**: SVG 곡선 그래프로 지원→멘토매칭→실무프로젝트→취업연계 4단계 시각화
- **파트너사 로고 마퀴(`PartnerMarquee`)**: CSS 무한 스크롤 애니메이션(`prefers-reduced-motion` 대응)
- **FAQ 아코디언(`FaqAccordion`)**: `details/summary` 기반 — 4단계 웹퍼블리싱 때의 기법을 React 전환 후에도 동일하게 유지
- **하단 CTA 배너(`BottomCta`)**: 상담 신청 유도, `/consultation` 연결
- **교육장 위치/연락처 안내 페이지(`/location`, "오시는 길")**: 지도 자리(placeholder) + 주소/전화/이메일/운영시간/오시는 방법
- **푸터 개편**: 로고+내비게이션(개인정보처리방침·학원소개·고객상담센터·학원위치안내·대표문의전화) + 사업자 정보 블록(`contactInfo.js`의 `businessInfo`) + 저작권. `/privacy` placeholder 페이지 추가

### 6. 테스트 및 디버깅 — 1차 진행 (2026-07-31)
Playwright로 dev 서버를 직접 띄워 브라우저 시나리오를 재현하며 점검. 정적 코드 리뷰 + 실제 조작 두 단계로 확인.

**확인 완료 (정상 동작)**
- 정원 초과 자동 대기: 정원이 찬 과정에 신청 시 `waiting` 상태로 전환됨
- 대기열 자동 승격: 확정 지원 취소 시 같은 과정 대기 1순위가 `confirmed`로 자동 승격됨 (관리자 대시보드에서 확인)
- 멘토 자동 매칭: 분야 일치 + 부하 최소 멘토에게 배정됨 (부하가 낮은 멘토가 선택됨을 확인)
- 멘토 거절 → 재매칭 실패 시 대기열(`requested`) 전환: 대체 멘토가 없는 분야에서 거절 시 배정 해제 + 관리자 모니터링에 "배정 대기중"으로 정확히 표시됨
- `RequireAuth` 권한 분기: 비로그인 → `/login` 리다이렉트, `user`/`mentor` 역할이 `/admin` 접근 시 권한 없음 placeholder 정상 표시
- 회원가입 mock 계정은 새로고침 시 초기화됨(설계상 의도된 동작, `AuthContext.jsx` 주석에 명시) — 실제 버그 아님, 백엔드 연동 전까지는 유의해서 테스트할 것

**버그 발견 및 수정 완료**
1. **정원 초과 버그**: 관리자 대시보드에서 대기중 지원자를 상태 변경 드롭다운으로 "합격"으로 바꿀 때 정원 체크가 없어, 정원 20명 과정에 21명이 확정되는 것을 실제로 재현함 → `ApplicationContext.jsx`의 `updateApplicationStatus`에 정원 체크 가드 추가 + `Admin.jsx`에서 정원이 찬 과정은 "합격" 옵션을 드롭다운에서 비활성화. 재현 스크립트로 수정 후 재검증 완료(정원 초과 차단 확인).
2. **모바일 헤더 반응형 깨짐**: `Header.css`에 모바일 브레이크포인트가 전혀 없어, 375px 폭에서 내비게이션 한글 메뉴가 글자 단위로 줄바꿈되어 모든 페이지 상단이 읽을 수 없는 세로 글자 더미로 깨짐(사이트 전체에 영향, 심각도 높음) → 640px 이하에서 로고/로그인 영역과 내비게이션을 분리된 줄로 배치하고 `white-space: nowrap`으로 단어 단위 줄바꿈이 되도록 수정. 수정 후 모바일 뷰포트 스크린샷으로 재검증 완료.

**추가 확인 완료 (2026-07-31, 2차)**
- 더블클릭/중복 제출 방지: 과정 지원·상담 신청 버튼을 빠르게 두 번 눌러도 중복 레코드가 생기지 않음(첫 클릭 직후 버튼이 사라지도록 렌더링되어 있어 안전). 다만 이는 같은 브라우저 탭 안에서의 클릭 경쟁만 해당 — 여러 사용자가 실제로 동시에 마지막 좌석에 신청하는 "진짜" 동시성은 현재 프론트엔드 단독(백엔드 없음, 각 브라우저가 독립된 메모리 상태) 구조상 의미 있게 재현할 수 없음. 6단계 나머지 항목이 아니라 7단계 백엔드 연동 이후 서버 쪽에서 다시 점검해야 함
- 반응형: 640px/1024px 중간 폭에서 홈/과정상세/포트폴리오/오시는길/상담신청/로그인/마이페이지/관리자/멘토 대시보드 전 페이지 스크린샷 확인 — 헤더 수정 이후 추가로 깨지는 곳 없음

**남은 것**
- API 예외처리 — 현재 백엔드가 없어 해당 사항 없음(5단계까지는 mock data + Context API 단계). 7단계 백엔드 연동 후, 진짜 동시 신청 레이스 컨디션과 함께 재점검 필요

6단계(테스트 및 디버깅)는 사실상 마무리. 다음은 7단계(배포 및 문서화)로 넘어가면 됨.

### 7. 배포 및 문서화와 산출물 정리 — 진행 중 (2026-07-31)
- **프론트엔드 배포 완료**: https://ui-ux-course-site.vercel.app (Vercel, 프로젝트명 `ui-ux-course-site`). `vercel.json`에 SPA rewrite 설정 추가(딥링크 새로고침 대응), 배포 후 홈/딥링크 라우팅 브라우저로 재검증 완료.
- **백엔드 배포는 아직 하지 않음** — 애초에 백엔드(Node.js+Express+SQLite) 자체가 구현되어 있지 않음(5단계까지 mock data + Context API 단계). 사용자와 논의 후 "프론트엔드만 우선 배포+문서화" 범위로 진행하기로 결정.
- **문서화 완료**: `docs/기능명세서.md`, `docs/DB스키마.md`, `docs/프로젝트일정표.md` 작성 (프로젝트 내 markdown 문서 형태로 진행하기로 결정)
- **산출물 정리 — 확인 필요**: 1~4단계에서 언급된 기획/디자인 원본 산출물(페르소나, 와이어프레임, `ui-ux-course-site.zip` 등)이 현재 프로젝트 폴더에 없음 (`reference/` 폴더에는 디자인 컨셉 이미지 1장만 존재). 발표자료도 아직 미착수.

**배포 이후 추가 UI 개선 (사용자 요청, 2026-07-31, 아직 배포에는 반영 안 함 — 로컬에서만 확인)**
- 헤더 고정(sticky): `Header.css`에 `position: sticky` 적용, 스크롤해도 상단 메뉴가 계속 보이도록 수정
- 과정 소개(`/course`) 페이지 상세화: 모집요강/이런 걸 배워요(스킬 태그)/커리큘럼(4단계)/지원 자격/과정 전용 FAQ 섹션 추가. 콘텐츠는 `src/data/courseDetail.js`에 placeholder로 분리(다른 데이터 파일과 동일한 패턴). `FaqAccordion` 컴포넌트를 `items`/`title` props를 받도록 리팩터링해 홈페이지 FAQ와 과정 FAQ에서 재사용
- 관리자 대시보드(`/admin`) 요약 통계 카드 추가: 정원 대비 확정, 합격/대기중/취소, 상담 진행중/배정대기중/완료 — 테이블만 있던 화면 상단에 한눈에 보는 요약 정보 추가
- 마이페이지(`/mypage`) 프로필 카드 + 요약 통계 카드 추가: 이름/이메일/역할 배지 + 내 지원·상담 현황 6개 지표(합격/대기중/취소, 진행중/배정대기/완료) — 리스트만 있던 화면에 한눈에 보는 요약 정보 추가
- **문의 게시판(Q&A) 신규 구현**: 이전에 "향후 계획"으로만 적어뒀던 기획(1:1 답변형, 자유 댓글 아님)을 실제로 구현함
  - `InquiryContext.jsx` 신규 추가 (`main.jsx`에 Provider 등록), `src/data/inquiries.js` seed data(답변완료 1건 + 답변대기 1건)
  - `/inquiry` 페이지: 로그인 사용자가 제목+내용으로 문의 등록 (`ConsultationRequest.jsx`와 동일한 패턴)
  - 마이페이지에 "문의 내역" 탭 추가 (지원 현황/상담 현황과 동일한 3번째 탭), 답변 완료 시 답변 내용이 함께 표시됨
  - 관리자 대시보드에 "문의 관리" 섹션 추가: 답변대기 문의에 인라인으로 답변 등록 → 상태가 자동으로 답변완료로 전환(별도 status 필드 없이 reply 유무로 파생시켜 상태 불일치 원천 차단)
  - 헤더 내비게이션에 "문의하기" 링크 추가
  - 전체 흐름(문의 등록 → 마이페이지 확인 → 관리자 답변 → 답변 반영)을 브라우저로 end-to-end 검증 완료, 데스크톱/모바일 모두 확인
  - 사용자 피드백 반영 1차: 처음엔 `/inquiry`가 글쓰기 폼만 있고 내역은 마이페이지에서만 보여서 "글을 써야 확인 가능한" 흐름이었음 → `/inquiry` 페이지 자체에도 "내 문의 내역"을 바로 아래에 표시하도록 수정. 목록 렌더링은 `components/InquiryList.jsx`로 공통화해 마이페이지 탭과 `/inquiry` 페이지가 함께 재사용
  - 사용자 피드백 반영 2차("게시판이 어디있는지 못 찾겠다"): 원인은 비로그인 상태로 `/inquiry`에 들어가면 로그인 유도 문구만 보이고 게시판 내용이 전혀 안 보이는 구조였음(1차 수정에서도 "내 문의 내역"이라 로그인 전엔 빈 목록) → `/inquiry`를 "로그인 없이도 전체 문의 목록(작성자 비공개)을 볼 수 있는 게시판 + 로그인해야 쓸 수 있는 글쓰기 폼"으로 구조 변경. `getAllInquiries()`를 최신순으로 뒤집어 표시. 마이페이지 "문의 내역" 탭은 기존대로 본인 것만 보여주는 개인 이력으로 유지(용도가 다름)
  - seed 데이터 2건 → 5건으로 보강(답변완료 3건 + 답변대기 2건)해 게시판에 실제 내용이 바로 보이도록 함
  - 사용자 피드백 반영 3차("내가 쓴건지 남이 쓴건지 구분이 안 가고 FAQ 같다"): 카드가 전부 펼쳐진 채로 나열되는 방식이 문제였음 → `details/summary` 기반의 진짜 게시판 목록(상태뱃지·제목·작성자·날짜 한 줄 요약 → 클릭 시 펼쳐서 내용/답변 확인, `FaqAccordion`과 동일한 CSS-only 아코디언 패턴)으로 재설계. 작성자 구분: 로그인한 사용자 본인 글은 "나"+"내 글" 태그로 강조, 타인 글은 전부 "익명"으로 표시(개인정보 노출 없이 구분만 가능). 마이페이지 "문의 내역" 탭은 전부 본인 글이라 이 구분이 무의미하므로 `showAuthor={false}`로 비활성화
- **커뮤니티 게시판 신규 구현 (2026-07-31)**: 문의 게시판은 1:1 답변형(관리자만 응답)이라 "다수가 함께 글을 쓰는 공간"이 아니라는 사용자 피드백을 받고, 기존 문의 게시판은 그대로 둔 채 별도로 진짜 다대다 커뮤니티 게시판을 신규 구현
  - `CommunityContext.jsx`(신규, `main.jsx`에 Provider 등록) + `src/data/communityPosts.js` seed data(5개 글, 카테고리 4종: 자유/스터디 모집/정보공유/후기, 댓글 0~4개로 다양하게 구성 — 여러 명이 한 글에 댓글을 주고받는 흐름을 보여줌)
  - `/community`(목록, 카테고리 필터 + 글쓰기 폼 토글) → `/community/:postId`(상세 + 댓글 목록 + 댓글 작성) 2단계 라우팅 — 문의 게시판(아코디언 방식)과 달리 댓글이 계속 늘어날 수 있어 목록→상세 페이지 분리가 더 적합하다고 판단
  - 문의 게시판과의 핵심 차이: 로그인한 아무 사용자나 서로의 글에 댓글을 남길 수 있음(1인당 1답변이 아니라 다인 스레드), 작성자 이름을 실명(로그인 계정명 또는 seed 데이터의 가상 수강생 이름)으로 노출 — 문의 게시판이 "익명" 처리한 것과 의도적으로 다르게 설계(커뮤니티는 사회적 공간이라 신원 노출이 자연스러움)
  - 헤더 내비게이션에 "커뮤니티" 링크 추가
  - 글쓰기·목록 필터·상세 진입·댓글 작성(다른 사용자 이름으로 표시)·새 글 작성 후 목록 최상단 노출까지 end-to-end 브라우저 검증 완료, 데스크톱/모바일 확인
- **헤더 메인메뉴-서브메뉴(드롭다운) 구조로 재편**: 평면 메뉴 7개가 너무 많아져 3개 그룹으로 재구성
  - 교육과정(과정 소개·수료생 포트폴리오) / 소통공간(멘토 상담·문의하기·커뮤니티) / 학원안내(학원소개·오시는 길), 마이페이지·역할별 링크(멘토 대시보드/관리자)는 그대로 평면 유지
  - `<details name="main-nav">` 기반 CSS-only 드롭다운(`NavDropdown.jsx` 신규, `FaqAccordion`과 같은 계열의 무-JS 아코디언 패턴) — 같은 `name`을 가진 details는 하나만 열리도록 브라우저가 자동 처리(다른 메뉴 열면 기존 것 자동 닫힘). 서브메뉴 링크 클릭 시 ref로 강제로 닫아 페이지 이동 후에도 드롭다운이 열린 채로 남지 않도록 처리(Header는 라우트 변경에 리마운트되지 않으므로 별도 처리 없으면 계속 열려있음)
  - 모바일(640px 이하): 드롭다운이 절대위치 대신 인라인으로 펼쳐지고, 열린 항목이 `flex-basis: 100%`를 가져 옆 메뉴와 겹치지 않고 다음 줄로 밀려나도록 수정(1차 구현에서 겹침 버그 발견 후 수정)
  - **학원소개(`/about`) 페이지 신규 제작**: 푸터에 이미 "학원소개" 링크가 있었지만 지금까지 `/`(홈)로 연결되는 빈 자리였음 — 드롭다운에 실제 항목으로 넣기 위해 교육 철학 3항목 + 실적(`AchievementBanner` 재사용) + 연혁 + 안내 CTA로 구성한 실제 페이지 제작, 푸터 링크도 `/about`으로 수정
  - 데스크톱(드롭다운 열기/배타적 닫힘/링크 클릭 후 자동 닫힘)과 모바일 모두 브라우저로 검증, 전체 페이지 회귀 테스트(콘솔 에러 없음) 완료

**추가 메뉴 1/4: 채용연계 파트너사(`/partners`) — 완료**
- `data/partners.js`를 문자열 배열 → 객체 배열(업종/소개/채용분야)로 확장, 기존 `PartnerMarquee`(홈 로고 마퀴)도 함께 갱신하고 마퀴 라벨을 `/partners`로 연결되는 링크로 변경
- 카드 그리드로 6개 파트너사(업종, 소개, 채용 분야 태그) 표시, `PortfolioCard`와 동일한 그라디언트 이니셜 썸네일 패턴 재사용
- "교육과정" 드롭다운에 서브메뉴로 추가
- 데스크톱/모바일 확인, 콘솔 에러 없음. (스크린샷에서 sticky 헤더가 중간에 겹쳐 보이는 현상 발견 → DOM엔 헤더가 1개뿐이고 스크롤 시 정상 고정되는 것 확인, Playwright fullPage 스크린샷 캡처 특유의 시각적 아티팩트로 결론, 실제 버그 아님)

**추가 메뉴 2/4: 강사진 소개(`/instructors`) — 완료**
- `data/instructors.js` 신규: 강사 4명, 각각 `courseDetail.js`의 커리큘럼 4단계(UX 리서치/UI 디자인/인터랙션/실무 프로젝트)와 1:1로 매칭해 "이 단계는 이 강사가 담당" 구조로 설계 — 과정 소개 페이지 커리큘럼과 자연스럽게 연결됨
- 카드에 담당 커리큘럼 단계, 이름·직함, 전문분야, 소개, 경력 표시. 원형 그라디언트 이니셜 아바타(파트너사 카드와는 사각형/원형으로 구분해 "회사 로고" 느낌과 "사람" 느낌을 시각적으로 다르게 처리)
- "학원안내" 드롭다운에 서브메뉴로 추가, 학원소개(`/about`) 페이지 하단 안내 문구에도 상호 링크 추가
- 데스크톱/모바일 확인, 콘솔 에러 없음

**추가 메뉴 3/4: 공지사항(`/notices`) — 완료**
- `data/notices.js` 신규: 5건(모집안내/이벤트/시스템/일반 카테고리, 그 중 1건은 `pinned: true`로 "중요" 고정)
- `/notices`(목록, 카테고리 필터, 중요 공지 상단 고정 정렬) → `/notices/:noticeId`(상세) — 커뮤니티 게시판과 같은 목록/상세 2단계 라우팅 패턴 재사용. 댓글 등 상호작용은 없는 읽기 전용 게시판이라 관리자 작성 UI는 만들지 않고 seed data로만 구성(실제 운영진이 올리는 공지는 백엔드 연동 이후 관리자 작성 기능 추가 필요)
- "학원안내" 드롭다운에 서브메뉴로 추가 (4번째 항목)
- 데스크톱/모바일 확인, 카테고리 필터·중요 공지 정렬·상세 진입 모두 테스트 완료, 콘솔 에러 없음

**추가 메뉴 4/4: 전체 FAQ 페이지(`/faq`) — 완료**
- `data/faqBank.js` 신규: 기존 홈(`faqs.js`)·과정 소개(`courseDetail.js`의 `courseFaqs`) 질문을 모두 포함하면서 4개 카테고리(국비지원・수강료 / 지원 절차 / 커리큘럼・수업 방식 / 멘토・취업연계)로 분류하고, 카테고리 공백을 메우기 위해 새 질문 몇 개 추가(수강료 분할납부, 온라인 수강 여부, 강사진 안내 등 — 오늘 만든 다른 페이지들과 자연스럽게 상호 연결됨)
- 검색창(질문+답변 텍스트 매칭) + 카테고리별 그룹 표시, 각 그룹은 기존 `FaqAccordion` 컴포넌트를 그대로 재사용(수정 없이 `items`/`title` props만 다르게 전달)
- 검색 결과 없을 때 문의 게시판으로 유도하는 안내 문구 추가
- "소통공간" 드롭다운에 서브메뉴로 추가
- 검색 필터링, 빈 결과 상태, 아코디언 열기, 데스크톱/모바일 모두 테스트 완료, 콘솔 에러 없음

## 헤더 메뉴 구조 최종 (2026-07-31)
- **교육과정**: 과정 소개 · 수료생 포트폴리오 · 채용연계 파트너사
- **소통공간**: 자주 묻는 질문 · 멘토 상담 · 문의하기 · 커뮤니티
- **학원안내**: 학원소개 · 강사진 소개 · 공지사항 · 오시는 길
- 평면 유지: 마이페이지, (역할별) 멘토 대시보드 / 관리자

오늘 하루 동안 배포 이후 로컬에서만 진행된 작업이 많이 쌓여있었으나, 전체 재검토 후 Vercel에 재배포 완료(2026-07-31).

## 배포 전 최종 점검 및 재배포 (2026-07-31)
- 전체 페이지(공개 15개 경로 + 역할별 보호 페이지 3개) 브라우저 회귀 테스트, 콘솔 에러 없음 확인
- `npm run build` 프로덕션 빌드 정상 완료
- Vercel 재배포 완료 → https://ui-ux-course-site.vercel.app (프로덕션 alias 유지, 새 배포로 교체)
- 배포된 라이브 사이트에서 신규 페이지(학원소개/강사진소개/공지사항/FAQ/파트너사/커뮤니티/문의게시판) 및 드롭다운 내비게이션 직접 재검증 완료, 딥링크 새로고침(SPA rewrite)도 정상 확인, 콘솔 에러 없음

---

## 현재 남은 작업 (다음 단계)
5단계 + 5-1단계 + 6단계 완료. 7단계(배포 및 문서화)는 프론트엔드 배포 + 문서화(기능명세/DB스키마/일정표)까지 완료, 산출물 정리와 발표자료가 남음.

**다음 대화 시작 시 먼저 제안할 작업: 7단계 마무리**
- 기획/디자인 원본 산출물(페르소나, 와이어프레임 등) 위치 확인 — 현재 프로젝트 폴더에는 없음
- 발표자료 제작
- (원하는 경우) 백엔드(Node.js+Express+SQLite) 구현 착수 — 이후 진짜 동시 신청 레이스 컨디션과 API 예외처리 재점검
- 이후: 향후 계획(팝업 광고/플로팅 UI/데모 접속 안내 페이지)은 사용자가 원할 때 별도 착수

## 문의 게시판 (Q&A) — 완료 (2026-07-31)
아래 있던 기획을 그대로 구현함. 상세 내역은 위 "배포 이후 추가 UI 개선" 참고.
- 지원자가 글(문의)을 작성하면 **담당자(관리자)가 답변을 남기는 형태** (1:1 답변형, 자유 댓글 아님) — 구현 완료
- 기존 "멘토 상담(Consultation)"과는 별개 기능으로 분리 — 구현 완료
- DB 스키마 문서(`docs/DB스키마.md`)의 `Inquiry`/`InquiryReply` 계획대로 구현하되, 프론트엔드 단계에서는 별도 `status` 필드 없이 `reply` 유무로 상태를 파생시킴(상태 불일치 방지). 백엔드 착수 시 이 차이를 반영해 스키마 문서 업데이트 필요.

## 다음 작업 목록 (2026-07-31 사용자 지정) — 2026-08-04 전체 완료

1. **문의 폼에 연락처・이메일 입력칸 추가 — 완료**: `/inquiry` 문의 등록 폼에 연락처(선택)・이메일(선택, 로그인 이메일로 자동 채움) 필드 추가. `InquiryContext.createInquiry(title, content, contact, email)`로 시그니처 확장, `data/inquiries.js` seed 데이터에도 `contact`/`email` 추가. 관리자 대시보드(`/admin`) 문의 관리 목록에 연락처/이메일 노출(공개 게시판에는 노출 안 함, PII 보호). `docs/DB스키마.md`의 Inquiry/InquiryReply 절도 "향후 계획(미착수)"에서 실제 컬럼(연락처/이메일 포함) 문서로 갱신.
2. **국비지원 전용 메뉴 신설 — 완료**: 사용자가 "정보 통합 페이지"로 결정. `/funding` 신규 페이지(지원 대상 체크리스트, 수강료 안내, 신청 절차 4단계, "국비지원・수강료" FAQ 재사용) 추가, 헤더 "교육과정" 드롭다운과 홈 `FundingBanner` CTA를 `/funding`으로 연결.
3. **지점(여러 교육장) 추가 — 완료(축소 범위)**: 사용자가 "메뉴에만 넣고 실제 기능 링크는 구현하지 않음"으로 결정. 헤더 "학원안내"에 "지점 안내" 메뉴 추가, `/branches`는 기존 `/privacy`와 동일한 `PagePlaceholder` 패턴으로 안내 문구만 표시(실제 지점 데이터/목록 없음).
4. **수강료 조회 페이지 — 착수 안 함(사용자 확인)**: 2번에서 만든 `/funding`의 수강료 안내 섹션(지원율/자부담금/훈련장려금)으로 충분하다고 확인받아 별도 계산기/페이지는 만들지 않음.
5. **수강생 후기 및 취업 성공사례 인터뷰 페이지 — 완료**: `/reviews` 신규 페이지. 기존 `portfolios.js`와 동일 인물(1~3기, 4기는 미수료라 제외) 기준으로 인터뷰 콘텐츠 구성 — "이 프로젝트를 만든 사람이 이 파트너사에 취업했다"는 흐름으로 포트폴리오・파트너사 페이지와 자연스럽게 연결. 카드에 배경・취업 회사/직무 뱃지・한줄 인용구 + `details/summary`로 펼치는 Q&A 2문항. 기수 필터는 `Portfolio.jsx`와 동일한 패턴 재사용. 헤더 "교육과정"에 "수강생 후기" 메뉴 추가.

**참고**: 이번 회차는 `npm run lint` + `npm run build`로 정적 검증만 완료. 이 환경에는 브라우저 자동화 도구(chromium-cli, playwright 등)가 설치되어 있지 않아 실제 브라우저 조작 검증은 못 했음 — `http://localhost:5173`에서 `/funding`, `/reviews`, `/branches` 및 헤더 드롭다운("교육과정"/"학원안내")을 직접 확인 필요.

## 향후 계획 — 팝업 광고
- 홈페이지에 팝업창(모달)으로 광고를 띄울 예정. 노출 조건/디자인/닫기 방식(오늘 하루 안 보기 등)은 착수 시 논의.

## 향후 계획 — 스크롤 추적 플로팅 UI (참고: SBS 아카데미AIX학원 사이트)
- **퀵메뉴 바**: 스크롤해도 화면에 고정되어 따라다니는 알약 모양 바. 바로가기 버튼 모음(예: 특강조회/교육과정조회/국비과정조회/포트폴리오/간편 수강료 조회) + "HOT" 배지 — 미착수
- **상담 전용 플로팅 버튼 — 완료(2026-08-04)**: `FloatingConsult.jsx`, 화면 우측 하단 고정, 말풍선 아이콘 클릭(데스크톱은 호버 아님, 클릭형 `details/summary`) 시 전화 상담(`tel:`)·1:1 문의(`/inquiry`)·카카오톡 상담(실제 채널 없어 "준비중" 표시, 비활성) 메뉴 노출. 전체 페이지 공통(`App.jsx`에 Header/Footer와 함께 마운트).
- **상단 광고 배너**: 페이지 최상단(헤더 위)에 노출되는 배너, X 버튼으로 닫으면 사라짐(팝업 광고와는 별개 요소) — 미착수
- 나머지 항목 착수 시 상세 디자인/문구 확정

## 추가 UI 개선 (2026-08-04, 사용자 요청)
1. **메인 히어로 슬라이드 5개 콘텐츠 확정**: `heroSlides.js`에 실제 카피(eyebrow/title/CTA) 작성 — 1.실무증명(기존 유지) 2.국비지원(`/funding` 연결) 3.1:1 멘토링(`/instructors` 연결) 4.실무프로젝트(`/portfolio` 연결) 5.취업연계(`/reviews` 연결). 오늘 만든 신규 페이지들과 CTA로 자연스럽게 이어지도록 구성. 각 슬라이드에 `imageHint`(촬영/이미지 선정 가이드 주석)와 `image`(실제 이미지 URL, 현재는 전부 null) 필드 추가 — `HeroSlider.jsx`도 `image`가 채워지면 배경 사진(어두운 오버레이)으로, 비어있으면 기존 그라디언트로 자동 대체되도록 수정. 실제 이미지 후보는 대화에서 텍스트로 별도 제안.
2. **오시는 길(`/location`) 상담 신청하기 버튼 제거**: 페이지 목적(위치·연락처 안내)과 맞지 않는다는 판단으로 하단 CTA 버튼 삭제.
3. **오시는 길 네이버 지도 연동**: `components/NaverMap.jsx` 신규 — 네이버 지도 JS SDK(v3)를 `VITE_NAVER_MAP_CLIENT_ID` 환경 변수로 동적 로드, 없거나 로드 실패 시 기존 placeholder로 자동 대체. `contactInfo.js`에 좌표(`lat`/`lng`, 강남역 인근 placeholder — 실주소 확정 시 교체 필요) 추가. **NCP(네이버 클라우드 플랫폼)에서 Maps Client ID를 발급받아 `.env.local`에 설정해야 실제 지도가 뜸** — README에 안내 추가.
4. **헤더 드롭다운 호버 오픈**: `Header.css`에 `@media (hover: hover) and (pointer: fine)` 가드로 데스크톱(마우스)에서는 클릭 전에 호버만 해도 서브메뉴가 뜨도록 추가. 터치 기기는 기존처럼 클릭(`details/summary` 기본 동작)만 유지.

**참고**: `npm run lint` + `npm run build` 정적 검증 완료(오류 없음). 이 환경엔 브라우저 자동화 도구가 없어 실제 브라우저 조작 검증은 못 함 — 특히 지도 연동(Client ID 미설정 시 placeholder 노출 확인)과 헤더 호버 동작은 `http://localhost:5173`에서 직접 확인 필요.

## 취업지원 페이지(`/career-support`) — 완료 (2026-08-04)
"교육 수료 후 취업지원 페이지" 요청으로 신규 제작. 기존 `/reviews`(취업 성공 인터뷰)·`/partners`(연계 기업 목록)와 겹치지 않게, "수료 후 실제로 어떤 지원을 받는지" 프로세스/프로그램 자체를 설명하는 페이지로 구성.
- `data/careerSupport.js`: 취업지원 프로세스 5단계(포트폴리오 컨설팅 → 이력서・자기소개서 첨삭 → 모의면접 → 채용 연계 → 입사 후 케어) + 지원 프로그램 5종 카드
- `AchievementBanner`(취업률 92% 등 기존 실적 재사용) + `/funding`과 동일한 번호 원형 스텝 리스트 패턴 재사용
- `/reviews`, `/partners` 페이지 설명 문구에 `/career-support`로 이어지는 상호 링크 추가(반대 방향도 마찬가지)
- 헤더 "교육과정" 드롭다운에 "취업지원" 메뉴 추가 (과정 소개・국비지원 안내・수료생 포트폴리오・수강생 후기・취업지원・채용연계 파트너사 순)
- `npm run lint` + `npm run build` 통과, 브라우저 조작 검증은 미실시(도구 없음) — `http://localhost:5173/career-support`에서 직접 확인 필요

## 지점 안내(`/branches`) 실제 콘텐츠 구현 — 완료 (2026-08-04)
2026-08-04 앞선 작업에서 "메뉴만 넣고 실제 기능은 구현하지 않음"으로 미뤄뒀던 `/branches`를, 사용자가 실제 지점 데이터를 요청해 정식 구현으로 전환.
- `data/branches.js`: 일산점・수원점・인천점・대전점 4개 지점(주소/전화/운영시간/좌표/오시는 방법 placeholder)
- `Branches.jsx`: 지점 선택 탭(`Portfolio.jsx`/`Reviews.jsx`의 필터 버튼 패턴 재사용) + 선택한 지점의 지도・정보를 `/location`과 동일한 레이아웃(`Location.css`의 `location__content`/`location__info` 클래스 재사용)으로 표시. 본원 안내는 기존 `/location`으로 안내하는 문구 추가해 두 페이지 역할을 분리(중복 방지)
- 지점 전환 시 `NaverMap`이 좌표 변경을 감지해 해당 지점 위치로 지도를 다시 그림 — `/location`과 마찬가지로 `VITE_NAVER_MAP_CLIENT_ID` 미설정 시 placeholder로 대체
- `App.jsx`의 `/branches` 라우트를 기존 `PagePlaceholder`에서 `Branches` 페이지로 교체
- `npm run lint` + `npm run build` 통과, 브라우저 조작 검증은 미실시(도구 없음) — `http://localhost:5173/branches`에서 탭 전환·지도 동작 직접 확인 필요

## 헤더 버그 수정 2건 — 완료 (2026-08-04)
사용자가 실제 브라우저에서 테스트 후 발견해 알려준 문제 2건 수정.
1. **드롭다운 호버가 전혀 동작하지 않음(클릭으로만 열리고 닫힘)**: 1차 시도(미디어 쿼리 게이팅 제거)로도 해결 안 됨 — CSS `!important`로 `details`의 기본 숨김을 덮어쓰는 방식 자체가 사용자 브라우저에서 먹히지 않는 것으로 판단. `NavDropdown.jsx`를 CSS 우회 대신 `onMouseEnter`/`onMouseLeave`로 `detailsRef.current.open`을 직접 제어하는 방식으로 재작성(클릭 시 이미 정상 동작하던 것과 동일한 메커니즘이라 신뢰도 높음). `Header.css`의 임시 hover CSS 블록은 제거(더 이상 필요 없음, 기존 `[open]` 화살표 규칙이 그대로 재사용됨).
2. **마이페이지 위치**: 콘텐츠 드롭다운들 사이 평문 링크로 있던 "마이페이지"를, 로그인/계정 정보 옆(`site-header__inner`의 nav 바깥, 로그인·로그아웃 블록 바로 앞)으로 이동. 데스크톱·모바일 모두 로그인 영역과 붙어 보이도록 CSS 조정(`.site-header__mypage` 신규 클래스, nowrap 처리).
- `npm run lint` + `npm run build` 통과, 브라우저 실동작 확인은 사용자 쪽에서 재검증 필요

## 브랜드 컨셉 정리 — "AI UI/UX 디자인 아카데미" 리브랜딩 (2026-08-04)
사용자 피드백: "메인은 AI를 활용하는 UI/UX 디자이너 교육인데 국비로 받을 수도 있는 것 — 홈페이지 메인이 국비교육이라는 건 이상하다." 브랜드 정체성을 재정의.
- **새 브랜드명**: "AI UI/UX 디자인 아카데미" (사용자 확정). 헤더 로고, 푸터 로고/카피라이트, `index.html` `<title>`, `contactInfo.js`(교육장명/사업자명/학원명), About 페이지 히어로 문구·연혁, `docs/기능명세서.md`·`progress.md` 프로젝트명까지 전부 교체.
- **국비지원 비중은 "여전히 눈에 띄게" 유지**(사용자 선택) — 브랜드 정체성에서만 빠지고, 실제 콘텐츠는 그대로 강조:
  - 히어로 슬라이더 1번(브랜드 대표 슬라이드)은 AI 중심 카피로 교체, 2번(국비지원) 슬라이드는 그대로 유지하되 "AI UI/UX 디자인 과정"을 함께 언급하도록 살짝 수정
  - `FundingBanner`(홈 상단 배너), `/funding` 페이지, FAQ "국비지원・수강료" 카테고리, `courseDetail.js` 수강료 안내, 후기・문의 게시판의 국비 관련 콘텐츠는 전부 그대로 유지(브랜드 정체성이 아니라 실제 지원 제도 설명이라 유지 대상으로 판단)
  - "우수 국비교육기관 선정"(연혁·실적 배너)도 실제 자격/수상 내역이라 유지
- **커리큘럼에 AI 실제 내용 반영**(사용자 확정, placeholder로): `courseDetail.js`의 `courseSkills`·4단계 `curriculum` 각 단계에 AI 관련 항목 추가(AI 활용 인터뷰 분석, Figma AI 기능, AI 프로토타이핑 툴 실습, AI 활용 목업・발표자료 제작), 진행 방식에 "AI 툴 실무 활용" 추가, 노트북 장비 FAQ 답변에 AI 툴 언급 추가. `instructors.js` 4명 전원의 specialty/bio에 담당 커리큘럼 단계에 맞는 AI 언급 보강(AI 리서치 툴, Figma AI, AI 프로토타이핑, AI 발표자료).
- `npm run lint` + `npm run build` 통과. 브라우저 실동작 확인은 미실시(도구 없음) — `http://localhost:5173`에서 홈 히어로 슬라이더, 헤더/푸터 로고, 과정 소개, 학원소개, 강사진 소개 페이지를 직접 확인 필요

## 지도 연동을 네이버 → 카카오맵으로 교체 (2026-08-04)
사용자가 카카오맵 API 키를 이미 발급받아 둔 상태라 네이버 대신 사용하기로 결정.
- `components/NaverMap.jsx`/`NaverMap.css` 삭제, `components/KakaoMap.jsx`/`KakaoMap.css` 신규 — 카카오맵 JS SDK를 `VITE_KAKAO_MAP_APP_KEY` 환경 변수로 동적 로드(`autoload=false` + `kakao.maps.load()` 콜백 패턴), 키 미설정/로드 실패 시 기존과 동일하게 placeholder로 대체
- `/location`, `/branches` 페이지의 지도 컴포넌트를 `KakaoMap`으로 교체(props는 동일하게 `lat`/`lng`/`name`)
- README 환경 변수 안내를 `VITE_KAKAO_MAP_APP_KEY`로 갱신 — 카카오 디벨로퍼스에서 발급 후 **해당 앱의 플랫폼(Web) 설정에 사용 도메인 등록 필요**(로컬은 `http://localhost:5173`)
- `npm run lint` + `npm run build` 통과. 사용자가 `.env.local`에 키 값을 넣어야 최종 확인 가능
- **키 적용 완료(2026-08-04)**: 사용자가 기존에 다른 앱에서 쓰던 카카오 JavaScript 키를 그대로 재사용(카카오는 앱 하나에 여러 도메인 등록 가능, 새 발급 불필요 — `http://localhost:5173` 플랫폼 등록 완료 확인받음). `.env.local`에 `VITE_KAKAO_MAP_APP_KEY` 추가, dev 서버 재시작 완료.
- **사용자가 브라우저에서 지도 렌더링 확인 완료(2026-08-04)**.

## 플로팅 상담 버튼 — 카카오톡 채널 추가 연결 (2026-08-04)
"카카오톡 상담은 준비중으로 막아두지 말고 채널 친구 추가로 안내하자"는 제안 반영.
- `contactInfo.js`에 `kakaoChannelId` 필드 추가(카카오톡 채널 관리자센터에서 확인하는 채널 공개 ID, 현재는 빈 값 placeholder)
- `FloatingConsult.jsx`: `kakaoChannelId`가 설정되면 `https://pf.kakao.com/{id}/friend`(카카오 채널 표준 "친구 추가" 링크 형식)로 새 탭 연결되는 실제 버튼으로 전환, 비어있으면 기존처럼 "준비중" 비활성 표시 — 지도 연동과 동일한 "설정 전 placeholder → 설정 후 자동 활성화" 패턴
- 실제 채널 공개 ID는 아직 미입력 상태 — 사용자가 카카오톡 채널을 개설하고 ID를 알려주면 반영 필요
- `npm run lint` + `npm run build` 통과

**추가 수정(2026-08-04, 같은 날 재요청)**: "이름은 그대로 '카카오톡 상담'으로 두고, 누르면 바로 이동시키지 말고 채널 추가를 안내하는 팝업을 띄워달라"는 요청으로 구조 변경.
- `components/KakaoConsultModal.jsx` 신규 — 네이티브 `<dialog>` 요소 사용(`showModal()`/`close()`, `::backdrop` 클릭 시 자동 닫힘 처리 포함). `kakaoChannelId` 설정 여부에 따라 "채널 추가하기" 버튼(새 탭, `pf.kakao.com/{id}/friend`) 또는 "준비 중" 안내 문구를 모달 안에서 분기
- `FloatingConsult.jsx`: 카카오 항목을 링크 대신 버튼으로 변경, 클릭 시 플로팅 메뉴를 닫고 모달을 엶(`showModal`). 항목 라벨은 다시 "카카오톡 상담"으로 유지
- `FloatingConsult.css`: `.floating-consult__item`이 이제 `<a>`/`<Link>`/`<button>` 셋 다 재사용되도록 버튼 기본 스타일 리셋 + 호버 셀렉터 통합, 더 이상 안 쓰는 `--soon` 관련 규칙 삭제(그 상태는 이제 모달 내부에서 처리)
- `npm run lint` + `npm run build` 통과

## reference 폴더 아이콘 적용 (2026-08-04)
사용자가 `reference/`에 로고 2종 + 아이콘 10종 + 카카오/국비 배지를 추가해두고 "알아서 적용해줘, 모르면 물어보고" 요청. 아이콘이 컬러풀 입체 스타일과 블랙 라인 스타일 두 종류가 섞여 있어(calendar/call이 두 스타일로 중복) 먼저 확인 후 진행.
- **사용자 결정**: 아이콘은 블랙 라인 스타일로 통일(ai/generative/calendar(1)/call(2)/support/writing/thumb-up). 로고는 텍스트("UIUX 아카데미"가 박혀있어 새 브랜드명과 불일치)를 빼고 UX 커서 마크 아이콘만 오려서 사용.
- **로고 처리**: 이 환경에 이미지 편집 도구가 없어 `npm install --no-save sharp`로 임시 설치해 `스몰로고.png`(2816x1536, 텍스트 없는 버전)의 여백을 트림하고 `public/logo-mark.png`(709x449 비율 유지, height 200px)로 저장 후 sharp는 다시 제거(package.json 영향 없음, `--no-save`). 헤더/푸터 로고에 아이콘+"AI UI/UX 디자인 아카데미" 텍스트 조합으로 적용.
- **아이콘 적용처**:
  - `public/icons/`에 정리: `support.png`(헤드셋), `call.png`(전화), `writing.png`(문의/작성), `kakao.png`(카카오톡 공식 아이콘, 컬러 유지), `funding-badge.png`(국비 배지), `ai-generative.png`(AI), `thumb-up.png`(성공/추천). `ai.png`·`calendar.png`는 복사만 해두고 아직 미사용(추후 필요시 사용 가능).
  - `FloatingConsult.jsx`: 메인 토글 버튼(💬→support.png, 딥그린 배경 위라 `filter: brightness(0) invert(1)`로 흰색 반전), 전화 상담(📞→call.png), 1:1 문의(✉️→writing.png), 카카오톡 상담(💛→kakao.png, 원색 유지)
  - `KakaoConsultModal.jsx`: 상단 아이콘 💛→kakao.png
  - `FundingBanner.jsx`: 🎓→funding-badge.png(흰 원형 배지 위에 배치)
  - `About.jsx`/`aboutContent.js`: 교육 철학 카드 3개에 아이콘 추가(AI 커리큘럼→ai-generative.png, 1:1 멘토 매칭→support.png, 취업 연계→thumb-up.png)
  - `Header.jsx`/`Footer.jsx`: 로고 텍스트 앞에 `logo-mark.png` 아이콘 추가
- **적용 안 한 부분**: CareerSupport 카드(🗂️📝🎤🤝💬), Funding 체크리스트(✓), Partners/Instructors 아바타 등은 매칭되는 아이콘이 없어 이모지/기존 스타일 그대로 유지 — 추가 아이콘 주시면 이어서 적용 가능
- `npm run lint` + `npm run build` 통과, dist에 `public/icons/`·`public/logo-mark.png` 정상 포함 확인. 브라우저 실동작 확인은 미실시 — `http://localhost:5173`에서 헤더/푸터 로고, 홈 국비 배너, 우측 하단 상담 버튼, 학원소개 페이지 확인 필요

## 향후 계획 — 데모 접속 안내 페이지 + 안내 팝업
- 완성된 홈페이지를 심사자 등이 한눈에 볼 수 있도록, **지원자/멘토/관리자 3개 역할로 각각 접속(로그인)할 수 있는 방법을 안내하는 페이지**를 별도로 제작 예정
  - 실제 로그인 구현(6단계) 이후, 역할별 테스트 계정 정보 또는 원클릭 데모 로그인 버튼 등을 안내하는 형태로 예상
- 이 안내 페이지로 유도하는 **팝업창**도 함께 제작 예정 (예: 첫 진입 시 "역할별로 둘러보기" 안내)
- 위 팝업 광고와는 별개 팝업으로, 노출 시점/문구는 착수 시 확정

## 헤더 메뉴 간격 조정 + 반응형 점검 (2026-08-04)
1. **메인 메뉴 간격**: "왼쪽으로 치우쳐 보인다"는 피드백 — `.site-header__nav`의 메뉴 그룹 간 `gap`을 `1.25rem → 2rem`으로 확대(`Header.css`). 모바일은 별도 `gap` 값을 쓰고 있어 영향 없음.
2. **반응형 전체 점검**: 이 환경엔 브라우저 도구가 없어 코드 감사로 진행. 대부분의 기존 페이지(About/Instructors/Partners/Portfolio 등)는 명시적 `@media` 없이 `grid-template-columns: repeat(auto-fit, minmax(...))` + `flex-wrap`으로 자연스럽게 반응형이 되는 패턴이고, 이는 이전에 브라우저로 테스트 완료된 방식 — 오늘 세션에 새로 만든 페이지(Funding/CareerSupport/Branches/KakaoConsultModal 등)도 같은 패턴을 따르고 있어 구조적으로는 안전함을 확인.
   - **버그 발견 및 수정 1**: `Reviews.css`의 카드 그리드가 `minmax(320px, ...)`로, 사이트 내 다른 그리드(220~280px)보다 훨씬 커서 320~375px 폭의 좁은 모바일에서 그리드가 줄어들지 못해 가로 스크롤이 생길 수 있는 문제 발견 → `280px`로 축소해 나머지 페이지와 동일한 안전 범위로 맞춤.
   - **버그 발견 및 수정 2**: 지난번 헤더 드롭다운 호버 수정(`NavDropdown.jsx`에서 `onMouseEnter`로 `open` 속성을 직접 제어)이 터치 기기에서 문제를 일으킬 수 있음을 발견 — 일부 모바일 브라우저는 첫 탭(tap) 시 `mouseenter`와 `click`을 연달아 합성 발생시키는데, 이 경우 호버 핸들러가 먼저 열고 곧바로 네이티브 클릭 토글이 다시 닫아버려 "탭해도 안 열리는" 것처럼 보일 수 있음 → `openOnHover`/`closeOnLeave`에 `window.innerWidth > 640` 조건을 추가해, `Header.css`의 모바일 분기 기준(640px)과 동일한 폭 이상에서만 호버 오픈이 동작하도록 제한(좁은 화면/터치 기기 대부분은 기존 클릭 방식만 사용).
- `npm run lint` + `npm run build` 통과. 실제 기기 확인은 못 했으니, 특히 `/reviews`와 헤더 드롭다운을 실제 모바일(또는 브라우저 개발자도구의 기기 시뮬레이터)에서 한 번 확인해 주시면 좋겠음.

## 상담/문의 시스템 개편 4건 + 배포 (2026-08-04)

1. **전화 상담 팝업 — 완료**: `contactInfo.js`에 `phoneContacts`(학원 대표번호/교육과정 담당자/국비지원 담당자) 추가. `PhoneConsultModal.jsx` 신규 — `KakaoConsultModal`과 동일한 `<dialog>` 패턴으로, 담당별 번호 목록 + 클릭 시 바로 전화 연결(`tel:`). `FloatingConsult.jsx`의 "전화 상담"을 직접 `tel:` 링크에서 이 팝업을 여는 버튼으로 교체.
2. **문의 게시판 비로그인 전체노출 버그 — 수정 완료**: 2026-07-31에 "게시판을 못 찾겠다"는 피드백으로 의도적으로 만들었던 "비로그인도 전체 목록 열람 가능(작성자 익명)" 구조를, 오늘 "개인정보 우려로 본인 글만 보이게 해달라"는 요청으로 되돌림. `InquiryBoard.jsx`가 `getAllInquiries()` 대신 `getMyInquiries()`를 사용하도록 변경, 비로그인 시 목록 섹션 자체를 렌더링하지 않음(로그인 유도 문구만 표시). "다른 지원자 글은 익명 표시" 안내 문구 삭제, 섹션명도 "등록된 문의" → "내 문의 내역"으로 변경. `InquiryContext`/데이터 구조는 변경 없음(조회 방식만 수정).
3. **비회원 상담 신청(콜백) 시스템 — 신규 구축**: 기존 `/consultation`(멘토 상담 신청)은 로그인한 학생을 분야별로 멘토와 자동 매칭하는 기능이라 그대로 두고, 비로그인 방문자를 위한 별도 흐름을 같은 페이지 안에 추가.
   - `data/callbackRequests.js` + `context/CallbackContext.jsx`(신규, `main.jsx`에 Provider 등록) — `user_id` 없이 이름/연락처/문의 내용만으로 접수, `contacted`(연락완료 여부) 하나로 상태 관리
   - `ConsultationRequest.jsx`: 비로그인 시 기존 "로그인이 필요합니다" 안내 대신 `GuestConsultationRequest`(이름·연락처·문의 내용 입력 폼)를 보여주고, 로그인하면 분야별 멘토 매칭도 가능하다고 안내. 로그인 시 기존 멘토 매칭 플로우는 그대로 유지
   - `Admin.jsx`에 "상담 신청 관리 (비회원)" 섹션 + 요약 통계 2종(연락대기/연락완료) 추가, "연락완료로 표시" 버튼 제공
   - `docs/DB스키마.md`에 `CallbackRequest` 테이블 정의 추가(회원 여부 무관한 리드 테이블이라 `Inquiry`/`Consultation`과 다르다는 점 명시)
4. **Vercel 배포 — 완료**: `npx vercel --prod`로 재배포. 처음 두 번은 대용량 `reference/` 폴더(로고 원본 PNG 등 약 9.9MB)가 매번 함께 업로드되며 `fetch failed`로 실패 → `.vercelignore` 신규 추가(`reference`, `docs` 제외)한 뒤 재시도해 성공. 배포 후 `curl`로 응답 코드(200)와 `<title>`이 최신 브랜드명으로 나오는지 직접 확인 완료.
   - **프로덕션 주소**: https://ui-ux-course-site.vercel.app
- `npm run lint` + `npm run build` 통과. 브라우저 실동작 확인(콜백 폼 제출, 문의 게시판 개인화, 전화 팝업)은 도구가 없어 미실시 — 배포된 사이트에서 직접 확인 권장.

## 헤더 호버 갭 버그 수정 + 상담 신청 결과 팝업화 (2026-08-04)
1. **드롭다운 호버 중 서브메뉴로 내려가면 사라지는 버그 — 수정 완료**: 원인은 `.site-header__dropdown-menu`의 `margin-top: 0.4rem`이 summary와 메뉴 사이에 진짜 빈 공간(어떤 요소에도 속하지 않는 영역)을 만들어서, 마우스가 그 틈을 지나는 순간 `<details>`가 hover 대상에서 벗어나 `mouseleave`가 먼저 발생하고 메뉴가 닫혀버리는 것이었음. `margin-top`을 0으로 없애고 시각적 여백은 메뉴 안쪽 `padding-top`(0.7rem)으로 대체 — 메뉴 박스 자체가 summary 바로 아래에 틈 없이 붙어서 hover가 끊기지 않으면서도, 보이는 간격은 이전과 동일하게 유지.
2. **상담 신청(비회원) 결과 팝업화 — 완료**: `ConsultationRequest.jsx`의 `GuestConsultationRequest`에서 제출 후 인라인 문구로 보여주던 "담당자가 곧 연락드리겠습니다" 안내를, `Kakao/PhoneConsultModal`과 동일한 `<dialog>` 패턴의 팝업으로 변경(제출 즉시 `showModal()`). 로그인 상태의 멘토 자동매칭 결과 메시지(다른 성격의 안내라 판단)는 기존 인라인 방식 그대로 유지.
- `npm run lint` + `npm run build` 통과. 브라우저 실동작 확인은 미실시 — 특히 헤더 드롭다운을 마우스로 천천히 아래로 내려가며 확인해 주시면 좋겠음.

## Vercel 재배포 (2026-08-04, 헤더 호버 버그 수정 + 상담 신청 팝업 반영)
`npx vercel --prod`로 재배포, `curl`로 200 응답 확인 완료.
- **프로덕션 주소**: https://ui-ux-course-site.vercel.app

---

## 다음 작업 목록 (2026-08-04 사용자 지정, 아직 미착수)
사용자가 다음 대화에서 진행하기로 정하고 기록만 해둔 항목들.

1. **모바일 텍스트 줄바꿈 최적화**: "글이 길어지면 어색하게 줄바꿈되어 두 줄로 애매하게 나오거나 한 글자만 다음 줄로 넘어가서 보기 안 좋음"이라는 피드백. 사이트 전반(카드 제목, 배너 문구, 버튼 등)에 걸친 문제로 보임.
   - **원인 추정**: 한글은 기본적으로 음절(글자) 단위로 줄바꿈이 가능해서(`word-break: normal`), 폭이 아슬아슬하게 안 맞으면 글자 하나만 다음 줄로 밀려나는 현상이 흔히 발생함.
   - **유력한 해결 방향**: `word-break: keep-all`을 전역(예: `index.css`의 `body` 또는 한글 텍스트가 오는 주요 컨테이너)에 적용하면, 줄바꿈이 음절 단위가 아니라 띄어쓰기(단어/구) 단위로만 일어나 이런 "한 글자만 삐져나오는" 현상이 대부분 해결됨. 다만 적용 후 문장이 길고 띄어쓰기가 드문 곳(버튼 라벨 등)은 오히려 컨테이너를 넘치거나 과하게 줄어들 수 있어, 적용 후 실제 화면(특히 320~375px 좁은 모바일)에서 페이지별로 확인 필요.
   - 이 환경엔 브라우저 도구가 없어 어느 페이지의 어떤 요소가 문제인지 특정하지 못했음 — 착수 시 브라우저(또는 스크린샷)로 문제 지점을 먼저 특정하고 진행하는 게 좋음.
2. **지원 마감 카운트다운에 시계 아이콘 적용**: `DeadlineCountdown.jsx`(과정 상세 + 홈 정원 뱃지 옆)에 시계 모양 아이콘 추가 요청.
   - **주의**: 현재 `public/icons/`에는 시계/알람 아이콘이 없음. `reference/3d-alarm.png`(알람시계)가 있지만 이건 지난번 "블랙 라인 스타일로 통일" 결정 때 컬러풀 입체 스타일이라 제외했던 파일 — 그대로 쓰면 스타일이 어긋남. 착수 시 ① 라인 스타일 시계 아이콘을 새로 받거나, ② `3d-alarm.png`를 예외로 컬러 그대로 쓸지, ③ 대체로 `calendar.png`(이미 `public/icons/`에 있는 라인 스타일, 현재 미사용)를 쓸지 사용자에게 확인 필요.
3. **아이콘 추가 적용 추천 (검토용, 아직 미적용)**: 현재 사용 가능한 아이콘(`public/icons/`: ai, ai-generative, calendar, call, funding-badge, kakao, support, thumb-up, writing) 기준으로 적용하면 좋을 만한 자리:
   - `CareerSupport.jsx` 지원 프로그램 카드 — 현재 이모지(🗂️📝🎤🤝💬) 사용 중. "이력서 첨삭" 카드는 `writing.png`, "커리어 상담" 카드는 `support.png`로 교체 가능(둘 다 이미 보유). "모의 면접"(마이크)·"파트너사 매칭"(악수)은 매칭되는 아이콘이 없어 별도 아이콘 필요.
   - `AchievementBanner.jsx`(홈/학원소개 실적 배너) — "취업률" 항목에 `thumb-up.png` 추가하면 시각적으로 강조 가능.
   - `Notices.jsx`/공지 카테고리 — "모집안내" 등 일정 관련 공지에 `calendar.png` 활용 가능(현재 미사용 아이콘).
   - `courseDetail.js` 커리큘럼의 AI 관련 항목(예: "AI 활용 인터뷰・설문 분석", "Figma AI 기능 활용")에 작은 `ai.png` 아이콘을 접두로 붙이는 것도 가능하나, 현재 topics가 단순 문자열 배열이라 `{icon, text}` 형태로 데이터 구조 변경이 먼저 필요함(작업 범위가 좀 더 큼).
   - `BottomCta.jsx`(하단 상담 신청 유도 배너)에 `call.png` 또는 `support.png` 작은 아이콘 추가로 시각적 포인트 주기 가능.

---

## 위 1~3번 작업 완료 + 배포 (2026-08-05)

### 1. 모바일 텍스트 줄바꿈 최적화 — 완료
`src/index.css`의 `body`에 `word-break: keep-all` 전역 적용. 한글이 음절 단위가 아니라 띄어쓰기(단어/구) 단위로만 줄바꿈되어 "한 글자만 다음 줄로 삐져나오는" 현상 대부분 해결. 기존 `white-space: nowrap`이 지정된 배지/탭/버튼 라벨(Header, Community, CourseDetail, Notices 등)은 애초에 줄바꿈 대상이 아니라 영향 없음 확인.

### 2. 지원 마감 카운트다운 시계 아이콘 — 완료
사용자 확인 없이 진행 필요해, 기존 라인 스타일(블랙 라인 통일 원칙)을 지키기 위해 옵션 ③(`calendar.png` 재사용, 새 아이콘 발급/컬러 아이콘 예외 없이)으로 결정. `DeadlineCountdown.jsx`에 `<img src="/icons/calendar.png">` 추가, `DeadlineCountdown.css`에 `.deadline-countdown__icon`(`width/height: 1em`)로 뱃지 폰트 크기에 맞춰 자동 스케일되도록 처리(`compact` 모드에서도 별도 분기 불필요).

### 3. 아이콘 추가 적용 — 완료
검토용으로 남겨뒀던 후보 중 매칭 아이콘이 있는 자리에 모두 적용:
- `CareerSupport.jsx` 지원 프로그램 카드: "이력서 첨삭"(id 2) → `writing.png`, "커리어 상담"(id 5) → `support.png`. `data/careerSupport.js`에 `iconImg` 필드 추가(있으면 이미지, 없으면 기존 이모지 렌더링하는 조건부 구조)해 나머지 3개 카드(🗂️🎤🤝)는 이모지 그대로 유지. `CareerSupport.css`에 `.career-support__card-icon--img`(padding으로 원형 배지 안에 맞춤) 추가.
- `AchievementBanner.jsx` "취업률" 항목: `data/achievements.js`의 해당 항목에 `icon: '/icons/thumb-up.png'` 추가, 딥그린 배경 위라 `filter: brightness(0) invert(1)`로 흰색 반전(`FloatingConsult`와 동일 패턴).
- `Notices.jsx` "모집안내" 카테고리 뱃지: 해당 카테고리일 때만 `calendar.png`를 조건부 렌더링(`notice.category === '모집안내'`), 나머지 카테고리(이벤트/시스템/일반)는 아이콘 없이 기존 그대로.
- `BottomCta.jsx`: 문구 앞에 `support.png` 추가(전화보다 상담 신청 문맥에 더 맞다고 판단해 `call.png` 대신 선택), 딥그린 배경이라 동일하게 흰색 반전.
- **적용 안 한 항목**: "모의 면접"(마이크)·"파트너사 매칭"(악수) 카드, `courseDetail.js` 커리큘럼 항목(topics 데이터 구조 변경이 선행돼야 해서 범위가 더 큼)은 매칭 아이콘 없음/작업 범위 문제로 이번엔 보류.

### 검증 및 배포
- `npm run lint`(기존 warning 6개는 `only-export-components`로 이번 변경과 무관) + `npm run build` 통과.
- `npx vercel --prod`로 배포, `curl`로 200 응답 + `<title>` 확인 완료. **프로덕션 주소**: https://ui-ux-course-site.vercel.app
- 이 환경엔 브라우저 자동화 도구가 없어 실제 화면 확인은 미실시 — 특히 좁은 모바일 폭(320~375px)에서 줄바꿈 개선 여부와, 새로 추가된 아이콘 5곳(카운트다운/CareerSupport 카드 2개/실적 배너/공지 뱃지/하단 CTA)의 정렬・크기를 배포된 사이트에서 직접 확인 필요.

## 후속 UI 수정 (2026-08-05, 같은 날 추가 요청 여러 건)
- FundingBanner 강제 `<br />` 제거(문장 중간 고정 줄바꿈이 폭에 따라 부자연스럽게 겹쳐 보이던 문제) — `keep-all`이 폭에 맞게 자연스럽게 줄바꿈하도록 전체 문장을 한 문단으로 변경.
- AchievementBanner 아이콘을 숫자 위(별도 줄)에서 숫자 앞(같은 줄)으로 이동 — 취업률 항목만 다른 3개 항목과 세로 정렬이 어긋나던 배치 버그 수정.
- 지원 마감 카운트다운 시계 아이콘을 라인 스타일(`calendar.png` 임시 대체) → 사용자가 지정한 `reference/3d-alarm.png`(컬러 3D 스타일, 예외 허용)로 교체, 이후 "3배 더 크게" 요청으로 `1em → 3em` 확대.
- `JourneyGraph.jsx`(홈 하단 "지원부터 취업까지" 곡선 그래프)에 호버 애니메이션 추가: 스텝에 마우스를 올리면 시작점부터 그 스텝까지 구간이 순차적으로(구간별 딜레이) 골드색으로 채워짐, 마우스를 떼면 원상복구. 이후 "너무 안 띄지 않는다"는 피드백으로 ①선 색상 진하게(#ffe08a→#f5b83d)+두껍게(3px→6px) ②호버 중인 스텝의 배지·마커에 테두리/색상 강조 추가. 추가로 구간 애니메이션 시간(딜레이 간격)이 어긋나 다음 구간이 이전 구간 끝나기 전에 시작되던 버그를 딜레이=지속시간(320ms)으로 맞춰 수정.
- AchievementBanner 아이콘 4종 전체 교체: 누적 수료생→`profit-growth.png`, 취업률→`self-employed.png`, 수강생 만족도→`tumb-up.png`, 우수 국비교육기관 선정→`excellence-awards.png`(전부 `reference/`에서 제공받아 `public/icons/`로 복사).
- 홈 `.home__capacity` 영역 개편: "실시간 잔여 정원 N명" 큰 배지를 삭제하고 `/course`로 연결되는 "지원하기" 버튼 추가. 이후 "잔여 N명" 보조 배지도 삭제 요청으로 최종적으로는 지원하기 버튼 + 지원 마감 카운트다운만 남음. 그 아래 있던 3개 소개 카드 섹션(`.home__features`: 실시간 잔여 정원/멘토 자동 매칭/마이페이지 관리)도 전체 삭제, 관련 미사용 CSS 정리.

## 지원 절차 개편 — 상담신청→멘토지정→멘토상담→지원하기→합/불 (2026-08-05)
"지원하기를 누르면 바로 합격으로 뜨는" 문제 제기로, 기존에 서로 분리되어 있던 두 시스템(과정 지원 / 멘토 상담)을 하나의 절차로 연결. 사용자 확인: ①지원하기 버튼 하드 게이트(상담 미완료 시 버튼 자체를 안 보여줌) ②합/불은 관리자 수동 심사(정원 자동 합격 로직 폐지).
- **게이트**: `CourseDetail.jsx`에서 `useConsultations().getMyConsultations()`에 `status: 'completed'`인 건이 하나라도 있어야 "지원하기" 버튼이 뜸. 없으면 현재 상담 상태(배정 대기중/매칭 완료/상담 진행중)를 보여주고 `/consultation`(상담 신청) 또는 `/mypage`(내 상담 현황)로 안내.
- **심사 상태 추가**: `utils/applicationStatus.js`에 `pending`("심사중")·`rejected`("불합격") 상태 추가. `ApplicationContext.applyToCourse`가 정원 기반으로 confirmed/waiting을 자동 결정하던 로직을 제거하고 항상 `pending`으로 접수하도록 변경(정원 검증은 관리자가 `confirmed`로 바꿀 때 `updateApplicationStatus`에서 기존 로직 그대로 수행).
- **재지원 허용**: `hasActiveApplication`/`CourseDetail`의 내 지원 조회 조건에 `rejected`도 `cancelled`처럼 제외 — 불합격 후에도 재상담・재지원이 가능하도록 함.
- **관리자**: `Admin.jsx`의 `STATUS_OPTIONS`에 `pending`/`rejected` 추가, 지원자 관리 섹션 안내 문구를 새 절차에 맞게 갱신.
- `MyPage.jsx`는 상태 라벨/뱃지 딕셔너리를 그대로 재사용해 별도 매핑 없이 새 상태 자동 반영, 다만 지원 취소 버튼은 `cancelled`/`rejected` 상태에서는 숨기도록 조건 보강.
- 홈(`/`)의 "지원하기" 버튼은 여전히 `/course`로 링크만 하고, 실제 게이트/신청 로직은 `CourseDetail.jsx`에 있어 별도 수정 불필요.
- `npm run lint` + `npm run build` 통과, `npx vercel --prod` 배포 완료. 브라우저 자동화 도구가 없어 실제 흐름(상담신청→매칭→상담완료→지원하기→관리자 합격/불합격 처리) 종단 테스트는 미실시 — 배포된 사이트에서 로그인 계정으로 직접 확인 필요.

---

## 디자인 업그레이드 + 이미지 전면 적용 + 지원 프로세스 보강 (2026-08-05, 이어지는 세션)
사용자가 "전체적으로 디자인 부분 업그레이드"를 요청 — 강도(정제/폴리싱 vs 과감한 리디자인)와 범위(공통 요소 먼저 vs 홈만)를 먼저 확인받아, **정제/폴리싱 + 공통 요소 우선**으로 진행. 이후 여러 차례에 걸쳐 애니메이션 다듬기·실제 이미지 적용·지원 프로세스 보강 요청이 이어졌고, 매 변경마다 로컬(`localhost:5173`)에서 먼저 확인받고 마지막에 한 번에 배포하기로 함.

### 1. 디자인 폴리싱 — 공통 요소
- `index.css`에 그림자 토큰(`--shadow-card`, `--shadow-card-hover`, `--shadow-btn`, `--shadow-btn-hover`) 신설. 사이트 전체 18개 CSS 파일에 중복돼 있던 동일한 카드 그림자 값(`0 1px 3px rgba(28,35,33,0.08)`)을 전부 `var(--shadow-card)`로 일괄 치환해 그림자 깊이감 통일.
- 버튼(`.btn--primary`)에 그림자 + 호버 시 `translateY(-1px)` 살짝 뜨는 효과 + 눌림 효과(`:active`) 추가. 이후 "화려하게" 요청으로 단색 배경을 대각선 그라디언트로 교체.
- 헤딩 자간(`letter-spacing`) 미세 조정, `img` 기본 리셋(`max-width:100%; display:block`), `::selection` 브랜드 컬러, `:focus-visible` 아웃라인(접근성), `scroll-behavior: smooth` 추가.
- 헤더/푸터 내비게이션 링크·드롭다운 summary에 호버 시 브랜드 컬러로 바뀌는 트랜지션 추가(기존엔 호버해도 반응 없었음). **드롭다운 메뉴 항목** 호버는 처음엔 옅은 배경색이라 "잘 안 보인다"는 피드백을 받아 초록 배경 + 흰 글씨로 대비를 확실히 높임.
- 히어로 슬라이더: 뷰포트 그림자 강화, 화살표・재생버튼・CTA 버튼에 호버 반응 추가, 각 슬라이드 배경에 은은한 골드 광원 블롭(`::before` radial-gradient) 추가.
- 수료생 포트폴리오 카드・파트너사 카드: 호버 시 살짝 떠오르는 효과(`translateY` + 그림자 강화) 추가.
- FAQ 아코디언: "+" 아이콘이 열릴 때 45도 회전하며 자연스럽게 "×"로 바뀌는 애니메이션으로 교체(기존엔 글자 자체가 순간 전환), 카드 호버 반응 추가.
- 실적 배너(`AchievementBanner`): 배경에 골드 광원 블롭 추가, 숫자를 흰색→골드 그라디언트 텍스트로 변경(`background-clip: text`). 이 패턴은 이후 `/course` 지원 현황 배너에도 재사용.
- 파트너사 마퀴: 좌우 끝이 자연스럽게 사라지는 `mask-image` 페이드 추가(기존엔 뚝 잘려 보임).
- 취업지원 카드 아이콘 배지: 단색 → 그라디언트 원형 배지로 교체.
- 홈 "수료생 포트폴리오" / FAQ 섹션 제목 앞에 초록→골드 그라디언트 세로 바 추가.

### 2. 여정 그래프(`JourneyGraph`) 호버 애니메이션 — 2차 보강
- **버그 수정**: 4개 구간을 각각 별도 `<path>`로 나누고 구간마다 "지금 시점 기준" `transition-delay`를 계산하던 기존 방식이, 호버 도중 다른 스텝으로 마우스를 옮기면 이미 진행 중이던 구간과 새로 계산된 딜레이가 어긋나 끊기거나 중간부터 재생되는 문제를 일으킴. **하나로 이어진 단일 `<path>` + 실제 곡선 길이(`getTotalLength`) 기반 `stroke-dashoffset`** 방식으로 재작성 — 목표 지점만 바꾸면 브라우저가 현재 위치에서 자동으로 부드럽게 재조준하므로, 스텝을 빠르게 넘나들어도 끊김이 사라짐.
- **강조 강화**: "애니메이션이 눈에 안 띈다"는 피드백으로 ①선 색상을 연한 골드(#ffe08a, 3px) → 진한 앰버(#f5b83d, 6px)로 진하고 굵게 ②호버 중인 스텝의 `STEP 0X` 배지 테두리・글자색과 그래프 위 마커(원)에 앰버 강조(테두리 생성 + 확대) 추가.

### 3. 홈 화면 정리
- `.home__capacity` 영역: "실시간 잔여 정원 N명" 큰 배지 삭제 → `/course`로 연결되는 "지원하기" 버튼 추가 → (후속 요청으로) 보조 "잔여 N명" 배지도 삭제. 최종적으로 지원하기 버튼 + 지원 마감 카운트다운만 남음.
- 그 아래 있던 3개 소개 카드 섹션(`.home__features`: 실시간 잔여 정원/멘토 자동 매칭/마이페이지 관리) 전체 삭제, 미사용 CSS 정리.
- 지원 마감 카운트다운 아이콘: 시계 아이콘이 없어 `calendar.png`로 임시 대체했다가, 사용자가 지정한 `reference/3d-alarm.png`(컬러 3D 스타일)로 교체 → "3배 더 크게" 요청으로 `1em → 3em` 확대.

### 4. 지원 프로세스 보강
- **`/course` 지원 현황 배너 신규**: 실적 배너와 같은 톤(짙은 초록 + 골드 그라디언트 숫자)으로 **전체 정원 / 합격 / 심사중(보류) / 남은 자리** 4개 표시. `ApplicationContext.getCourseWithCapacity`에 `pendingCount` 추가.
- **관리자 대시보드 요약 통계 보강**: 지원자 통계 카드가 합격/대기중/취소 3개뿐이고 "심사중"이 빠져 있던 걸 발견해 추가 — 이제 **합격 → 심사중 → 대기중 → 취소** 순서로 4개 표시(요청받은 목록 그대로, 불합격은 제외).
- **다음 작업 목록에 기록**(아래 "다음에 바꿀 내용" 참고): 합격 취소 시 대기 1순위 자동 승격 로직을 수동 전환으로 바꿔달라는 요청 — 아직 미착수.

### 5. 실제 이미지 전면 적용
사용자가 `reference/` 폴더에 이미지를 추가해줄 때마다 즉시 적용. 원본이 전부 수 MB짜리 고해상도 PNG라, 이 환경에 이미지 편집 도구가 없어 매번 `npm install --no-save sharp`로 임시 설치 → 크롭/리사이즈/압축 → `public/images/`에 저장 → **`npm uninstall sharp`로 즉시 제거**(package.json·package-lock.json에 흔적 없음, 이전 로고 작업 때와 동일한 방식).
- **히어로 슬라이더 5장 전부 적용**: `메인비주얼01~05.png` → `heroSlides.js`의 슬라이드 1~5 `image` 필드. 이미지 내용이 각 슬라이드 카피(AI 디자인/국비지원/1:1 멘토링/AI 프로토타이핑・포트폴리오/채용연계)와 정확히 맞아떨어져 순서 그대로 배치. 1920px 폭 JPEG로 리사이즈(장당 약 200~310KB).
- **포트폴리오 카드 사진 6장**: `포폴01~06.png`(전부 프로젝트 케이스 스터디 목업, 인물 사진 아님) → `portfolios.js`의 id 1~6에 순서대로 매칭. 기존 "이름 첫 글자 + 그라디언트" 플레이스홀더를 실제 이미지로 교체(`PortfolioCard.jsx`에 조건부 렌더링 추가), 썸네일 높이 120px→160px 확대, 호버 시 사진이 살짝 확대되는 효과 추가.
- **파트너사 로고 6개**: 로고 6개가 한 장에 2행 3열로 들어있는 이미지(`채용연계 파트너사.png`)를 grid 좌표로 분할 크롭 후 `trim()`으로 여백 제거 → 회사명이 이미지 라벨과 데이터(`partners.js`)에 이미 동일하게 있어 자동 매칭(테크노베이션/브릿지웍스/넥스트디자인/픽셀크래프트/스튜디오온/메이커스랩). `Partners.jsx` 카드 로고 배지(이니셜+그라디언트 → 실제 로고, 밝은 패널 위 `object-fit: contain`)와 홈 하단 파트너사 마퀴(회사명 앞에 작은 로고 아이콘 추가)에 적용.
- **강사 프로필 사진**: 1차로 받은 파일이 실제로는 `/instructors` 페이지 목업 스크린샷(카드 안에 작은 원형 아바타가 이미 들어간 상태)이라, 그 안에서 원형 사진 영역만 좌표 추정 → 크롭 → 미세 보정을 2차례 거쳐 추출. 이후 사용자가 **더 나은 화질의 파일로 재업로드**(인물별 이름 라벨이 붙은 깔끔한 2×2 정면 인물 사진) — 기존 파일을 완전히 대체해 재크롭・재적용(파일 경로는 동일하게 유지해 코드 변경 없이 이미지만 교체). `instructors.js`에 `photo` 필드 추가, `Instructors.jsx/css`에 조건부 이미지 렌더링(기존 이니셜 아바타는 `photo` 없을 때의 폴백으로 유지).

### 6. 기타 텍스트/구조 수정
- 헤더 메뉴 "지점 안내" → "다른 지점 안내"로 변경(본원 안내인 "오시는 길"과 구분되도록). `/branches` 페이지 제목도 동일하게 변경.

### 검증 및 배포
- 매 변경마다 `npm run lint` + `npm run build` 통과 확인. 이미지 처리에 쓴 sharp는 항상 임시 설치 후 완전히 제거해 `package.json`/`package-lock.json`에 흔적 없음.
- 이 환경엔 브라우저 자동화 도구가 없어, 사용자가 로컬 개발 서버(`localhost:5173`)에서 직접 눈으로 확인하며 여러 차례 피드백을 주고받은 뒤 최종 승인 → `npx vercel --prod` 배포. **프로덕션 주소**: https://ui-ux-course-site.vercel.app

---

## 합격 취소 시 자동 승격 로직 → 수동 전환 — 완료 (2026-08-06)
`ApplicationContext.jsx`의 `updateApplicationStatus`가 확정(confirmed) 좌석이 취소/변경으로 비면 `promoteNextWaiting`으로 대기(waiting) 1순위를 자동 합격 처리하던 로직을 제거. 이제 좌석이 비어도 자동으로 다음 사람이 합격되지 않고, **관리자가 대기중인 지원자를 직접 "합격"으로 변경**해야 승격된다(기존 정원 초과 가드는 그대로 유지되어 수동 변경 시에도 정원을 넘길 수 없음).
- `promoteNextWaiting` 함수 자체를 삭제(더 이상 어디서도 호출되지 않아 불필요)
- 빈 자리 인지 방법은 별도 알림 없이 기존 실시간 지표(`/course` 지원 현황 배너·관리자 대시보드 요약 통계의 "남은 자리")로 충분하다고 판단해 그대로 사용 — 새 UI 추가 없음
- `Admin.jsx`의 "지원자 관리" 섹션 안내 문구를 자동 승격 언급에서 "빈 자리가 생기면 대기중인 지원자를 직접 합격으로 변경" 안내로 수정
- `docs/기능명세서.md`의 "대기열 자동 승격" 절을 새 동작(관리자 수동 처리)에 맞게 갱신, 겸사겸사 1번 절(정원 초과 방지)도 2026-08-05에 이미 바뀐 실제 동작(항상 pending 접수 + 상담 완료 게이트)을 반영하도록 갱신(그동안 문서가 구버전 로직 그대로였음)
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 관리자 계정(admin@example.com/1234)으로 확정 지원 취소 → 대기중이던 지원자가 자동으로 합격되지 않고 그대로 "대기중"에 남는지, 대기중 지원자를 드롭다운으로 직접 "합격" 선택 시 정상 반영되는지 직접 확인 필요

## 과정 카탈로그 확장 + 지원자 관리 국비 과정 한정 + 시드 지원자 이름/지원일 랜덤화 (2026-08-06)
"국비교육 하나만 있는 컨셉인데, 원래 UI/UX 과정이 여러 개 있고 지금 모집 중인 게 국비 과정인 걸로 가자"는 방향 확정 후 3가지 요청 반영. 정원/승인/멘토매칭 등 기존 지원 로직은 전부 국비 과정(`course-ui-ux`, `DEFAULT_COURSE_ID`) 하나에만 묶어두고, 나머지 과정은 소개용 placeholder로만 추가(범위를 좁게 잡아 기존 로직을 건드리지 않음).

1. **다른 과정 소개 — 완료**: `data/courses.js`에 `status`('recruiting'/'in_progress')·`fundingType`('national') 필드 추가. 기존 과정(UI/UX 디자인 실무 과정)은 `status: 'recruiting'` + `fundingType: 'national'`로 지정, 신규로 과정 3개(UX 리서치 심화/프로덕트 디자인 부트캠프/디자인 시스템 마스터) 추가(제목·요약만 있는 소개용, 지원/정원 로직 없음). `CourseDetail.jsx` 히어로에 "국비지원 과정" 배지 추가, 하단에 "다른 교육과정" 섹션 신규 — 나머지 과정 3개를 카드로 나열(클릭/지원 불가). 최초엔 "모집예정" 배지만 달았다가, 같은 날 후속 요청으로 "이미 개설되어 진행 중이고 종료 후 다음 기수를 모집하는" 구조로 리프레이밍(아래 후속 항목 참고).
2. **시드 지원자 이름/지원일 랜덤화 — 완료**: `data/applications.js`의 시드 지원자 19명이 기존엔 `seed-user-1` 같은 raw id로 표시되고 지원일도 1분 간격 순차 데이터였음 → 성씨 10개×이름 20개 조합으로 랜덤 한글 이름을 생성해 각 지원건에 `name` 필드로 부여, `appliedAt`도 2026-06-15~2026-07-31 범위에서 무작위 생성하도록 변경(둘 다 앱 로드마다 새로 생성됨 — 자동 승격 로직이 이미 사라져서 순서에 기능적 의미가 없어졌기 때문에 랜덤화해도 안전).
3. **지원자 관리 국비 과정 한정 — 완료**: `Admin.jsx`의 "지원자 관리" 섹션(목록 + 요약 통계)이 `getAllApplications()`로 전체 지원건을 가져오던 것을, `DEFAULT_COURSE_ID`로 필터링하도록 변경(다른 과정은 아직 모집 전이라 실제로는 지원 데이터가 없지만, 향후 다른 과정에 지원 데이터가 생겨도 이 섹션이 섞여 보이지 않도록 명시적으로 범위를 고정). 지원자 이름도 raw id 대신 실제 이름이 보이도록 `applicantName()` 헬퍼 추가(시드 지원자는 새로 부여한 `name` 필드, 로그인 후 실제 지원한 사용자는 `mockUsers`에서 이름 조회, 둘 다 없으면 기존처럼 id로 폴백). 안내 문구에 "국비지원 과정 지원자만 표시" 명시.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/course` 페이지의 국비지원 배지·"다른 교육과정" 섹션, `/admin`의 지원자 관리 목록에 랜덤 한글 이름이 뜨는지 직접 확인 필요

## 모집 기간 명시 + 다른 과정 "진행중" 리프레이밍 (2026-08-06, 같은 날 후속 요청)
1. **국비과정 모집 기간 명시 + 지원일 정합화 — 완료**: `courses.js`의 국비 과정에 `applicationStart`('2026-07-15') 필드 추가(기존엔 마감일만 있었음). `CourseDetail.jsx` 모집요강 맨 위에 "모집 기간: 2026.07.15 ~ 2026.08.15" 행을 과정 데이터 기준으로 동적 표시. `data/applications.js`의 시드 지원자 `appliedAt` 무작위 범위를 하드코딩된 날짜 대신 `course.applicationStart` ~ `min(오늘, applicationDeadline)`로 계산하도록 변경(이미 확정된 시드 지원자가 모집 기간 밖이나 미래 날짜로 보이지 않도록, 데모 기준일 2026-08-06을 상한으로 사용) — 이제 모집 기간 데이터가 바뀌면 지원일 범위도 자동으로 따라간다.
2. **다른 과정 "모집예정" → "진행중" 리프레이밍 — 완료**: "모집예정이라고만 하면 부족하다, 이미 시작되어 진행 중이고 끝나면 모집 예정인 것으로 하자"는 피드백 반영. `courses.js`의 나머지 과정 3개 `status`를 `'upcoming'`→`'in_progress'`로 변경, `currentCohort: 1`·`progressNote`(예: "1기 과정 진행 중 · 종료 후 2기 모집 예정") 필드 추가. `CourseDetail.jsx`의 "다른 교육과정" 카드 배지를 "모집예정" → "N기 진행중"으로, 카드 하단에 `progressNote` 문구 추가. 섹션 설명 문구도 "이미 개설되어 각자의 기수가 진행 중이며, 종료 후 다음 기수 모집이 시작됩니다"로 수정.
- `npm run lint` + `npm run build` 통과.

## 멘토 소개 페이지 + 상담 매칭 관리자 화면 개선 (2026-08-06, 같은 날 후속 요청)
1. **멘토 소개 페이지(`/mentors`) 신규 — 완료**: 강사진 소개(`/instructors`) 페이지와 동일한 카드 그리드 패턴으로 신규 제작. `data/mentors.js`에 각 멘토(지수/태호/은채)의 `title`·`bio`·`career`·`photo`(현재 전부 null, 이니셜 아바타로 자동 대체) 필드 추가. 헤더 "소통공간" 드롭다운에 "멘토 소개"를 "멘토 상담" 바로 위에 추가, `/consultation` 페이지에도 "멘토 소개 보기" 상호 링크 추가.
2. **상담 매칭 모니터링 학생 이름 랜덤화 — 완료**: 관리자 대시보드의 "상담 매칭 모니터링" 표가 `seed-student-1` 같은 raw id를 그대로 보여주던 것을, 지원자 이름 랜덤화 때 쓴 로직을 `data/randomNames.js`로 공용 분리해 `data/consultations.js` 시드 3건에도 동일하게 `name` 필드 부여. `Admin.jsx`에 `consultantStudentName()` 헬퍼 추가(시드는 `name` 필드, 실제 로그인 학생은 `mockUsers`에서 이름 조회) — `applications.js`도 이 공용 유틸을 쓰도록 함께 리팩터링.
3. **상담 신청 관리 — 요청 내용 기반 스마트 매칭 — 완료**: "상담요청 내용에 따라 관리자가 적절한 멘토를 매칭할 수 있게"라는 요청을, 자유 텍스트(`content`)를 자동 해석하는 대신 이미 구조화된 필드인 상담 분야(`category`)를 기준으로 구현(실제 문의 내용도 표에서 바로 읽을 수 있도록 노출). `Admin.jsx` "상담 매칭 모니터링" 표에 "요청 내용" 열 추가(길면 줄바꿈, 모바일에서는 라벨 아래 별도 줄로 표시). 배정 대기중(`requested`) 건의 "멘토 선택" 드롭다운을 전체 멘토를 그냥 나열하던 것에서, 상담 분야와 전문분야가 일치하는 멘토를 상단에·그 안에서는 현재 부하가 적은 순으로 정렬하고 각 옵션에 "· 분야 일치"와 "진행중 N/M" 표시를 추가해 관리자가 요청 내용에 맞는 멘토를 한눈에 고를 수 있도록 개선.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/mentors` 페이지, `/admin`의 상담 매칭 모니터링(요청 내용 열, 학생 이름, 정렬된 멘토 선택 드롭다운) 직접 확인 필요

## 다음 논의 필요 (2026-08-06, 사용자 질문 — 아직 결론 안 남)
- **포트폴리오(1~3기) 범위**: 지금처럼 국비 과정에만 한정할지, 다른 과정들도 완료 기수가 생기면 샘플을 추가해 전체 과정 포트폴리오로 넓힐지 미결정. 다른 과정들이 "이미 진행 중이나 아직 완료된 기수가 없다"는 설정(위 리프레이밍 항목 참고)과 맞물려 있어, 다른 과정에 아직 포트폴리오가 없는 게 논리적으로는 자연스럽다는 의견을 전달함 — 사용자 최종 결정 필요.
- **오시는 길(`/location`) 등에 추가 연동 가능한 서비스**: 카카오맵처럼 붙일 수 있는 다른 서비스(캘린더 담기, 길찾기 딥링크, 카카오 공유하기 등) 후보를 논의했으나 사용자가 "일단 보류"로 결정(2026-08-06) — 착수 안 함, 필요 시 다시 논의.

## 세션 중 누락 항목 점검 + 강사 사진 재크롭 + 멘토 소개 위치 조정 + 학원 일정 페이지 (2026-08-06, 같은 날 후속 요청)

1. **누락 항목 점검 — 완료(누락 없음)**: "중간에 추가 요청한 것 중 진행이 누락된 게 있으면 다시 진행해달라"는 요청으로 이번 세션의 모든 지시사항을 재점검. 자동 승격→수동 전환, 과정 카탈로그(모집기간·진행중 리프레이밍 포함), 지원자/상담 이름 랜덤화, 지원자 관리 국비 과정 한정, 멘토 소개 페이지, 상담 스마트 매칭까지 전부 실제 코드에 반영되어 있음을 코드 확인으로 재검증. 두 가지 "질문"(포트폴리오 범위·연동 서비스)은 의견만 전달하고 답변 대기 중이었던 것이라 별도 누락은 아님(위 "다음 논의 필요" 참고, 연동은 오늘 보류로 결론).
2. **강사진 프로필 사진 재크롭 — 완료**: 기존 4장(한지민/윤성민/조은비/강태오)이 전부 400x300 비율로, 원본 2x2 그리드 이미지(`reference/강사 프로필 사진.png`)에서 얼굴이 중앙이 아니라 한쪽으로 치우친 채 크롭되어 있었음(원형 아바타 안에서 얼굴이 잘려 보이는 문제). `npm install --no-save sharp`로 원본에서 인물별 얼굴 중심 800x800 정사각형 영역을 다시 계산해 추출 → 500x500으로 리사이즈해 기존 파일 경로 그대로 덮어씀(코드 변경 없이 이미지만 교체). 얼굴이 원형 아바타 중앙에 오는지 4장 모두 육안 확인 완료. 작업 후 sharp는 다시 `npm uninstall`로 제거(package.json/package-lock.json에 흔적 없음 확인).
3. **멘토 소개를 강사진 소개 옆으로 이동 — 완료**: `/mentors`(멘토 소개) 메뉴를 "소통공간" 그룹(멘토 상담 옆)에서 "학원안내" 그룹의 "강사진 소개" 바로 뒤로 이동(`Header.jsx`). `/instructors` 페이지 소개 문구에도 "멘토 소개" 상호 링크 추가(기존에 `/consultation`→`/mentors` 링크는 이미 있었음).
4. **연동 서비스 추가**: 보류 결정, 착수 안 함.
5. **학원 일정 페이지(`/schedule`) 신규 — 완료**: "달력 모양으로 며칠부터 며칠까지 어떤 강의가 진행되는지, 모집일자, 행사 등을 한 번에 볼 수 있는 페이지" 요청으로 신규 제작. 외부 캘린더 라이브러리 없이 순수 날짜 계산으로 월간 그리드를 직접 구현(요일 헤더 + 이전달/다음달 이동 버튼, 2026년 8월을 데모 시작 월로 고정).
   - `data/schedule.js` 신규: 일정을 `recruiting`(모집)·`course`(수업)·`event`(행사) 3가지 타입으로 구분해 9건 배치(국비 과정 4기 모집·수업, 나머지 3개 과정의 진행 중인 1기 수업, 수료식·입학설명회·발대식 등 행사, 디자인 시스템 마스터 2기 모집 예고). 국비 과정 모집 기간은 하드코딩하지 않고 `courses.js`의 `applicationStart`/`applicationDeadline`을 그대로 참조해 두 데이터가 어긋나지 않도록 함.
   - `Schedule.jsx`: 달력 각 날짜 칸에 그 날짜에 걸쳐 있는 일정을 타입별 색상 pill로 표시(최대 2개, 초과분은 "+N"), 오늘(데모 기준일 2026-08-06) 칸은 테두리로 강조. 달력 아래에 "이번 달 일정" 목록을 배지+제목+기간으로 별도 정리.
   - 헤더 "학원안내" 드롭다운에 "학원 일정"을 "멘토 소개"와 "공지사항" 사이에 추가.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/instructors`(사진 정렬), `/mentors`가 학원안내 드롭다운에 있는지, `/schedule`(달력 표시·월 이동·일정 목록) 직접 확인 필요

## 다른 과정 일정 노출 + 지원자 일괄 상태변경 + 비회원 상담 멘토 지정 + 실적 수치 정합화 (2026-08-06, 같은 날 후속 요청)

1. **다른 교육과정 진행 일자 + 다음 모집 예정 기간 노출 — 완료**: `courses.js`의 나머지 과정 3개에 `progressStart`/`progressEnd`(현재 진행 중인 기수 기간)·`nextRecruitingStart`/`nextRecruitingEnd`(다음 기수 모집 예정 기간) 필드 추가. `CourseDetail.jsx`의 "다른 교육과정" 카드에 "진행 기간"·"다음 기수 모집 예정" 두 줄을 추가로 표시. `data/schedule.js`를 이 새 필드에서 파생되도록 리팩터링(기존엔 일부 날짜가 `courses.js`와 별개로 하드코딩되어 있어 둘이 어긋날 수 있었음) — 이제 과정 소개 페이지와 `/schedule` 달력이 항상 같은 날짜를 보여준다. 다른 과정 2개(UX 리서치 심화/프로덕트 디자인 부트캠프)에 빠져 있던 "다음 기수 모집" 일정도 `/schedule`에 새로 추가됨(디자인 시스템 마스터만 있었음).
2. **지원자 관리 전원 일괄 상태변경 — 완료**: `Admin.jsx` "지원자 관리" 표 위에 상태 선택 드롭다운 + "전원 일괄 변경 적용" 버튼 추가. 클릭 시 현재 표시된(국비 과정) 지원자 전원에게 선택한 상태를 순차 적용, 실수 방지를 위해 적용 전 인원수·상태를 보여주는 확인창(`window.confirm`)을 거침. 정원 초과 가드는 기존 `updateApplicationStatus` 내부 로직을 그대로 재사용하므로, "전원 합격"을 선택해도 정원을 넘는 인원은 자동으로 걸러짐(별도 처리 불필요).
3. **비회원 상담 신청 관리자 멘토 지정 — 완료**: 비회원 콜백 요청(`CallbackContext`)에 `mentorId` 필드 추가(seed 데이터·`createCallbackRequest` 모두 `null`로 시작). `assignCallbackMentor(requestId, mentorId)` 액션 신규 추가. `Admin.jsx` "상담 신청 관리 (비회원)" 각 항목에 담당 멘토 선택 드롭다운 추가(멘토별 현재 부하도 함께 표시) — 비회원 신청은 상담 분야가 없어 자동/추천 매칭 대상이 아니므로, 관리자가 문의 내용을 보고 직접 지정하는 방식으로 구현.
4. **실적 수치 정합화 — 완료**: "진행된 과정이 별로 없는데 누적 교육생 1200+는 말이 안 된다"는 지적으로 전체 수치 재점검. `achievements.js`의 "누적 수료생"이 1200+였던 것을, 실제 설정(2025년 설립 후 현재 4기 모집 중 → 1~3기만 수료, 과정 정원 20명 × 3개 기수)에 맞춰 60+로 수정(근거를 주석으로 남김). 다른 과정 3개는 아직 1기가 진행 중이라 수료생이 0명이므로 포함하지 않음. `취업률 92%`·`수강생 만족도 4.8/5.0`(비율/평점이라 표본 수와 무관하게 형식상 모순 없음)·`우수 국비교육기관 선정 2025`(연혁 페이지와 일치)·강사/멘토 프로필의 "포트폴리오 컨설팅 200건 이상" 등(입사 전 경력을 포함한 개인 이력이라 이 아카데미 수료생 수와 직접 비교 대상 아님)은 점검 결과 내부적으로 모순되지 않아 그대로 둠.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/course`의 "다른 교육과정" 날짜, `/schedule`의 새 모집 일정, `/admin`의 일괄 변경 버튼과 비회원 상담 멘토 지정 드롭다운, 홈 실적 배너의 "누적 수료생 60+" 직접 확인 필요

## 홈 팝업 광고 구현 (2026-08-06, 같은 날 후속 요청)
2026-08-04 "향후 계획"에 적어뒀던 팝업 광고를 실제로 구현. 내용은 현재 모집 중인 국비지원 과정(`course-ui-ux`), 버튼 3개(나가기/상세보기/다시보지 않기) 구성으로 확정.
- `components/HomePromoModal.jsx`(신규) — 기존 `KakaoConsultModal`/`PhoneConsultModal`과 동일한 네이티브 `<dialog>` 패턴(`showModal()`/`close()`, `::backdrop` 클릭 시 닫힘). 홈 진입 시 자동으로 열리며, 국비지원・N기 모집중 배지 + 과정명 + 요약 + `DeadlineCountdown`(지원 마감 카운트다운, 기존 컴포넌트 재사용)을 보여줌.
- 버튼 3개 동작 구분: **나가기**(그냥 닫기, 다음 방문/새로고침 시 다시 노출) / **상세보기**(닫고 `/course`로 이동) / **다시보지 않기**(`localStorage`에 영구 저장 후 닫기 — 이후 방문부터는 아예 뜨지 않음). "다시보지 않기"는 실수로 누르지 않도록 버튼 대신 밑줄 텍스트 링크로 덜 두드러지게 배치.
- `Home.jsx`에 마운트, 이미 페이지에서 조회하던 `course`(`getCourseWithCapacity(DEFAULT_COURSE_ID)`)를 그대로 props로 전달해 데이터 중복 조회 없음.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 홈 진입 시 팝업이 뜨는지, 버튼 3개 각각의 동작(특히 "다시보지 않기" 이후 새로고침해도 안 뜨는지)을 직접 확인 필요 — `localStorage.removeItem('home-promo-dismissed')`로 초기화 가능

## 플로팅 상담 버튼 색상 변경 + 팝업 광고 스타일 리디자인 (2026-08-06, 같은 날 후속 요청)

1. **플로팅 상담 버튼 색상 — 완료**: "배경(사이트 전체 딥그린 톤)과 색이 비슷해서 안 띈다"는 피드백에 3가지 방향(①골드 포인트 컬러로 교체 ②기존 초록 유지 + pulse 애니메이션 ③둘 다 적용)을 제시했고, 사용자가 ①골드를 선택. `FloatingConsult.css`의 `.floating-consult__button` 배경을 `var(--color-primary)`(딥그린) → 골드 그라디언트(`#f5b83d → #d99a1f → #c17f16`, 기존 `JourneyGraph`・실적 배너에서 이미 쓰던 앰버 계열과 통일)로 변경, 그림자도 앰버 톤으로 맞추고 hover 시 살짝 떠오르는 효과 추가. 골드는 사이트에서 지금까지 "강조/성과" 용도로만 쓰였는데 상시 노출되는 UI 요소에 처음 쓰이게 됨 — 다른 골드 강조 요소와 시각적으로 경쟁하지 않는지 실제 화면에서 함께 확인 권장.
2. **팝업 광고 스타일 리디자인 — 완료**: "모바일 앱 팝업 같다, 가운데 사진 넣을 예정이니 전체적으로 스타일을 바꿔달라"는 요청 반영. `HomePromoModal.jsx/css` 구조 변경:
   - 상단에 풀블리드 히어로 영역 신규 추가(높이 12rem, 딥그린 그라디언트 + 은은한 골드 광원 블롭 — 히어로 슬라이더・실적 배너와 같은 패턴). 사진이 아직 없어 흰 원형 배지 안에 `funding-badge.png` 아이콘을 크게 배치한 placeholder로 표시, 좌측 하단에 "국비지원 과정" 리본 태그.
   - `courses.js`의 국비 과정에 `promoImage` 필드 추가(현재 `null` → placeholder 자동 노출, 실제 사진 경로를 채우면 `<img>`로 자동 교체 — `heroSlides.js`/`instructors.js`의 `image`/`photo: null` placeholder 패턴과 동일).
   - 모달 전체 모서리를 더 둥글게(16px→20px), 그림자를 더 크고 부드럽게, `::backdrop`에 `backdrop-filter: blur`를 추가해 뒷배경이 흐려지는 웹 라이트박스 느낌으로 변경(기존엔 단순 반투명 검정 오버레이만 있어 상대적으로 "앱 알림창"처럼 보였음).
   - 닫기(✕) 버튼을 히어로 이미지 위로 옮기고 반투명 어두운 원형 배경을 줘서, 사진이 무엇이 오든 항상 잘 보이도록 처리.
   - 본문 영역 패딩・제목 크기 확대, 기존 버튼 3개(나가기/상세보기/다시보지 않기) 배치와 동작은 그대로 유지.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 우측 하단 상담 버튼 색상, 홈 팝업의 히어로 영역(placeholder 아이콘・리본)・모서리・배경 블러 직접 확인 필요. **실제 사진이 준비되면 `courses.js`의 국비 과정 `promoImage` 필드에 이미지 경로만 채우면 코드 수정 없이 반영됨.**

## "AI・에이전트" 키워드 노출 + AI 툴 로고 섹션 신규 (2026-08-06, 같은 날 후속 요청)
1. **"AI・에이전트" 키워드 자연스럽게 노출 — 완료**: "넣고 싶은데 자연스러운 자리가 없을까, 이미지도 만들 것"이라는 질문에 `/course`(과정 소개) 페이지가 커리큘럼・툴 활용을 이미 다루고 있어 가장 자연스러운 자리라고 판단, "이런 걸 배워요" 섹션 바로 아래에 "실무에서 쓰는 AI 툴" 섹션을 신규 추가하며 인트로 카피에 "에이전트" 키워드를 녹임("AI 에이전트에게 반복 작업을 맡기고 디자이너는 판단과 의사결정에 집중"). `courseSkills`에 "AI 에이전트 활용" 태그, `courseOverview`의 "진행 방식"에도 "AI 툴・에이전트 실무 활용"으로 반영.
   - `data/aiTools.js` 신규: `aiToolsIntro`(제목/설명/`image: null`)로 이 섹션 상단 인트로 카피+이미지 관리 — 사용자가 "이미지도 같이 만들어서 넣을 것"이라고 해, `promoImage`와 동일한 `image: null` placeholder 패턴 적용(현재는 `ai-generative.png` 아이콘이 그라디언트 위에 표시되다가, 경로를 채우면 자동으로 실제 이미지로 교체됨).
2. **AI 툴 로고 섹션 — 완료**: 같은 섹션 하단에 Claude・ChatGPT・Gemini・Figma AI 4개 툴을 카드형으로 배치, 각 카드에 로고(현재 `logo: null` → 이니셜 원형 배지로 자동 대체)・제작사・실무 활용 예시(예: Claude "AI 에이전트로 반복 업무 자동화") 표시. 실제 로고 이미지 파일이 없어(외부 브랜드 자산이라 임의로 생성하지 않음) `partners.js`가 실제 로고 받기 전 썼던 "이니셜+그라디언트" placeholder와 동일한 패턴 적용 — `aiTools.js`의 각 항목 `logo` 필드에 경로만 채우면 자동으로 실제 로고 이미지로 교체됨.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/course`의 "실무에서 쓰는 AI 툴" 섹션(인트로 placeholder 이미지, 4개 툴 카드) 직접 확인 필요. **인트로 이미지·툴 로고가 준비되면 `data/aiTools.js`의 `image`/`logo` 필드에 경로만 채우면 코드 수정 없이 반영됨.**

## 헤더 드롭다운 리디자인 + 실제 이미지 2건 적용 + 문구 수정 (2026-08-06, 같은 날 후속 요청)
1. **헤더 드롭다운 메뉴 스타일 리디자인 — 완료**: 스크린샷으로 "메인메뉴 서브메뉴 구성/모양이 안 예쁘다"는 피드백을 받아 `Header.css`의 `.site-header__dropdown-menu`를 리디자인. 폭을 `max-content`(내용에 딱 맞는 좁은 폭) → `15rem` 고정폭으로 넓히고, 모서리를 더 둥글게(8px→16px), 그림자를 더 크고 부드럽게 키움. 상단에 브랜드 그라디언트(초록→골드) 포인트 바를 추가해 카드형 패널처럼 보이게 함. 각 메뉴 항목은 둥근 필(pill) 모양 + 우측에 호버 시 나타나는 "›" 화살표(슬라이드 인 트랜지션)를 추가해 미세한 인터랙션을 줌. **호버 시 색 대비는 기존 방식(2026-07-31에 "옅은 배경색이라 잘 안 보인다"는 피드백으로 이미 한 번 강한 대비로 바꾼 이력이 있음, [[헤더 메뉴 구조]] 참고)을 유지하되, 단색 블록 대신 버튼과 동일한 대각선 그라디언트 채움으로 톤을 살짝 고급스럽게 바꿈** — 가독성을 낮추지 않으면서 스타일만 개선. 모바일(정적 인라인 목록)은 카드 장식(상단 포인트 바)만 숨기고 기존 동작 그대로 유지.
2. **홈 팝업 히어로 이미지 실사진 적용 — 완료**: 사용자가 `reference/교육과정(팝업).png`(수강생들이 랩탑으로 실습하는 강의실 사진, Figma 화면 노출)을 추가해줘서 배너 비율(약 2.33:1)에 맞게 인물・Figma 화면이 잘 보이는 영역으로 크롭 → `public/images/promo-course.jpg`로 저장, `courses.js`의 `promoImage`를 `null` placeholder에서 이 경로로 교체(2026-08-06 오전에 만든 `HomePromoModal`이 자동으로 실제 사진을 표시하도록 이미 설계돼 있어 컴포넌트 코드 수정 없이 반영됨).
3. **"실무에서 쓰는 AI 툴" 인트로 이미지 실사진 적용 — 완료**: `reference/AI 에이전트.png`("AI 에이전트" 라벨이 박힌 홀로그램 일러스트)를 정사각형(6rem 썸네일)에 맞게 라벨・얼굴이 보이는 영역으로 크롭 → `public/images/ai-agent-intro.jpg`로 저장, `data/aiTools.js`의 `aiToolsIntro.image`를 `null`에서 이 경로로 교체.
4. **"취업 연계 파트너사 (예시)" 문구 수정 — 완료**: `PartnerMarquee.jsx`의 홈 하단 파트너사 마퀴 라벨에서 "(예시)" 삭제 → "취업 연계 파트너사 →"로 변경.
5. **홈 팝업 "다시보지 않기" 이후 안 보이는 문제 — 코드 문제 아님**: `localStorage`의 `home-promo-dismissed` 값이 사용자 브라우저에 영구 저장된 것이라 코드 수정으로 해결할 사안이 아님. 브라우저 콘솔에서 `localStorage.removeItem('home-promo-dismissed')` 실행 또는 사이트 데이터 삭제로 초기화 가능하다고 안내함(코드는 2026-08-06 오전 구현 당시 의도대로 정상 동작 중).
- 이미지 크롭은 기존 방식과 동일하게 `npm install --no-save sharp`로 임시 설치 → 크롭/리사이즈 → 완료 후 `npm uninstall sharp`로 제거(흔적 없음 확인). `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 헤더 드롭다운 새 스타일, 홈 팝업 실사진, `/course`의 AI 인트로 실사진, 홈 하단 파트너사 문구 직접 확인 필요.

## 다음 확인 필요 — AI 툴 로고(Claude・ChatGPT・Gemini・Figma AI) (2026-08-06)
사용자가 "실제 아이콘 직접 넣어줄 수 있어?"라고 물어봄 — 이 환경에는 인터넷에서 이미지를 가져오는 도구가 없고, 각 사의 공식 로고는 브랜드 자산이라 임의로 생성/재현하지 않는 것으로 판단해 보류. `partners.js`가 실제 로고 받기 전 이니셜 placeholder를 쓰다가 사용자가 `reference/`에 로고 파일을 넣어주면 그때 적용했던 것과 동일한 방식 제안함 — 사용자가 각 툴의 로고 이미지 파일을 `reference/`에 추가해주면, 그때 크롭/적용 가능.

## 비회원 상담 멘토 지정 실제 반영 + AI 인트로 이미지 확대 + 팝업 재노출 안내 상세화 (2026-08-06, 같은 날 후속 요청)
1. **비회원 상담 멘토 지정이 실제로 시스템에 반영되도록 수정 — 완료**: 기존엔 관리자가 비회원 상담 신청에 멘토를 지정해도 `CallbackRequest.mentorId` 필드에 저장만 될 뿐, 그 멘토에게는 아무것도 보이지 않는 "죽은 지정"이었음. `MentorDashboard.jsx`에 `useCallbackRequests()`를 연결해 "담당 비회원 상담 신청" 섹션을 신규 추가 — 자신에게 배정된 비회원 신청을 이름・연락처・문의 내용과 함께 보여주고, 멘토가 직접 "연락완료로 표시" 처리 가능(관리자 쪽 처리와 동일한 `markContacted` 공유). 정규 상담(`Consultation`)의 부하 계산(`getMentorLoad`)에는 포함하지 않음 — 비회원 신청은 분야 매칭 없이 관리자가 임의 배정하는 별도 트랙이라 자동매칭 부하 산정과 섞이면 왜곡되기 때문. `Admin.jsx`의 안내 문구에도 "지정하면 해당 멘토 대시보드에도 노출됨" 명시.
2. **"실무에서 쓰는 AI 툴" 인트로 이미지 확대 — 완료**: "전체가 나오게 이미지 영역을 더 크게 키워달라"는 요청으로, 기존 좌측 6rem 정사각형 썸네일(원본을 심하게 크롭해 일러스트 대부분이 잘려나갔음) 대신 **카드 상단 전체 폭 16:9 배너**로 레이아웃 변경(`HomePromoModal`의 히어로 이미지와 같은 언어). `reference/AI 에이전트.png`를 원본 대비 훨씬 넓게(가로 전체, 세로 상단・하단 여백만 트림) 재크롭해 `public/images/ai-agent-intro.jpg`로 교체 — 이제 홀로그램 인물 전신・두 개의 말풍선・"AI 에이전트" 라벨이 전부 한 화면에 들어옴.
3. **팝업 "다시보지 않기" 초기화 방법 상세 안내 — 완료(가이드 제공, 코드 변경 아님)**: 이전에 안내한 콘솔 명령을 "어디서 실행하는지" 구체적으로 설명 요청받아 아래와 같이 답변함(문서화 목적으로 여기 기록):
   1. `localhost:5173` 사이트가 열린 브라우저 탭에서 **F12**를 누르거나, 페이지 빈 곳에서 마우스 우클릭 → **"검사"(Inspect)** 클릭
   2. 열린 개발자도구 상단 탭에서 **"Console"(콘솔)** 탭 선택
   3. 콘솔 하단의 입력줄(`>` 표시)을 클릭해 `localStorage.removeItem('home-promo-dismissed')`를 붙여넣고 **Enter**
   4. 페이지 새로고침(F5) — 팝업이 다시 뜸
- 이미지 재크롭은 기존과 동일하게 `npm install --no-save sharp` → 크롭/리사이즈 → `npm uninstall sharp`로 제거(흔적 없음 확인). `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/mentor`(admin@example.com이 아닌 멘토 계정, jisu@example.com/1234)로 로그인해 "담당 비회원 상담 신청" 섹션 노출 확인, `/course`의 AI 인트로 배너 이미지 확인 필요.

## /schedule 2025년 일정 공백 수정 (2026-08-06, 같은 날 후속 요청)
"2025년에 우수 국비교육기관으로 선정됐다면서 그 해 일정이 하나도 없는 건 말이 안 된다"는 지적 반영. 그동안 `/schedule`은 4기(현재 모집)와 다른 3개 과정의 진행/차기모집만 다뤄, 1~3기가 실제로 언제 모집・진행・수료했는지에 대한 이력 데이터 자체가 없었음(4기가 모집 중이려면 1~3기가 이미 끝났어야 하는데, 그 흔적이 캘린더 어디에도 없던 상태).
- `data/schedule.js`에 `nationalCourseHistory` 배열 신규 추가 — 기존에 이미 있던 "3기 수료식"(2026-08-07) 날짜를 고정점으로 두고 역산해, 1~3기 모집・수업・수료식과 "우수 국비교육기관 선정"(2025-12-10) 행사까지 총 9건의 2025~2026 이력을 채움: 1기 모집(2025-08)→1기 수업(2025-09~11)→1기 수료식→**우수 국비교육기관 선정(2025-12)**→2기 모집(2025-12)→2기 수업(2026-01~03)→2기 수료식→3기 모집(2026-04)→3기 수업(2026-05~08, 기존 3기 수료식 날짜로 자연스럽게 이어짐)→(기존 데이터인 4기 모집・발대식・수업으로 연결).
- `aboutContent.js`의 연혁("2025년 설립, 1기 모집 시작" / "2025년 우수 국비교육기관 선정")과 이제 실제로 맞물리는 날짜가 생겨, `/schedule`에서 2025년으로 이전달 이동하면 더 이상 빈 달력이 아님.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/schedule`을 2025년 8월~12월까지 "이전달"로 이동하며 1기 모집・수업・수료식과 우수 국비교육기관 선정 일정이 뜨는지 직접 확인 필요.

## 팝업 이미지 확대 + 멘토 부하 합산 반영 + 기수 간격 재조정 (2026-08-06, 같은 날 후속 요청)
1. **홈 팝업 이미지 확대 + 텍스트 오버레이 — 완료**: "이미지를 더 크게 쓰고 글씨를 그 위에 잘 보이게 해달라"는 요청으로 `HomePromoModal`을 "사진 배너(위) + 흰 배경 텍스트(아래)" 2단 구성에서, **사진이 카드 전체 배경이 되고 그 위에 모든 텍스트・버튼이 얹히는 구성**으로 전면 개편(`HeroSlider`가 슬라이드 배경 이미지에 어두운 그라디언트를 합성해 흰 글씨를 얹는 방식과 동일한 기법 재사용: `linear-gradient(...), url(...)`를 `backgroundImage`로 합성, 상단은 연하게・하단(텍스트・버튼 영역)은 짙게 어두워지도록). 모달 최소 높이를 12rem 히어로 한정에서 30rem 전체로 키워 이미지 노출 영역 자체도 크게 확장. 이미지가 없을 때(placeholder)는 기존 그라디언트로 자동 대체.
2. **비회원 상담 멘토 지정 시 진행 건수 숫자 미반영 — 수정 완료**: "은채(진행중 0/4)처럼 담당 멘토를 지정해도 숫자가 안 바뀐다"는 버그 리포트. 원인은 `Admin.jsx`의 두 드롭다운(상담 매칭 모니터링의 멘토 순위, 비회원 상담의 담당 멘토 선택)이 전부 `ConsultationContext.getMentorLoad`만 사용해, 비회원 상담(`CallbackContext`) 배정 건수를 전혀 반영하지 않았기 때문. `Admin.jsx`에 `mentorLoad(mentorId)` 헬퍼를 추가해 **정규 상담(matched/in_progress) + 아직 연락하지 않은 비회원 상담 배정 건수**를 합산하도록 통일하고, 기존 `getMentorLoad` 호출 두 곳을 전부 이걸로 교체. `MentorDashboard.jsx`의 "현재 진행 N/M건" 표시도 동일한 계산식으로 맞춤(멘토 본인이 보는 부하와 관리자가 보는 부하가 일치하도록).
3. **지수・태호처럼 이미 배정된 멘토도 관리자 화면에서 항상 확인 가능하도록 — 완료**: 위 부하 계산 통일과 별개로, "이미 배정되어 있으면 관리자 목록에도 전부 떠야 한다"는 요청을 명확히 충족시키기 위해 **"멘토 현황" 섹션을 신규 추가**(상담 매칭 모니터링 위). 멘토 3명(지수・태호・은채) 전원을 항상 카드로 나열하고, 각자 전문분야 + 합산 진행 건수(정규 상담 + 비회원 상담)를 표시 — 개별 요청 건 안의 드롭다운을 하나하나 열어보지 않아도 누가 얼마나 배정되어 있는지 한 화면에서 바로 확인 가능.
4. **국비 과정 기수 간격 재조정 — 완료**: 방금 전에 추가한 1~3기 이력(위 항목)이 "기수 간 텀이 너무 짧고 2025년 시작이 너무 늦다"는 후속 피드백을 받아 재조정. 1기 모집 시작을 2025년 1월 초(연초)로 당기고, 기수별 모집 시작 간격이 전부 6개월 이상이 되도록 재배치(1기 모집 2025-01-06 → 2기 모집 2025-08-04, 약 7개월 / 2기 모집 2025-08-04 → 3기 모집 2026-04-01, 약 8개월). 수업 기간 자체(약 10주)는 `courseOverview`의 "10주 과정" 설명과 계속 어긋나지 않도록 그대로 유지하고, 기수 간 유휴 기간만 늘렸다. 3기 수료식(2026-08-07)・4기 이후 일정은 기존 값 그대로 유지.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 홈 팝업의 사진+텍스트 오버레이, `/admin`의 "멘토 현황" 섹션과 비회원 상담 멘토 지정 시 숫자가 바로 바뀌는지, `/schedule`에서 2025년 1월부터 1기 모집이 뜨는지 직접 확인 필요.

## 이미지 크기/비율 2건 조정 (2026-08-06, 같은 날 후속 요청)
1. **홈 팝업 이미지 비율 수정 — 완료**: "이미지를 너무 크게 썼다, 원본 전체가 잘 나오게 비율 조절해달라"는 피드백. 원인은 모달 컨테이너를 `min-height: 30rem`로 고정해뒀는데, 크롭해 둔 사진(2.33:1 와이드 배너)이 그 세로 비율에 맞춰 `background-size: cover`로 채워지면서 좌우가 심하게 잘려나갔던 것. **크롭을 아예 없애고** `reference/교육과정(팝업).png` 원본을 리사이즈만 해서 원본 비율(840×698, 약 1.2:1) 그대로 `public/images/promo-course.jpg`로 재생성, 모달 컨테이너도 `min-height: 30rem` 대신 `aspect-ratio: 840 / 698`로 이미지 원본 비율에 정확히 맞춤 — 이제 `cover`로 채워도 잘리는 부분 없이 원본 전체가 그대로 보임(결과적으로 모달 높이도 이전보다 작아짐).
2. **"실무에서 쓰는 AI 툴" 인트로 이미지 축소 + 레이아웃 원복 — 완료**: "이미지가 너무 크다, 400×300px로 줄이고 설명이 옆으로 오게 해달라"는 요청으로, 직전에 전체 폭 16:9 배너로 바꿨던 것을 다시 **이미지(좌) + 설명(우) 나란히 배치**로 되돌리고 이미지 크기를 정확히 `400×300px`로 고정(`CourseDetail.css`의 `.course-detail__ai-intro-media`). `reference/AI 에이전트.png`를 4:3 비율(1260×948 원본 영역)로 다시 크롭해 400×300으로 리사이즈 — "AI 에이전트" 라벨과 홀로그램 얼굴이 여전히 잘 보이는 영역으로 선택. 모바일(640px 이하)에서는 세로로 쌓이고 이미지가 폭 100%・4:3 비율로 자동 조정되도록 반응형 처리 재적용.
- 이미지 재작업은 기존과 동일하게 `npm install --no-save sharp` → 처리 → `npm uninstall sharp`로 제거(흔적 없음 확인). `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 홈 팝업(사진 전체 노출 확인)과 `/course`의 AI 툴 인트로(이미지+설명 나란히, 400×300) 직접 확인 필요.

## 홈 팝업 디자인 리뉴얼 + 멘토 프로필 사진 적용 (2026-08-06, 같은 날 후속 요청)
1. **홈 팝업 스타일 전면 리뉴얼 — 완료**: "너무 촌스럽다, 알아서 잘 고쳐봐"라는 요청으로 `HomePromoModal`을 자유롭게 재디자인. 기존엔 중앙 정렬 텍스트 + 흰색 사각 버튼 + 사이트 공용 배지(회색+초록) 조합이 다소 "예전 인터넷 팝업광고" 같은 인상이었음 — 다음과 같이 정리:
   - 텍스트를 중앙 정렬 → **좌측 정렬**로 변경(에디토리얼한 포스터 느낌).
   - 두 개의 공용 배지(국비지원 과정 / N기 모집중)를 없애고, **골드 톤 반투명 테두리 아이보우 태그** 하나("국비지원 과정 · N기 모집중")로 통합.
   - 버튼: "상세보기"는 플로팅 상담 버튼과 통일감 있게 **골드 그라디언트 필 버튼**으로, "나가기"는 흰색 사각 블록 대신 **반투명 고스트(테두리만 있는) 필 버튼**으로 변경 — 버튼 자체도 사각형 대신 완전한 pill 형태로.
   - 모달 모서리를 더 둥글게(20px→24px), 그림자를 더 깊고 은은하게, 팝업이 열릴 때 살짝 떠오르며 나타나는 **등장 애니메이션**(`@keyframes`, 0.4s) 추가 — 열릴 때마다 재생되어 정적이던 느낌을 줄임.
   - 배경 그라디언트 오버레이도 상단은 연하게 유지하되 하단(텍스트 영역)을 더 짙게 만들어 가독성 유지.
2. **멘토 프로필 사진 적용 — 완료**: 사용자가 `reference/멘토 프로필 사진.png`(3인 인물 사진, 이름 라벨 없음)를 추가. 남성은 1명뿐이라 태호로 바로 매칭, 여성 2명은 라벨이 없어 분위기로 판단 — 안경・블레이저 차림의 차분한 인상은 "시니어 UX 멘토" 지수로, 밝고 편안한 카페 사진의 인물은 주니어 멘토링을 강조하는 은채로 매칭(**추정 매칭이라 실제와 다르면 알려주면 바로 맞바꿀 수 있음**). 강사진 사진 때와 동일한 방식으로 얼굴 중심 정사각형(500×500)으로 크롭해 `public/images/mentors/{jisu,taeho,eunchae}.jpg`로 저장, `data/mentors.js`의 `photo: null`을 각 경로로 교체(이니셜 아바타 대신 실제 사진이 `/mentors` 페이지에 표시됨).
- 이미지 작업은 기존과 동일하게 `npm install --no-save sharp` → 크롭 → `npm uninstall sharp`로 제거(흔적 없음 확인). `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 홈 팝업 새 디자인과 `/mentors` 페이지의 실제 사진 3장, 특히 **지수・은채 매칭이 맞는지** 직접 확인 필요.

## 홈 팝업 구조 전면 재설계 (2026-08-06, 같은 날 후속 요청)
"여전히 촌스럽다, 요즘 트렌드에 맞게 완전 리디자인 해달라"는 요청. 색상・버튼 모양만 다듬은 이전 두 차례 수정으로는 근본적으로 해결이 안 된다고 판단해 **구조 자체를 변경**:
- 문제로 짚은 지점: "사진 전체를 배경으로 깔고 어두운 스크림을 씌운 뒤 그 위에 흰 글씨를 얹는" 방식 자체가, 색이나 버튼 모양을 아무리 바꿔도 국내 학원・업체 홍보 전단지에서 흔히 쓰는 오래된 템플릿 문법이라 "촌스럽다"는 인상의 근본 원인이라고 판단.
- **사진과 텍스트를 완전히 분리한 카드형 구조로 전환**(요즘 SaaS/제품 안내 모달에 가까운 문법): 사진은 카드 안쪽에 여백을 두고 담아(엣지투엣지 아님) 어두운 스크림 없이 원본 그대로 선명하게 노출, 사진 위에는 작은 흰색 알약 태그("국비지원 · 4기 모집중") 하나만 하단 구석에 살짝 겹치도록 배치. 텍스트는 사진 아래 별도 흰 배경 영역에서 진한 텍스트 색으로 표시(가독성 문제 원천 차단, 더 이상 그라디언트 스크림 튜닝 불필요).
- 골드 그라디언트・고스트 버튼 등 이전에 시도한 화려한 장식 요소는 걷어내고, 버튼은 사이트 기본 초록 그라디언트 1개(전체 폭, `border-radius: 12px`)만 남기고, "나가기"・"다시보지 않기"는 버튼이 아니라 하단의 작은 텍스트 링크 두 개로 축소(가운데 점으로 구분) — 시각적 위계를 "주요 행동 1개 + 부가 링크"로 명확히 단순화.
- 모달 자체 폭도 26rem→24rem으로 줄이고 카드 코너・그림자를 정돈, 열릴 때 살짝 떠오르는 등장 애니메이션은 유지.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 홈 진입 시 팝업 확인 필요 — 여전히 마음에 안 들면 어느 요소가 문제인지 구체적으로 짚어주면 좋음(예: 사진 위치, 색상, 타이포, 버튼 배치 등).

## 멘토 사진 재크롭(얼굴 확대) + 실적 배너 배경 패턴 (2026-08-06, 같은 날 후속 요청)
1. **멘토 프로필 사진 얼굴 확대 — 완료**: "얼굴이 너무 작게 나온다"는 피드백으로 기존 800×800 크롭을 재조정. 1차로 480×480까지 줄였더니 이번엔 턱이 잘려나가는 문제가 생겨(중심점은 맞는데 프레임이 너무 타이트), 최종적으로 **560×560**(얼굴 중심을 아래로 살짝 옮겨 턱까지 여유 있게 포함)으로 재크롭 → 500×500 리사이즈. 결과적으로 기존 800×800 대비 얼굴이 프레임에서 차지하는 비중이 크게 늘어남. 파일 경로는 그대로라 코드 변경 없이 이미지만 교체됨.
2. **실적 배너(홈/학원소개) 배경 패턴 추가 — 완료**: "단조롭다"는 피드백으로 `AchievementBanner.css`에 두 가지 텍스처를 추가. 기존엔 우측 상단 골드 광원 블롭(`::before`) 하나만 있던 단색 그라디언트 배경에, `::after`로 ①좌측 하단에 은은한 두 번째 골드 광원(비대칭 배치로 입체감 추가) ②전체 면에 아주 옅은(9% 흰색) 점 격자 패턴(20px 간격)을 추가. 텍스트・숫자가 패턴 위에 항상 선명하게 보이도록 `.achievement-banner__list`에 `z-index: 1`을 명시해 패턴보다 위에 오도록 쌓기 순서 고정.
- 이미지 재작업은 기존과 동일하게 `npm install --no-save sharp` → 크롭 → `npm uninstall sharp`로 제거(흔적 없음 확인). `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/mentors`의 확대된 얼굴 사진과, 홈/학원소개의 실적 배너 배경 패턴 직접 확인 필요.

## 팝업 세부조정 3건 + 태호 사진 재중앙 + 상담신청 로그인 필수화 + 달력 트레일링 패딩 (2026-08-06, 같은 날 후속 요청)
1. **팝업 "나가기" 버튼 삭제 — 완료**: `HomePromoModal.jsx`에서 "나가기" 버튼 제거, 닫기는 우상단 ✕ 버튼 하나로 통일.
2. **"다시보지 않기" → "오늘 하루 다시 보지 않기"로 변경 — 완료**: 기존엔 `localStorage`에 `true`(영구)를 저장했던 것을, **오늘 날짜 문자열**(`YYYY-MM-DD`)을 저장하도록 변경(`STORAGE_KEY`도 `home-promo-dismissed-until`로 변경). 팝업을 열 때 저장된 날짜가 오늘과 같으면 스킵, 다르면(자정이 지나 날짜가 바뀌면) 다시 노출 — 매일 아침 다시 뜨는 방식으로 전환.
3. **지원 마감 카운트다운 시계 아이콘 시소 모션 — 완료**: 팝업 안 `DeadlineCountdown`의 시계 아이콘(`.deadline-countdown__icon`)에만 좌우로 까닥이는 회전 애니메이션(`@keyframes`, ±12deg, 1.4s 무한반복, `transform-origin: 50% 85%`로 바닥 쪽을 축으로 시소처럼 움직이게)을 추가. 사이트 다른 곳(홈 지원마감 배지, 과정상세 페이지 등)의 `DeadlineCountdown`에는 영향 없도록 `.home-promo-modal__body .deadline-countdown__icon` 선택자로 팝업 안에만 한정.
4. **멘토 태호 사진 재중앙 — 완료**: 얼굴이 왼쪽으로 치우쳐 있던 걸(오른쪽에 화이트보드 여백이 과하게 남음) 크롭 박스를 좌측으로 이동해 재중앙 정렬(1차 시도는 반대로 오른쪽 치우침이 되어 재조정, 최종적으로 균형 맞춤). 지수・은채는 육안 확인 결과 이미 중심이 맞아 보여 그대로 둠.
5. **멘토 상담 신청 로그인 필수화 — 완료**: `/consultation`을 `RequireAuth`로 감싸 비로그인 접근 시 `/login`으로 리다이렉트되도록 변경. 기존에 비로그인 방문자를 위해 있던 `GuestConsultationRequest`(이름/연락처/문의 내용만으로 접수하는 비회원 폼) 컴포넌트를 `ConsultationRequest.jsx`에서 완전히 제거. **참고**: 2026-08-04에 만든 "비회원 상담 신청(콜백)" 시스템의 유일한 접수 경로가 이 폼이었어서, 이번 변경으로 새 비회원 상담 신청을 만들 방법이 없어짐 — 다만 `CallbackContext`・관리자 대시보드의 "상담 신청 관리 (비회원)" 섹션・멘토 대시보드의 "담당 비회원 상담 신청" 섹션은 기존 시드 데이터 조회/처리용으로 코드는 그대로 남겨둠(요청 범위가 "멘토 상담 신청" 페이지 한정이라 그 하위 관리 기능까지 스스로 판단해 지우지는 않음 — 필요 없다면 알려주면 함께 정리 가능).
6. **`/schedule` 달력 틀 정렬 — 완료**: 달력 마지막 주가 실제 요일 수만큼만(예: 2칸) 렌더링되고 나머지 5칸은 그냥 빈 공간이라, 매달 마지막 줄이 짧게 끊겨 격자 틀이 고르지 않아 보이던 문제. `Schedule.jsx`의 `cells` 계산에 트레일링 패딩을 추가해(`(7 - length % 7) % 7`만큼 `null` 채움) 항상 7의 배수로 완결된 행이 렌더링되도록 수정 — 다만 날짜 배치 자체(요일 계산)는 처음부터 정확했음(실제 달력과 대조 검증 완료).
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 홈 팝업(나가기 삭제・오늘 하루 문구・시계 시소 모션), `/mentors`(태호 재중앙), `/consultation`(비로그인 접근 시 로그인 페이지로 이동하는지), `/schedule`(달력 마지막 줄 포함 격자 정렬) 직접 확인 필요.

## 여정 그래프 배경 패턴 + 문의 게시판 비회원 허용 (2026-08-06, 같은 날 후속 요청)
1. **여정 그래프("지원부터 취업까지") 배경 패턴 — 완료**: "배경이 너무 어둡고 심심하다"는 피드백으로 `JourneyGraph.css`에 실적 배너・학원 일정과 동일한 질감 언어를 적용. 기존엔 장식 없는 단색 대각선 그라디언트뿐이었던 배경에, 좌상단 은은한 골드 광원(`::before`) + 전체에 아주 옅은(7% 흰색) 점 격자 패턴(`::after`, 24px 간격)을 추가. 곡선 그래프・스텝 텍스트가 패턴 위에 항상 선명히 보이도록 `.journey-graph__inner`에 `position: relative; z-index: 1`을 명시.
2. **문의 게시판 비회원 허용 — 완료**: "문의하기는 비회원도 가능해야 한다"는 요청으로, 로그인해야만 보이던 문의 등록 폼을 **로그인 여부와 무관하게 항상 노출**하도록 변경(`InquiryBoard.jsx`). 비로그인 상태에서는 폼 맨 위에 "이름" 입력칸이 추가로 나타나 필수로 받고, 로그인 상태에서는 기존과 동일(이름 칸 없음, 계정으로 식별). `InquiryContext.createInquiry`가 `currentUser` 존재 여부와 무관하게 항상 접수하도록 가드를 제거하고, 비회원 문의는 `userId: null` + 입력받은 `name`으로 구분해 저장(로그인 사용자는 기존처럼 `userId`만 사용).
   - **비회원은 로그인 계정이 없어 나중에 자신의 문의를 다시 조회할 방법이 없음** — 기존 "내 문의 내역" 섹션은 로그인 사용자에게만 그대로 노출(변경 없음), 비회원은 제출 직후 확인 메시지("담당자가 연락처/이메일로 안내드릴 예정")만 보여주는 것으로 설계(비회원 상담 신청 콜백 시스템과 동일한 패턴).
   - 관리자 대시보드 "문의 관리" 목록이 지금까지 `{inquiry.userId}`를 그대로 표시해 비회원 문의는 빈 값/`null`로 보였을 문제도 함께 수정 — `inquiryAuthorName()` 헬퍼 추가(로그인 사용자는 `mockUsers`에서 이름 조회, 비회원은 입력한 이름 + "(비회원)" 태그).
   - `data/inquiries.js`에 비회원 문의 시드 예시 1건 추가(방문자A, 답변대기 상태)해 기능이 바로 눈에 보이도록 함.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 홈 하단 여정 그래프 배경, `/inquiry`를 비로그인 상태로 열어 이름 칸이 뜨는지·제출이 되는지, `/admin` 문의 관리에 "방문자A (비회원)"으로 표시되는지 직접 확인 필요.

## /schedule 달력 격자 폭 불일치 수정 (2026-08-06, 같은 날 후속 요청)
지난번 트레일링 패딩 수정 이후에도 "요일 헤더(일월화수목금토)보다 날짜 칸이 더 바깥으로 나가 있다"는 스크린샷 피드백. 요일 헤더(`.schedule__weekdays`)와 날짜 그리드(`.schedule__grid`)는 동일한 `grid-template-columns: repeat(7, 1fr)`를 쓰고 있어 논리적으로는 정확히 맞아야 하는데, 실제로는 어긋나 있었음 — 원인은 날짜 로직이 아니라 **CSS 그리드 트랙 폭 계산 버그**였음.
- `.schedule__pill`에 `white-space: nowrap`이 걸려 있어("UI/UX 디자인 실무 과정 4기 발대식(OT)" 같은 긴 제목이 줄바꿈 없이 한 줄을 고집), 그리드 아이템(`.schedule__cell`)의 기본 최소 폭(`min-width: auto`)이 이 줄바꿈 없는 텍스트의 전체 너비만큼 잡히면서, 긴 일정이 있는 날짜의 열만 `1fr`보다 넓게 벌어짐 → 7개 열이 더 이상 균등하지 않게 되고, 전체 격자 폭이 요일 헤더 grid보다 넓어져 오른쪽으로 밀려나 보였던 것.
- `.schedule__cell`・`.schedule__pills`・`.schedule__pill` 세 곳 모두에 `min-width: 0`을 추가해, 내용(긴 텍스트)이 그리드 트랙 크기 계산에 영향을 주지 않고 항상 `1fr` 균등폭을 지키도록 수정 — 대신 원래 있던 `overflow: hidden` + `text-overflow: ellipsis`가 정상적으로 작동해 긴 제목은 "..."으로 말줄임 처리됨.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/schedule`의 요일 헤더와 날짜 칸 열이 정확히 세로로 맞는지, 긴 일정 제목이 말줄임(...)으로 잘려 보이는지 직접 확인 필요.

## 3기 모집 시작 2월로 이동 (2026-08-06, 같은 날 후속 요청)
`data/schedule.js`의 `sch-hist-3-recruit`(국비 과정 3기 모집)을 `2026-04-01~04-28` → **`2026-02-02~03-01`**로 변경(같은 약 4주 기간 유지, 시작만 당김).
- **해석 메모**: "3기 시작"을 3기 모집(지원 접수) 시작일로 해석해 옮김 — 3기 수업 시작(2026-05-27)과 3기 수료식(2026-08-07, `notices.js`의 "3기 수료식 8월 초" 문구와도 연결된 고정값)은 이미 확정된 값이라 건드리지 않음. 만약 "수업이 2월에 시작"하는 쪽을 의도하신 거라면 수료식 날짜도 함께 옮겨야 해서(현재 10주 과정 기준 2월 시작 시 수료는 4월경), 확인 후 다시 조정 가능.
- 2기 모집 시작(2025-08-04) 기준 6개월 후로 당겨져 있어, 기존에 유지해온 "기수 간 모집 시작 6개월 이상 간격" 규칙은 그대로 충족(정확히 6개월).
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/schedule`을 2026년 2월로 이동해 3기 모집 일정이 뜨는지 확인 필요.

## 3기 모집~수업 사이 공백 해소 (2026-08-06, 같은 날 후속 요청)
"3기 모집만 당겨져서 모집 마감과 개강 사이 공백이 3개월이나 생기고, 수료는 그대로 8월이라 이상하다"는 지적. 3가지 처리 방향(①수업・수료식도 함께 앞당기기 ②모집만 살짝 되돌려 공백만 줄이기 ③현재 상태 유지, 공백은 서류심사 기간으로 안내)을 제시했고, 사용자가 ①번을 선택.
- `data/schedule.js`: `sch-hist-3-course`(3기 수업)를 `2026-05-27~08-05` → **`2026-03-09~05-22`**로 변경(모집 마감 2026-03-01로부터 약 1주 후 개강 — 1・2기와 동일한 간격 패턴), `sch-2`(3기 수료식)도 `2026-08-07` → **`2026-05-22`**로 함께 이동(수업 마지막 날과 동일 날짜로 유지).
- `data/notices.js`의 "3기 수료식 및 취업 후기 공유회 안내" 공지도 함께 수정 — 날짜가 8월(미래 예고)에서 5월(이미 지난 과거)로 바뀌었으므로, 제목을 "…안내"→"…성료"로, 본문을 미래형("진행할 예정") → 과거형("성황리에 마쳤습니다")으로, `createdAt`도 `2026-07-22`→`2026-05-25`로 조정(더 이상 "재학생(4기)도 참관 가능" 문구는 유지하지 않음 — 이 시점엔 4기가 아직 모집조차 시작 전이라 "재학생"이라는 표현이 성립하지 않았음).
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/schedule`(3월 수업 시작・5월 수료식), `/notices`(공지 문구・날짜) 직접 확인 필요.

## /schedule 다른 과정 노출 확인 + 과정별 색상 구분 (2026-08-06, 같은 날 후속 요청)
- **"다른 교육과정도 일정표에 넣어달라" — 이미 반영되어 있음, 추가 작업 없음**: 확인해보니 다른 과정 3개(UX 리서치 심화・프로덕트 디자인 부트캠프・디자인 시스템 마스터)는 이미 `data/schedule.js`의 `otherCourses.flatMap(...)`으로 포함되어 있었음. 다만 각 과정의 1기 수업이 2026년 6월(디자인 시스템)・7월(UX 리서치・프로덕트 디자인)부터 시작해서, 방금 보고 있던 2~3월 달력에는 국비 과정 일정만 보였던 것 — 사용자에게 6월 이후로 이동하면 확인 가능하다고 안내하고 그대로 유지하기로 확인받음(다른 과정들의 시작 시점을 앞당길 필요는 없음).
- **과정별 색상 구분 — 완료**: "이미 들어가 있는 건 알겠는데, 과정명이 길어서 pill만 봐서는 뭐가 뭔지 구분이 안 된다"는 후속 피드백으로, 각 pill 앞에 **과정별 고유 색점**을 추가. `data/schedule.js`의 모든 일정 항목에 `courseId` 필드를 추가(국비 과정 이력・4기・행사는 전부 국비 과정으로, `otherCourses`는 각자의 `c.id`로 태깅). `Schedule.jsx`에 4개 과정 색상 매핑(`COURSE_COLORS`: 국비=브랜드 그린, UX 리서치=블루, 프로덕트 디자인=퍼플, 디자인 시스템=테라코타)을 추가하고, 달력 칸의 pill과 하단 "이번 달 일정" 목록 양쪽에 `courseId` 기준 색점을 표시. 기존 모집/수업/행사 타입 범례(색 배경)는 그대로 유지하고, 그 아래 별도로 "과정별" 색점 범례를 새로 추가해 두 축(타입・과정)을 동시에 구분할 수 있도록 함.
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/schedule`의 과정별 범례와, 달력 pill・하단 목록에 색점이 올바른 과정 색으로 찍히는지 직접 확인 필요.

## 국비 과정 10주 → 20주(주 4일, 월~목) 전환 (2026-08-06, 같은 날 후속 요청)
"교육과정이 10주로 되어있는데 20주(약 5개월)로 바꾸고, 관련 페이지도 함께 수정해달라. 토요일도 수업하는 것처럼 보이는데 월~목 주 4일로 하자"는 요청. 두 가지를 함께 처리:

1. **커리큘럼/모집요강 콘텐츠 — 완료**: `data/courseDetail.js`의 `courseOverview`("10주 과정 (약 400시간)" → "20주 과정 (약 640시간)", "평일 09:00~18:00 (주 5일)" → "09:00~18:00 (월~목, 주 4일)")와 `curriculum` 4단계의 주차 구간을 기존 대비 정확히 2배로 재분배(1~2주차→1~4주차, 3~5주차→5~10주차, 6~7주차→11~14주차, 8~10주차→15~20주차, 합 20주 유지). 이 주차 구간을 참조하던 다른 파일들도 함께 수정: `data/instructors.js`(강사 4명의 담당 커리큘럼 주차 표기), `data/courseDetail.js`의 FAQ 답변("1~2주차"→"1~4주차"), `data/faqBank.js`(동일 FAQ), `data/reviews.js`(수강생 후기 답변의 "1~2주차" 언급).
2. **/schedule 국비 과정 수업 기간을 20주(140일) 기준으로 재계산 — 완료**: `data/schedule.js`의 1~4기 수업 기간을 전부 140일 기준으로 다시 계산(1기: 02-10~06-29, 2기: 09-08~2026-01-25, 3기: 03-11~07-28, 4기: 08-24~2027-01-10). 3기 수료식(`sch-2`)도 3기 수업 종료일과 같은 2026-07-28로 이동, `notices.js`의 수료식 공지도 날짜・문구를 다시 맞춤(createdAt을 데모 기준일 2026-08-06 직전인 08-01로 조정).
3. **달력에서 금・토・일에 "수업" pill이 뜨던 문제 수정 — 완료**: 기존엔 수업 기간(startDate~endDate) 안의 모든 날짜에 무조건 "수업" pill을 표시해 토요일에도 수업하는 것처럼 보였음. `Schedule.jsx`에 `isClassDay(year, month, day)` 헬퍼를 추가해 요일이 월~목(getDay() 1~4)일 때만 `type: 'course'` pill을 표시하도록 필터링(모집・행사 타입은 요일 제한 없이 그대로 표시). 상단 안내 문구에도 "수업은 월~목요일(주 4일)만 진행" 문구 추가.
4. **(발견) 정원 대기 자동 승격 관련 FAQ 2건 최신화 — 완료**: 작업 중 `data/faqs.js`・`data/faqBank.js`의 "정원 초과 시... 대기 순번에 따라 자동으로 승격됩니다" 문구가, 이번 세션 초반에 이미 수동 전환으로 바뀐 실제 동작과 어긋나 있는 걸 발견해 함께 수정("확정 인원이 취소되어 빈 자리가 생기면 관리자가 대기 순번을 확인해 합격으로 전환합니다").
- **참고**: 다른 과정 3개(UX 리서치・프로덕트 디자인・디자인 시스템)의 수업 기간은 이번 20주 전환 대상에 아니며 건드리지 않음(국비 과정만 상세 커리큘럼/주차 페이지가 있어 요청 범위로 판단).
- `npm run lint`(기존 무관 warning 6개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/course`(20주・월~목 문구, 재분배된 커리큘럼), `/instructors`, `/faq`, `/reviews`, `/schedule`(국비 과정 수업 기간・금토일에 수업 pill 안 뜨는지), `/notices` 직접 확인 필요.

---

## 오늘(2026-08-06) 전체 작업 요약
이 파일 위쪽에 시간순으로 상세 기록되어 있으며, 아래는 빠르게 훑어보기 위한 목차 성격의 요약이다.

1. 합격 취소 시 대기자 자동 승격 로직 → 관리자 수동 전환
2. "국비교육 하나만 있는 컨셉" 재구성 논의 → 과정 카탈로그(국비 과정 모집중 + 다른 과정 3개 소개) 도입, 지원자 관리 국비 과정 한정, 지원자/상담 이름・지원일 랜덤화
3. 모집 기간 명시(`applicationStart`) + 다른 과정 "모집예정" → "진행중"(진행 기간・다음 모집 예정 표기)으로 리프레이밍
4. 멘토 소개 페이지(`/mentors`) 신규, 상담 매칭 모니터링 학생 이름 랜덤화, 상담 신청 관리 요청 내용 기반 스마트 매칭(분야 일치 멘토 우선 정렬)
5. 세션 중 누락 항목 점검(누락 없음 확인) + 강사 프로필 사진 얼굴 중심 재크롭 + 헤더 드롭다운에 멘토 소개를 강사진 소개 옆으로 이동 + 학원 일정(`/schedule`) 페이지 신규
6. 다른 과정 진행 일자・다음 모집 예정 기간 노출, 지원자 관리 전원 일괄 상태변경 버튼, 비회원 상담 신청 관리자 멘토 지정, "누적 수료생 1200+" 등 비현실적 실적 수치를 60+로 정합화
7. 홈 팝업 광고(`HomePromoModal`) 신규 구현(나가기/상세보기/다시보지 않기)
8. AI・에이전트 키워드 반영 + "실무에서 쓰는 AI 툴" 섹션(Claude・ChatGPT・Gemini・Figma AI) 신규
9. 헤더 드롭다운 메뉴 리디자인, 홈 팝업・AI 인트로에 실제 사진 적용(reference 폴더 이미지 크롭), "파트너사 (예시)" 문구 삭제
10. 비회원 상담 신청 시 멘토 지정이 실제로 멘토 대시보드에 반영되도록 수정, AI 인트로 이미지 확대, 팝업 재노출 안내
11. /schedule에 2025년 이력(1~3기 모집・수업・수료식, 우수 국비교육기관 선정 등) 신규 추가해 연혁과의 공백 해소
12. 팝업 이미지 비율 수정(크롭 없이 원본 그대로), AI 인트로 이미지를 400×300 축소 + 좌우 배치로 원복
13. 팝업 스타일을 카드형(사진+텍스트 분리) 구조로 전면 재설계, 멘토 프로필 사진 3인 적용
14. 멘토 얼굴 확대 재크롭, 실적 배너 배경에 골드 광원+점 패턴 추가
15. 팝업에서 "나가기" 삭제, "다시보지 않기"를 "오늘 하루" 방식으로 변경, 지원마감 시계 아이콘 시소 애니메이션, 멘토 태호 사진 재중앙, 멘토 상담 신청 로그인 필수화(+비회원 콜백 접수 경로 제거), `/schedule` 달력 트레일링 패딩
16. 여정 그래프 배경에 골드 광원+점 패턴 추가, 문의 게시판 비회원 작성 허용(관리자 목록에 "이름 (비회원)"으로 표시)
17. `/schedule` 요일 헤더와 날짜 칸이 어긋나던 CSS 그리드 버그 수정(긴 pill 텍스트가 그리드 트랙 최소폭을 밀어내던 문제)
18. 3기 모집 시작을 2월로 조정 → 모집~개강 사이 공백이 길어진 문제를 수업・수료식도 함께 앞당겨 해소(관련 공지 문구도 동기화)
19. `/schedule`에 다른 과정 3개가 이미 반영되어 있음을 확인, 과정별 색점 범례・pill 색 구분 추가
20. 국비 과정을 10주 → 20주(주 4일, 월~목)로 전환 + 커리큘럼・FAQ・후기 등 관련 텍스트 동기화 + `/schedule` 수업 기간 재계산 + 금토일 수업 pill 노출 버그 수정 + (발견) 대기자 자동 승격 관련 FAQ 문구 최신화
21. 20주(640시간) 계산이 맞는지 재확인 요청 — 이미 정확히 반영되어 있음을 확인만 하고 코드 변경 없음
22. `/schedule` 행사가 `/notices`에 전부 반영됐는지 감사 — **입학 설명회(8/12) 공지 누락 발견해 신규 추가**, (발견) notice-1의 "모집 마감 2주 앞" 문구가 실제 날짜 차이(3주)와 안 맞던 것도 함께 수정. 오래된 과거 행사(1・2기 수료식, 우수 국비교육기관 선정)와 내부 행사(4기 발대식)는 공지 없이 유지하기로 판단(과거 일정은 실제 사이트에서도 공지가 계속 남아있지 않는 게 자연스럽고, 발대식은 이미 확정된 재학생 대상 내부 행사라 공개 공지 필요성이 낮다고 봄).

모든 항목은 `npm run lint` + `npm run build` 통과 확인 완료. 이 환경엔 브라우저 자동화 도구가 없어 실제 화면 조작 검증은 못 했으므로, 위 각 항목에 적어둔 "직접 확인 필요" 페이지들을 실제로 열어보는 걸 권장.

## 학원 일정 상세보기 + 관리자 CRUD + 안내문구 간소화 (2026-08-06, 같은 날 후속 요청)
1. **날짜 칸 "+N" 클릭해서 전체 일정 보기 — 완료**: 하루에 일정이 3개 이상이면 2개만 보이고 "+N"으로 접혀 나머지를 확인할 방법이 없었음. `Schedule.jsx`의 날짜 칸을 `<div>`에서 클릭 가능한 `<button>`으로 바꾸고(일정이 있는 날만 클릭 가능, 빈 날은 `disabled`), 클릭하면 그 날짜의 전체 일정을 모달(`<dialog>`, 기존 카카오/전화상담 모달과 동일한 패턴)로 보여주도록 함 — 구분(모집/수업/행사) 배지 + 제목 + 과정명 + 기간을 목록으로 표시.
2. **관리자의 학원 일정 추가・수정・삭제 — 완료**: 지금까지 `scheduleEvents`가 `data/schedule.js`에서 내보내는 정적 배열이라 런타임에 바꿀 방법이 전혀 없었음. 다른 기능들과 동일한 패턴으로 `context/ScheduleContext.jsx`를 신규 추가(정적 배열을 초기값으로 시작하는 React state + `addEvent`/`updateEvent`/`deleteEvent`/`getAllEvents`), `main.jsx`에 Provider 등록. `Schedule.jsx`는 정적 import 대신 이 Context에서 일정을 읽어오도록 변경. `Admin.jsx`에 "학원 일정 관리" 섹션 신규 추가 — 제목・구분(모집/수업/행사)・과정・시작일・종료일 입력 폼으로 새 일정을 추가하고, 목록의 "수정" 버튼을 누르면 같은 폼이 해당 일정 데이터로 채워져 수정 모드로 전환(취소 가능), "삭제" 버튼은 확인창을 거쳐 삭제.
3. **`/schedule` 상단 안내 문구 간소화 — 완료**: "수업은 월~목요일(주 4일)만 진행되어, 수업 기간 중에도 금・토・일에는 수업 표시가 뜨지 않습니다"라는 문장이 "손님이 굳이 볼 내용은 아닌 것 같다"는 피드백으로 삭제 — 이건 UI 동작을 설명하는 개발 메모에 가까웠음. "모집 기간, 과정별 수업 일정, 행사를 한 화면에서 확인할 수 있습니다." 한 문장만 남김(월~목 주 4일 정보 자체는 `/course` 모집요강에 이미 있으므로 정보 손실 없음).
- `npm run lint`(기존 무관 warning 6개만, `ScheduleContext.jsx`도 다른 Context 파일들과 동일한 유형의 무관 warning 1개 추가) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/schedule`의 날짜 칸 클릭 시 모달이 뜨는지, `/admin`의 "학원 일정 관리"에서 추가・수정・삭제가 실제로 `/schedule`에 반영되는지 직접 확인 필요.

## 회원가입 항목 추가 + DB 연결 여부 질의응답 + 학원 일정 수정 UX 개선 (2026-08-07)

1. **회원가입 항목 추가(생년월일・성별・휴대폰번호・집주소) — 완료**: `Login.jsx`의 회원가입 모드에 4개 필드를 신규 추가. 생년월일은 `type="date"`, 성별은 라디오(여성/남성), 휴대폰번호는 입력하는 동안 자동으로 `010-0000-0000` 형식으로 하이픈이 채워지도록 처리(`formatPhone`). 집주소는 직접 타이핑이 아니라 **다음(카카오) 우편번호 서비스**로 검색해서 채우는 방식으로 구현(`loadDaumPostcodeScript` — 기존 `KakaoMap.jsx`의 외부 스크립트 동적 로드 패턴과 동일하되, 다음 우편번호 서비스는 카카오맵과 달리 별도 API 키 발급 없이 스크립트만 로드하면 바로 쓸 수 있음) — "주소 검색" 버튼 클릭 시 다음 우편번호 팝업이 뜨고, 선택하면 우편번호・주소가 자동 입력되며 상세주소(동/호수 등)만 직접 입력. 주소를 검색하지 않고 제출하면 별도 에러 메시지로 막음(우편번호/주소 input은 `readOnly`라 HTML `required`만으로는 검증이 안 되기 때문).
   - `AuthContext.jsx`의 `register(email, password, name, profile)`에 네 번째 인자로 `{ birthDate, gender, phone, address: { zonecode, address, detail } }`를 받아 신규 유저 객체에 그대로 병합하도록 확장(기존 mock 계정 3개는 이 필드 없이도 동작에 지장 없음 — 마이페이지 등 다른 화면에서 아직 이 필드들을 표시하지는 않음, 필요해지면 추가 가능).
2. **회원가입 구현에 DB 연결이 필요한지 — 질의응답(코드 변경 아님)**: 현재 이 프로젝트는 회원가입/로그인을 포함한 모든 데이터가 `AuthContext.jsx`의 `mockUsers` 배열(메모리)+`localStorage`(로그인 유지용 id 하나만) 조합으로 동작하는 프론트엔드 전용 데모이고, 실제 서버/DB는 없다고 안내함. 지금 방식대로면 DB 없이도 동작은 하지만 **새로고침하면 방금 가입한 회원 정보가 사라지는** 한계가 있고, 실제 서비스로 운영하려면(다른 기기・재방문 시에도 유지) 회원 정보를 저장할 백엔드+DB 연결이 필요하다고 설명함. 지금 당장 DB를 붙일지는 사용자 판단에 맡기고 별도 작업은 하지 않음.
3. **관리자 학원 일정 "수정" 클릭 시 어디를 고치는지 안 보이던 문제 — 완료**: `Admin.jsx`의 "학원 일정 관리" 표에서 "수정"을 눌러도 위쪽 폼 내용만 바뀔 뿐 폼과 표 양쪽 다 지금 뭘 수정 중인지 시각적 신호가 전혀 없었음. `handleScheduleEditClick`을 새로 만들어 "수정" 클릭 시 ① 폼 영역(`scheduleFormRef`)으로 부드럽게 스크롤(`scrollIntoView`) ② 폼 상단에 "✏️ OOO 일정을 수정하고 있습니다" 안내 배지 표시 + 폼 전체에 초록 테두리 강조(`admin__schedule-form-wrap--editing`) ③ 표에서 수정 중인 행에 배경색+좌측 포인트 바 강조(`admin__schedule-row--editing`), 버튼 라벨도 "수정"→"수정 중"으로 변경 ④ `ScheduleEventForm` 내부에서 수정 모드 진입 시 제목 입력칸에 자동 포커스가 가도록 처리. 새 일정 추가(수정 아닌 상태)에는 이 강조가 전혀 나타나지 않아 기존 동작과 구분됨.
- `npm run lint`(기존 무관 warning 7개만, `AuthContext.jsx` 라인 이동으로 번호만 바뀜) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/login`(회원가입 모드로 전환 후 4개 필드・주소 검색 팝업 동작), `/admin`의 "학원 일정 관리"에서 "수정" 클릭 시 스크롤・강조 표시가 바로 눈에 띄는지 직접 확인 필요.

## 학원 연혁 페이지 + 교육시설 소개 페이지 신규 + 마이페이지 "내 멘토" 섹션 (2026-08-07, 같은 날 후속 요청)

1. **학원 연혁 페이지(`/history`) — 완료**: 기존 `About.jsx`(학원소개)에 3줄로만 있던 연혁을, `aboutContent.js`의 `history` 배열을 8개 항목(설립・1~3기 모집/수업/수료・우수 국비교육기관 선정・4기 모집)으로 확장한 뒤 전용 페이지로 분리. 날짜는 `data/schedule.js`의 `nationalCourseHistory`(1~3기 실제 모집/수업/수료 일정)와 어긋나지 않게 맞춰(이 배열이 유일한 출처), 세로 타임라인(점+선) 형태로 표시. `About.jsx`는 이 중 `highlight: true`로 표시된 3개 항목(설립・우수기관 선정・4기 모집중)만 요약으로 보여주고 "전체 연혁 보기 →"로 `/history`를 연결 — 데이터를 이중 관리하지 않도록 단일 배열에서 파생시킴. 헤더 "학원안내" 드롭다운에 "학원소개" 바로 다음 위치로 추가.
2. **교육시설 소개 페이지(`/facilities`) — 완료**: `data/facilities.js` 신규(이론 강의실・실습존・협업 스튜디오・멘토링룸・라운지 5개 항목, 각 `image: null` — 실제 사진이 준비되면 경로만 채우면 다른 placeholder 이미지들과 동일하게 자동 교체). 사진이 없는 지금은 카드 상단에 초록 그라디언트 배경 + 카메라 아이콘 + "사진 준비 중" 문구로 표시. 헤더 "학원안내" 드롭다운에 "학원 연혁" 다음 위치로 추가.
3. **마이페이지 "내 멘토" 섹션(일반회원 전용) — 완료**: "일반회원이 마이페이지에서 연결된 멘토를 보고 바로 상담・문의할 수 있게 해달라"는 요청. 이 프로젝트엔 "내 담당 멘토"라는 별도 저장값이 없고 상담 신청(`ConsultationContext`)마다 분야 일치・부하 최소 기준으로 멘토가 자동 배정되는 구조라서, `MyPage.jsx`에 `MyMentorsSection`을 신규 추가해 **`getMyConsultations()`에서 멘토가 배정된 신청들을 멘토별로 묶어 가장 최근 것만 남기는 방식**으로 "내 멘토" 목록을 도출(별도 데이터 저장 불필요). `currentUser.role === 'user'`일 때만 프로필 카드 바로 아래(통계 카드보다 위)에 노출 — 아직 배정된 멘토가 없으면 "상담을 신청하면 자동 배정됩니다" 안내와 신청 링크만 표시. 각 멘토 카드에 사진/이니셜 아바타・전문분야 태그・최근 상담분야 및 상태 배지와 함께 **"상담 신청"(`/consultation`)・"문의하기"(`/inquiry`) 버튼**을 바로 배치(문의 게시판은 멘토별이 아니라 관리자가 통합 답변하는 구조라 특정 멘토 지정 없이 바로가기만 제공).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/history`(타임라인), `/facilities`(placeholder 카드 5개), `/about`(요약 3줄+링크), `/mypage`를 지원자 계정(minji@example.com)으로 로그인해 "내 멘토" 섹션과 버튼 동작 직접 확인 필요.

## 멘토 배정된 합격자 테스트 계정 추가 + 교육시설 실사진 적용 (2026-08-07, 같은 날 후속 요청)

1. **멘토가 배정된 합격자 테스트 계정 신규 — 완료**: 민지(`user-minji`)는 지원/상담 이력이 전혀 없는 백지 계정이라, 어제 만든 마이페이지 "내 멘토" 섹션이나 합격 상태 화면을 바로 보여줄 방법이 없었음. `data/users.js`에 `유나`(`yuna@example.com`, 비밀번호 1234) 계정을 신규 추가하고, **국비 과정 합격(confirmed) 좌석 1석**(`data/applications.js` — 기존 무작위 시드 19석 중 1석을 이 계정 명의로 지정, 정원 집계 자체는 그대로 19석 유지)과 **은채(mentor-3) 멘토와 상담 진행중(in_progress) 이력 1건**(`data/consultations.js`)을 시드로 연결해, 로그인하자마자 합격 상태・배정된 멘토・"상담 신청/문의하기" 버튼이 있는 화면을 바로 확인할 수 있게 함. `Login.jsx`의 테스트 계정 안내 문구에도 추가.
2. **교육시설 소개 페이지 실사진 적용 — 완료**: 사용자가 추가한 `reference/학원시설.png`(5개 공간이 3+2 배열로 한 장에 모인 콜라주, 각 칸에 영문 라벨 텍스트 포함)를 칸별로 분리 — 라벨 텍스트가 걸리지 않는 영역만 골라 4:3 비율로 크롭(기존 카드 레이아웃과 동일 비율) 후 `public/images/facilities/{theory,practice,collab,mentoring,lounge}.jpg`로 저장, `data/facilities.js`의 5개 항목 `image: null`을 각 경로로 교체(placeholder 카메라 아이콘 대신 실제 사진이 `/facilities`에 표시됨).
- 이미지 크롭은 기존과 동일하게 `npm install --no-save sharp` → 크롭 → `npm uninstall sharp`로 제거(흔적 없음 확인). `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/login`에서 yuna@example.com/1234로 로그인해 `/mypage`의 합격 상태・"내 멘토"(은채) 표시 확인, `/facilities`에서 5장의 실제 사진 확인 필요.

## 로그인 힌트 줄바꿈 + 마이페이지 배지가 실제 지원 상태를 안 따라가던 버그 수정 (2026-08-07, 같은 날 후속 요청)

1. **로그인 화면 테스트 계정 안내 줄바꿈 — 완료**: 스크린샷으로 "각 계정이 한 줄씩 차지하도록 해달라"는 피드백. `Login.jsx`의 안내 문구(`·`로 한 줄에 이어붙였던 것)를 계정별로 `<br />`을 추가해 4줄로 분리(지원자/합격자/멘토/관리자 각 한 줄).
2. **마이페이지 프로필 배지가 실제 지원 상태를 반영하지 않던 문제 — 수정 완료**: "유나는 지원자가 아니라 합격자로 떠야 하고, 관리자가 상태를 바꾸면 같이 반영돼야 한다"는 지적. 원인은 `MyPage.jsx`의 `ROLE_LABEL = { user: '지원자', ... }`가 계정의 **role**(항상 `'user'`)만 보고 고정 문구를 붙이는 구조였고, 실제 지원(`ApplicationContext`) 합격 여부는 전혀 참조하지 않았던 것 — 그래서 유나처럼 이미 합격 좌석이 있는 계정도 프로필 배지는 계속 "지원자"로 보였음. `applicantIdentity(applications)` 함수를 추가해 **role이 `'user'`인 계정은 실제 지원 현황에서 배지를 도출**하도록 변경(합격 건이 있으면 "합격자", 없고 대기 건만 있으면 "대기자", 그 외엔 기존과 동일하게 "지원자"). 지원 데이터가 `ApplicationContext`의 단일 state이므로 관리자가 상태를 바꾸는 즉시 마이페이지 배지에도 자동 반영됨(별도 동기화 코드 불필요).
3. **"상태를 수정하면 다른 곳에도 다 반영되는지" 전체 점검 — 완료(위 1건 외 발견 없음)**: 지원(신청/합격/대기/취소)・상담(매칭/진행/완료)・비회원 상담 연락 여부・문의 답변 상태가 쓰이는 모든 화면(관리자 각 관리 섹션, 마이페이지 3개 탭 및 "내 멘토" 섹션, 멘토 대시보드)을 점검. 전부 `ApplicationContext`/`ConsultationContext`/`CallbackContext`/`InquiryContext`의 동일한 React state를 직접 구독해서 읽어오는 구조라 어느 화면에서 상태를 바꿔도 자동으로 반영되고 있었음(로컬 `useState`로 값을 따로 복사해두는 곳도 없음을 확인) — 유일하게 반영이 안 되던 곳이 위 2번의 마이페이지 프로필 배지였고, 이번에 함께 고침.
- **참고(확인 요청)**: "지수, 유나 모두 지원자 목록에 있는지" 확인해보니, **유나**는 `data/applications.js`에 합격(confirmed) 좌석으로 시드되어 있어 관리자 "지원자 관리" 목록에 정상적으로 보임. 다만 **지수는 멘토 계정**(`mentor-1`)이라 애초에 국비 과정에 지원한 이력 자체가 없어 지원자 목록에 나타나지 않음 — 혹시 "민지"를 말씀하신 거라면, 민지도 아직 지원 이력이 전혀 없는 백지 계정이라 마찬가지로 지원자 목록에는 없음(로그인만 가능한 상태). 의도하신 계정이 다르면 알려주시면 확인하겠습니다.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/login`(계정 안내 줄바꿈), yuna@example.com으로 로그인해 `/mypage` 배지가 "합격자"로 뜨는지, `/admin`에서 유나의 지원 상태를 바꿔보고 마이페이지가 바로 바뀌는지 직접 확인 필요.

## 학원 일정 공휴일 반영 + 로그인 화면 개선 2건 + 멘토 사진 재중앙 (2026-08-07, 같은 날 후속 요청)

1. **`/schedule`에 공휴일 반영 — 신규 추가(기존엔 전혀 없었음)**: 확인해보니 지금까지 일정 타입은 모집/수업/행사 3종뿐이고 공휴일 데이터 자체가 없어서, 공휴일에도 "수업" pill이 그대로 뜨는 상태였음(월~목요일 조건만 봤을 뿐 공휴일 여부는 안 봄). `data/holidays.js` 신규 추가 — 2025~2027년 초(학원 일정이 다루는 전체 기간) 대한민국 공휴일을 담음(신정・삼일절・어린이날・현충일・광복절・개천절・한글날・성탄절은 양력 고정일이라 정확하고, 설날・추석・부처님오신날은 음력 기준이라 매년 날짜가 바뀌어 공식 캘린더 기준 최선의 추정치로 채움 — 대체공휴일 규정까지 계산해서 반영했지만, 실 서비스 전환 시 최신 공식 달력으로 재확인 필요하다고 코드 주석에 명시). `Schedule.jsx`에서 이 공휴일들을 기존 일정과 동일한 형태(`type: 'holiday'`)로 합쳐서 달력 칸・일정 목록・상세 모달에 노출(빨간 점/배지, 날짜 숫자도 빨간색으로 표시)하고, `isClassDay`가 공휴일도 함께 걸러내도록 수정해 월~목이라도 공휴일이면 "수업" pill이 더 이상 뜨지 않음.
2. **로그인 화면 계정별 줄바꿈 — 완료**: 스크린샷 피드백대로 `·`로 이어붙였던 테스트 계정 안내를 계정별 한 줄씩(`<br />`)으로 분리.
3. **로그인 화면 테스트 계정 안내 가시성 개선 — 완료**: 옅은 회색 작은 글씨로 눈에 잘 안 띄던 안내 문구를, 초록 테두리 박스(연한 배경)로 감싸고 제목 줄과 각 이메일을 굵게 표시해 눈에 잘 띄도록 변경.
4. **회원가입 이메일 중복 확인 + 비밀번호 확인 — 완료**: `AuthContext.jsx`에 `isEmailTaken(email)` 추가. 회원가입 폼 이메일 입력칸 옆에 "중복 확인" 버튼을 추가해 클릭 시 형식 검증 후 사용 가능/이미 사용 중 메시지를 바로 보여주고, 이메일을 다시 수정하면 재확인이 필요한 상태로 자동 전환(체크된 이메일 값과 현재 입력값이 달라지면 무효화). "비밀번호 확인" 입력칸을 신규 추가해 입력하는 즉시 일치 여부 메시지를 보여주고, 제출 시에도 ① 이메일 중복 확인 완료 여부 ② 비밀번호 일치 여부를 서버 없이 클라이언트에서 재검증해 통과하지 못하면 가입이 진행되지 않도록 함.
5. **멘토 지수・태호 프로필 사진 재중앙 — 완료**: "얼굴이 오른쪽 아래로 치우쳐 있다"는 피드백으로, 기존 크롭 값을 미세 조정하는 대신 원본 콜라주(`reference/멘토 프로필 사진.png`)에서 각 인물 패널의 경계를 픽셀 단위로 다시 측정하고 얼굴 중심 좌표를 새로 잡아 정사각형 크롭을 완전히 새로 생성(좌우・상하 모두 얼굴 중심이 정확히 크롭 중앙에 오도록 계산). `public/images/mentors/{jisu,taeho}.jpg` 교체 — 은채는 이번 피드백 대상이 아니라 그대로 둠.
- 이미지 작업은 기존과 동일하게 `npm install --no-save sharp` → 크롭 → `npm uninstall sharp`로 제거(흔적 없음 확인). `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/schedule`(공휴일 빨간 표시 확인 — 예: 2026년 1월 1일 신정), `/login`(줄바꿈된 계정 안내 박스, 회원가입 모드에서 이메일 중복확인・비밀번호 확인 동작), `/mentors`(지수・태호 사진 중앙 정렬) 직접 확인 필요.

## 멘토 상담 자동매칭 폐지(전면 수동 배정) + 상담분야 "교육과정 지원" 추가 (2026-08-07, 같은 날 후속 요청)

1. **상담 분야에 "교육과정 지원" 추가 — 완료**: `data/mentors.js`의 `CONSULTATION_CATEGORIES`에 신규 추가하고, 커리어 멘토 태호(기존 취업/이력서・학습 방법/진로 담당)의 전문분야에 포함시킴(진로 상담과 자연스럽게 이어지는 영역이라 단일 담당으로 지정 — 다른 멘토와 함께 담당시키길 원하면 `mentors.js`에서 조정 가능).
2. **상담 신청 시 즉시 자동 매칭되던 것을 전면 폐지, 관리자 수동 배정으로 전환 — 완료**: `ConsultationContext.jsx`의 `requestConsultation`이 분야・부하 기준으로 멘토를 즉시 자동 배정하던 `pickAvailableMentor` 로직을 제거 — 이제 모든 신청은 예외 없이 `requested`(배정 대기중) 상태로만 접수되고, 관리자가 "상담 매칭 모니터링"에서 요청 내용을 직접 읽고 멘토를 수동 지정해야 `matched`로 전환됨(기존에 있던 관리자 수동 배정 UI・분야 일치 우선 정렬은 그대로 재사용, 이제 유일한 배정 경로가 됨). 멘토가 상담을 거절했을 때 다른 멘토로 자동 재매칭하던 로직도 제거하고, 거절 시 무조건 `requested`로 되돌려 관리자가 다시 지정하도록 통일. 자동매칭을 전제로 안내하던 문구들도 전부 함께 수정: 상담 신청 페이지 안내문・제출 후 결과 메시지, `/mentors` 인트로, 홈 여정 그래프의 "멘토 매칭" 단계 설명, FAQ 2건("멘토 상담은 어떻게 신청하나요?"), 관리자 "멘토 현황"・"상담 매칭 모니터링" 섹션 설명 문구.
3. **"민지 계정으로 상담 신청했는데 관리자에서 아무것도 안 보인다" — 코드 버그 아님, 원인 확인**: `ConsultationProvider`는 `main.jsx`에서 앱 최상단에 한 번만 생성되어 `/consultation`과 `/admin` 양쪽이 완전히 같은 상태를 공유하고, 관리자의 "상담 매칭 모니터링" 표는 상태와 무관하게 모든 상담 신청을 필터링 없이 보여주는 구조라 코드상 표시 로직 버그는 발견되지 않음. 가장 유력한 원인은 이 프로젝트가 실제 DB 없이 브라우저 메모리에서만 데이터를 유지하는 구조라, **신청 이후 브라우저 새로고침(또는 개발 서버의 코드 변경으로 인한 자동 새로고침)이 한 번이라도 있었다면 그 사이 만든 상담 신청이 전부 초기화되어 사라진다는 점**(민지의 로그인 세션은 `localStorage`에 남아 있어 로그인 상태처럼 보이지만, 상담 신청 자체는 별도로 저장되지 않음) — 이전에 안내드린 "새로고침하면 방금 가입한 회원 정보가 사라지는" 한계와 동일한 원인. 이번에 자동매칭을 없애 모든 신규 신청이 무조건 "배정 대기중"으로 눈에 띄게 남게 되어, 앞으로는 관리자가 놓치기 더 어려워짐. 다만 이 새로고침 시 데이터가 사라지는 근본 한계 자체를 없애려면(localStorage 등에 실제로 저장) 별도 작업이 필요한데, 원하시면 진행하겠습니다.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/consultation`에 새로 생긴 "교육과정 지원" 분야로 신청 → 결과 메시지가 "배정됨"이 아니라 "접수됨"으로 뜨는지, `/admin`의 "상담 매칭 모니터링"에 방금 신청이 바로 "배정 대기중"으로 뜨는지, 멘토 수동 지정 후 `/mypage`・`/mentor`에 반영되는지 직접 확인 필요.

## 커뮤니티 작성자 확인 + 강사/멘토 담당 정보 강화 + 테스트 계정 성 추가 + 요약 통계 그룹화 (2026-08-07, 같은 날 후속 요청)

1. **커뮤니티 글・댓글 작성자가 회원인지 관리자가 확인 — 신규 추가(중요 발견 포함)**: 지금까지 `Admin.jsx`에 커뮤니티 관련 섹션이 전혀 없어 관리자가 확인할 방법 자체가 없었음. "커뮤니티 관리" 섹션을 신규 추가해 모든 게시글・댓글을 최신순으로 나열하고, 각 항목마다 작성자 이름뿐 아니라 **계정 이메일**(`mockUsers`에서 `authorId`로 조회)까지 함께 표시 — 실제 로그인 계정과 연결되어 있는지 한눈에 검증 가능. **조사 중 발견한 사실**: `CommunityContext.jsx`의 `createPost`/`addComment`는 `currentUser`가 없으면 아예 실행되지 않아 실제 서비스 동작상으로는 비회원 작성이 원천 차단되어 있음(요구사항 충족). 다만 `data/communityPosts.js`의 **시드(데모용) 게시글・댓글은 애초에 `authorId` 없이 이름만 있는 가상 인물**(오세훈・김하늘・이지우 등, 실제 로그인 계정과 무관한 데모 flavor 데이터라고 파일 상단에 이미 주석으로 명시돼 있었음)이라, 관리자 화면에서는 이 시드 데이터들이 전부 "알 수 없음"으로 표시됨 — 버그가 아니라 데모 데이터의 특성이며, 지원자・상담 신청의 무작위 시드(`seed-user-N`, `seed-student-N`)도 실제 계정과 연결되지 않는 동일한 패턴을 이미 쓰고 있음. 실제 테스트 계정(김민지・이유나 등)으로 새로 작성한 글・댓글은 정상적으로 이메일이 표시됨. 시드 데이터까지 전부 실제 계정에 연결시키길 원하시면 별도로 정리 가능합니다.
2. **관리자가 강사・멘토의 수업/담당 정보를 볼 수 있도록 — 신규 추가**: "강사 관리" 섹션을 신규 추가해 강사 4명의 이름・직함・전문분야・담당 커리큘럼 주차(`data/instructors.js`)를 표로 노출(현재 강사-학생 배정 개념 자체가 없어 조회 전용). 기존 "멘토 현황" 섹션은 진행 건수 숫자만 보여주던 것에서, **지금 담당하고 있는 회원 명단(이름 · 분야 · 상태)**까지 각 멘토 카드 안에 함께 표시하도록 확장(`getMentorConsultations` + 비회원 배정을 합산) — "지수・태호가 정확히 누구를 맡고 있는지"가 이제 카드 하나로 바로 보임.
3. **테스트 계정 회원 2명(민지・유나) 성 추가 — 완료**: `data/users.js`에서 `민지`→**김민지**, `유나`→**이유나**로 변경(멘토 지수・관리자 성훈은 대상 아님, 기존 멘토 데이터 명명 관례와 동일하게 given-name만 쓰는 스타일이라 그대로 유지). 같은 이름을 직접 들고 있던 `data/applications.js`・`data/consultations.js`의 유나 시드 레코드도 함께 수정(계정과 항상 같은 이름을 쓰도록). 참고로 `data/portfolios.js`・`reviews.js`에 이미 "최유나"라는 무관한 인물이 있어 겹치지 않는 성을 골랐음.
4. **요약 통계 카드를 그룹화 + 클릭 시 해당 섹션으로 이동 — 완료**: "카드가 너무 많아서 뭐가 뭔지 모르겠다"는 피드백으로, 기존에 12개 카드가 구분 없이 일렬로 늘어서 있던 것을 **지원자 현황(5)・상담 현황(3)・비회원 상담 신청 현황(2)・문의 현황(2)** 4개 그룹으로 묶고 그룹 제목을 추가, 그룹 내에서 중복되던 접두어("상담 신청(비회원) 연락대기"→"연락대기" 등)도 정리해 라벨을 짧게 다듬음. 모든 카드를 `<button>`으로 바꿔 클릭하면 `scrollIntoView`로 해당 관리 섹션(지원자 관리・상담 매칭 모니터링・상담 신청 관리(비회원)・문의 관리)까지 부드럽게 스크롤 이동하도록 함(학원 일정 "수정" 클릭 시 폼으로 스크롤하던 것과 동일한 패턴 재사용).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/admin`의 새 "커뮤니티 관리"(시드 항목은 "알 수 없음", 실제 로그인 후 작성한 글은 이메일 표시), "강사 관리", "멘토 현황"의 담당 회원 명단, 요약 통계 그룹화・카드 클릭 시 스크롤 이동, `/login`에서 김민지・이유나로 로그인해 이름 반영 확인 직접 필요.

## 커뮤니티 시드 작성자 실제 계정 연결 + 관리자 화면 노출 문구 수정 + 커뮤니티 관리 섹션 제거 (2026-08-07, 같은 날 후속 요청)

1. **커뮤니티 시드 글・댓글 작성자를 전부 실제 로그인 계정과 연결 — 완료**: 직전 작업에서 발견한 대로 `data/communityPosts.js`의 시드 글・댓글 작성자(오세훈・김하늘・이지우・최유진・박서준, 5명)에게 `authorId`가 없어 실제 계정과 연결되어 있지 않았음. "이미 회원이 많이 있는 것처럼" 해달라는 요청대로, `data/users.js`에 이 5명을 전부 정식 로그인 계정(이메일・비밀번호 1234・역할 user)으로 추가하고, `communityPosts.js`의 모든 글・댓글에 해당 `authorId`를 연결(총 5개 글 + 9개 댓글 전부 실제 계정과 매칭). 이제 커뮤니티에 등장하는 모든 작성자가 실제 회원 데이터와 연결되어 있음 — 다만 이 5개 계정은 로그인 화면에 노출되는 대표 테스트 계정 목록에는 추가하지 않음(배경 회원 데이터 성격이라 구분).
2. **관리자 화면에 파일 경로가 그대로 노출되던 문제 — 수정 완료**: "강사 관리" 섹션 설명문에 실수로 개발자용 코드 주석에 가까운 문구("담당 주차는 data/instructors.js의 커리큘럼 구성과 짝을 이룹니다")를 그대로 넣어, 관리자 화면에 파일 경로가 노출되고 있었음. 사용자용 안내 문구("현재 등록된 강사진과 담당 커리큘럼입니다.")로 교체. 다른 관리자 화면 문구들도 전부 점검해 동일한 문제가 없는지 확인함(추가 발견 없음).
3. **관리자 페이지 "커뮤니티 관리" 섹션 제거 — 완료**: 직전 요청("게시글・댓글 작성자가 회원인지 확인할 수 있는지")을 별도의 관리자 UI 섹션 추가로 오해해 구현했었는데, 실제로는 "글을 쓴 사람이 실제 회원인지"에 대한 데이터 확인만 필요했던 것이라는 피드백을 받고 해당 섹션(글・댓글 목록 표, 관련 계산 로직・CSS)을 전부 제거함. 회원 연결 자체는 위 1번으로 이미 보장되어 있어 별도 화면 없이도 요구사항이 충족됨.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/community`(기존 글・댓글이 그대로 보이는지, 특히 이제 osehun@example.com 등으로 로그인해서 자기 글에 댓글을 남길 수도 있는지), `/admin`의 "강사 관리" 설명 문구가 정상적으로 보이는지, "커뮤니티 관리" 섹션이 더 이상 없는지 직접 확인 필요.

## 관리자 대시보드 탭 분리 (2026-08-07, 같은 날 후속 요청)

"한 페이지에 너무 많은 내용이 길게 들어있다"는 피드백으로 `Admin.jsx`를 탭 구조로 재편.
- 상단에 `role="tablist"` 탭 6개(요약 / 지원자 관리 / 강사・멘토 / 상담 관리 / 문의 관리 / 학원 일정) 신규 추가 — `MyPage.jsx`의 기존 탭 UI 패턴(밑줄 강조, `aria-selected`)을 그대로 재사용. 기존에 있던 8개 섹션(요약 통계・지원자 관리・강사 관리・멘토 현황・상담 매칭 모니터링・상담 신청 관리(비회원)・문의 관리・학원 일정 관리)을 성격이 비슷한 것끼리 묶어 6개 탭에 배정(강사 관리+멘토 현황 → "강사・멘토", 상담 매칭 모니터링+비회원 상담 신청 관리 → "상담 관리").
- 어제 추가했던 "요약 통계 카드 클릭 시 `scrollIntoView`로 해당 섹션까지 스크롤" 기능은, 이제 섹션들이 다른 탭에 숨겨져 있어 스크롤만으로는 보이지 않으므로 **클릭 시 해당 탭으로 전환**하도록 동작을 변경(관련 `ref` 4개와 `scrollToSection` 함수는 제거, `setActiveTab` 호출로 교체). 학원 일정 관리 탭 안에서 "수정" 클릭 시 폼으로 스크롤하던 기능은 같은 탭 내부 이동이라 기존 그대로 유지.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/admin`의 탭 6개를 각각 눌러 해당 섹션들이 올바르게 나오는지, "요약" 탭의 통계 카드 클릭 시 맞는 탭으로 전환되는지 직접 확인 필요.

## 첫 GitHub 푸시 + Vercel 프로덕션 배포 (2026-08-07)

1. **GitHub 첫 커밋 + 푸시 — 완료**: 이 프로젝트는 지금까지 로컬에서만 작업해 커밋 이력이 전혀 없었음. `reference/`(원본 이미지, 약 96MB)는 저장소에 올릴지 사용자에게 확인 후 **제외하기로 결정**(`.gitignore`에 추가 — 실제 서비스에 쓰이는 이미지는 이미 `public/images/`에 다 복사되어 있어 앱 동작에는 영향 없음). 기본 브랜치명을 `master`→`main`으로 변경(GitHub 원격 저장소가 완전히 비어 있어 브랜치명 제약 없음, 최신 관례에 맞춤). 198개 파일을 최초 커밋으로 묶어 `https://github.com/chlghdsud0827-cell/finalproject.git`의 `main` 브랜치로 푸시 완료.
2. **Vercel 프로덕션 배포 — 완료**: 기존에 연결되어 있던 Vercel 프로젝트(`ui-ux-course-site`)로 `vercel deploy --prod` 실행(첫 시도는 업로드 중 네트워크 오류로 실패, 재시도 후 성공). 배포 URL: `https://ui-ux-course-site.vercel.app`.
3. **(발견) Vercel 프로덕션에 환경변수가 하나도 설정되어 있지 않음**: `vercel env ls production` 확인 결과 환경변수가 0개 — 로컬 `.env.local`의 `VITE_KAKAO_MAP_APP_KEY`는 git에 포함되지 않는 게 정상(보안상 당연함)이지만, **Vercel 프로젝트 자체에도 별도로 등록되어 있지 않아서 배포된 사이트의 `/location`・`/branches` 페이지는 실제 카카오맵 대신 placeholder("지도 영역")로 보임**. 실제 지도를 띄우려면 Vercel 대시보드(Settings → Environment Variables)에 `VITE_KAKAO_MAP_APP_KEY`를 직접 등록하고 재배포해야 함(키 값은 민감정보라 이 환경에서 자동으로 처리하지 않음).
- 배포된 사이트에서 로그인(테스트 계정), 지원, 상담 신청 등 핵심 플로우가 실제로 동작하는지, 그리고 `/location`・`/branches`의 지도 placeholder 노출을 직접 확인 권장.

## AI 툴 로고 적용 + 오시는 길/지점 주소 확정 + 카카오톡 채널 ID 임의 지정 (2026-08-07, 같은 날 후속 요청)

이전에 "다음 할 일"로 제안했던 항목 중 DB 연결(보류)・개인정보처리방침(보류)・전체 QA(보류)를 제외한 나머지를 처리.

1. **AI 툴 로고 4개 적용 — 완료**: 사용자가 `reference/`에 추가한 Claude・ChatGPT・Gemini・Figma 공식 아이콘 파일을 정리해 적용. Gemini 아이콘만 원본이 정사각형이 아니어서(600×335) 중앙 기준으로 정사각형 크롭 후 4개 전부 200×200으로 통일, `public/images/ai-logos/{claude,chatgpt,gemini,figma}.png`로 저장하고 `data/aiTools.js`의 `logo: null`을 각 경로로 교체(기존 이니셜 placeholder 대신 `/course`의 "실무에서 쓰는 AI 툴" 섹션에 실제 로고 표시).
2. **오시는 길(본원) + 다른 지점 4곳 주소 확정 — 완료**: "실제 위치가 없으니 적당히 랜덤으로 지정하고 지도 위치도 맞춰달라"는 요청. `data/contactInfo.js`・`data/branches.js`의 "테헤란로 000(주소 확정 예정)"류 placeholder에 구체적인 번지・층수를 채워 넣음(예: 강남 본원 "테헤란로 231, 5층"). 확인해보니 **기존 위도・경도 좌표는 이미 각 안내문에 적힌 지하철역(강남역・정발산역・부평역・정부청사역)의 실제 좌표와 정확히 일치**하도록 이전부터 잘 지정되어 있었음 — 그래서 좌표 자체는 바꿀 필요 없이 주소 텍스트만 확정하면 지도 위치와 자연스럽게 맞아떨어짐. 다만 수원점만 안내 문구가 "인계로"(수원시청 인근) 주소인데 지하철 안내는 "수원역"으로 되어 있어 서로 어긋나 있던 것을 발견해 "수원시청역"으로 수정(좌표는 원래 수원시청 기준이라 이게 맞음). 각 안내에 붙어있던 "(예시)" 표기도 이제 확정된 정보이므로 제거.
3. **카카오톡 채널 ID 임의 지정 — 완료**: `contactInfo.js`의 `kakaoChannelId`를 빈 값(→"준비중" 표시)에서 형식만 갖춘 임의 문자열(`_xqTpK9m`)로 변경. 이제 카카오톡 상담 팝업에 "채널 추가하기" 버튼이 정상 노출되어 작동하는 것처럼 보이지만, 실제로 등록된 채널이 아니라 클릭하면 카카오 쪽에서 "존재하지 않는 채널" 오류가 뜸(요청하신 대로 "되는 것처럼 보이지만 실제로는 안 되는" 상태).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. 이미지 작업은 기존과 동일하게 `npm install --no-save sharp` → 처리 → `npm uninstall sharp`로 제거(흔적 없음 확인). dev 서버(`localhost:5173`)에서 `/course`(AI 툴 로고 4개), `/location`(주소・지도), `/branches`(4개 지점 주소・지도), 우측 하단 상담 버튼 → 카카오톡 상담(채널 추가하기 버튼 노출) 직접 확인 필요. **아직 push는 하지 않음** — 확인 후 반영해달라고 하면 커밋・배포 진행하겠습니다.

## 주소↔지도 좌표 정합성 재검증 (WebSearch로 실제 좌표 대조) (2026-08-07, 같은 날 후속 요청)

"주소가 실제로 지도에 표시되는 위치와 일치하냐"는 질문을 받고, 지어낸 주소 5곳을 WebSearch로 하나씩 대조 검증(이전엔 지하철역 이름만 기억에 의존해 대조했음).
- **강남 본원 — 불일치 발견 후 수정**: `테헤란로 231`을 골랐었는데, 검색해보니 이 번지는 실존하는 "센터필드" 건물 주소이고, 센터필드는 테헤란로・언주로 교차로(강남역이 아니라 **역삼역** 바로 앞)에 있음 — 좌표(강남역)와 "강남역 도보 5분"이라는 안내 문구 둘 다와 맞지 않는 상태였음. 테헤란로는 강남역 사거리에서 시작해 역삼역 방향(동쪽)으로 갈수록 번지수가 커지므로(강남파이낸스센터 152・센터필드 231 모두 역삼역 인근), 강남역 도보 5분 거리에 맞는 낮은 번지수 `테헤란로 34`로 교체.
- **대전점 — 오차 확인 후 좌표 보정**: 정부청사역의 실제 좌표(위키백과 기준 36.357675, 127.381019)와 기존에 넣어둔 좌표(36.351427, 127.384953)를 대조하니 약 780m(도보 10분 이상) 차이로, "도보 6분" 안내와 맞지 않아 실제 좌표에서 약 200m 이내로 들어오도록 보정(36.3562, 127.3825).
- **나머지 3곳(일산・수원・인천) — 검증 결과 이상 없음**: 정발산역(중앙로 1270 인근 vs 지정한 1275)・수원시청역(검색된 좌표와 약 260m 차이)・부평역(부평구청 실주소 168 사용, 검색된 부평역 좌표와 약 400m 차이)은 모두 "도보 5~10분" 범위 안에 들어오는 오차라 그대로 둠.
- **참고**: 위 대조는 전부 텍스트 기반 웹 검색 요약에 의존한 것이라(검색 자체가 자동 요약이라 스니펫 간 사소한 수치 불일치도 있었음), 100% 정밀한 실측치는 아니지만 "같은 동네・도보 거리 안"이라는 수준의 정합성은 확보됨.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. 아직 push는 하지 않음.

## 지점 지도 좌표를 랜드마크 건물에서 일반 건물 위치로 조정 (2026-08-07, 같은 날 후속 요청)

"지도에 표시된 위치가 역・시청 같은 공공건물로 보여서 이상하다"는 피드백. 직전 작업에서 도보 거리를 맞추려고 지하철역・구청 등 실제 랜드마크의 좌표를 거의 그대로 썼던 게 원인 — 예를 들어 인천점은 부평구청의 실제 주소(부평대로 168)를 그대로 썼고, 나머지도 역 좌표와 사실상 같은 지점이라 지도에 학원이 아니라 그 공공기관 건물이 그대로 보이는 상태였음.
- 강남 본원 + 지점 4곳 전부, 안내문에 적힌 지하철역 좌표에서 그 역이 있는 도로를 따라 약 200~300m씩 이동(방향은 각 도로가 뻗은 방향에 맞춤) — 여전히 "도보 5~10분" 범위 안이라 기존 오시는 길 안내 문구와는 어긋나지 않으면서, 지도상에는 역・구청 건물이 아니라 인근의 평범한 건물 위치로 보이게 됨. 주소 번지수도 이동한 위치에 맞게 살짝 조정(인천점은 부평구청 실주소 168과 겹치지 않도록 140으로 변경).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/location`・`/branches`의 지도 핀 위치가 이제 역/관공서 건물이 아니라 인근 건물로 보이는지 직접 확인 필요. 아직 push는 하지 않음.

## 수원점・대전점 지도 위치가 주차장으로 보이던 문제 수정 (2026-08-07, 같은 날 후속 요청)

"수원점, 대전점 지도가 주차장에 표시된다"는 피드백. 직전 수정에서 좌표를 역 위치에서 임의로 200~300m씩 추정 이동시켰는데, 두 곳은 하필 관공서 주변 주차장 부지에 떨어진 것으로 보임 — 방향 감각만으로 어림잡아 옮기는 방식의 한계.
- 이번엔 임의 추정 대신 WebSearch로 실제 아파트・오피스텔 건물의 좌표를 찾아 대체: 수원점은 수원시청역 인근 실제 오피스텔 단지 좌표로, 대전점은 정부청사역에서 약 500m(도보 5~7분, 부동산 매물 정보로 확인) 거리의 실제 아파트 단지 좌표로 교체. 둘 다 특정 세대 매물 정보가 검색되는 실존 건물이라 지도에 평범한 주거・업무용 건물로 표시될 것으로 예상됨.
- 일산점・인천점은 이번 피드백 대상이 아니었고 동일한 검증 방법으로 재확인해봤으나 대체할 정확한 좌표를 찾지 못해 기존 값 유지 — 혹시 이 두 곳도 이상하게 보이면 알려주시면 같은 방식으로 다시 잡겠습니다.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/branches`의 수원점・대전점 지도가 이제 건물로 보이는지 직접 확인 필요. 아직 push는 하지 않음.

## DB 연결 1단계: 계정(회원가입/로그인)을 Supabase로 전환 (2026-08-07)

사용자가 이미 쓰고 있는 Supabase 프로젝트 3개 중 하나(`chlghdsud0827-cell's Project`, URL: `ocvrmomeqcesodqqqyll.supabase.co`) 안에, 다른 앱과 테이블이 섞이지 않도록 전용 스키마(`academy`)를 새로 만들어 이번 프로젝트 전용으로 쓰기로 함(무료 플랜 프로젝트 개수 제한 때문에 새 프로젝트를 만들 수 없어서 나온 대안).

1. **`@supabase/supabase-js` 설치 + 클라이언트 설정 — 완료**: `src/lib/supabaseClient.js` 신규 추가, 기본 스키마를 `academy`로 지정해서 이후 모든 쿼리가 자동으로 `academy.*` 테이블을 가리키게 함. `.env.local`에 `VITE_SUPABASE_URL`・`VITE_SUPABASE_ANON_KEY` 추가(anon key는 공개돼도 되는 키 — RLS로 보호됨. service_role 키는 받지도, 쓰지도 않음).
2. **SQL 마이그레이션 작성 — 완료(사용자가 직접 실행 필요)**: `supabase/migrations/0001_academy_profiles.sql` — `academy` 스키마 + `academy.profiles` 테이블(로그인 자체는 Supabase Auth의 `auth.users`가 담당하고, 이 테이블은 회원가입 때 추가로 받는 이름・역할・생년월일・성별・전화・주소만 보관) + RLS 정책 3개(로그인 회원은 전체 조회 가능・본인 프로필만 생성/수정 가능).
3. **`AuthContext.jsx`를 mock 배열 → Supabase Auth로 전면 교체 — 완료**: `login`/`register`/`logout`/`isEmailTaken`이 전부 `supabase.auth.*` + `academy.profiles` 호출로 바뀌면서 비동기 함수가 됨(기존엔 동기 함수였음). 페이지 새로고침 시 Supabase 세션을 비동기로 복원하는 동안 `RequireAuth`가 성급하게 로그인 페이지로 튕기지 않도록 `loading` 상태를 추가. `Login.jsx`도 `handleSubmit`/`handleEmailCheck`를 `async`로 변경, 제출 중 버튼 비활성화 추가.
4. **테스트 계정 비밀번호 `1234` → `123456`으로 변경 — 완료**: Supabase Auth 기본 정책상 비밀번호 최소 6자가 필요함. 이 정책을 낮추는 대신(보안상 기본값이 더 나음) 테스트 비밀번호 쪽을 6자로 맞춤. 로그인 화면 안내 문구도 함께 수정.
5. **계정 시딩 스크립트 작성 — 완료(아직 실행 안 함)**: `scripts/seed-accounts.mjs` — 기존 mock 테스트 계정 4개(김민지・이유나・지수・성훈) + 커뮤니티 시드 작성자 5명을 실제 Supabase Auth 계정 + `academy.profiles` 행으로 생성하는 1회성 스크립트. 아래 "사용자가 할 일" 완료 후 실행 예정.
6. **알려진 임시 제약(2단계에서 해소 예정)**: 아직 지원/상담/문의/커뮤니티/일정은 mock 데이터 그대로라, ① 새로 Supabase 계정으로 가입해서 지원・상담 등을 하면 관리자 화면에 이름 대신 계정 ID(UUID)가 뜰 수 있음(현재 `applicantName` 등이 여전히 옛 mock `users.js` 배열에서만 이름을 찾기 때문) ② 이유나 계정에 미리 연결해둔 합격/상담 시드 데이터는, 이유나가 새 Supabase 계정으로 재가입하면 계정 ID가 달라져서 마이페이지에 안 보일 수 있음. 둘 다 다음 단계(지원・상담 데이터도 Supabase로 이전)에서 실제 계정 ID로 다시 연결하면 해결됨.
- 아직 빌드만 통과 확인했고(`npm run lint`/`npm run build`), 실제 Supabase 연동 동작은 사용자가 아래 3가지를 완료하고 시딩 스크립트를 실행해야 확인 가능. push는 하지 않음.

### 사용자가 할 일 (Supabase 대시보드)
1. SQL Editor에서 `supabase/migrations/0001_academy_profiles.sql` 내용을 붙여넣고 실행
2. Project Settings → Data API → Exposed schemas에 `academy` 추가
3. Authentication → Sign In / Providers → Email → "Confirm email" 끄기 (테스트 계정이 이메일 인증 없이 바로 로그인되도록)

완료되면 알려주시면 `node --env-file=.env.local scripts/seed-accounts.mjs`를 제가 실행해서 계정 9개를 만들고, 로그인이 실제로 되는지 확인하겠습니다.

## 일산점・인천점 지도 위치가 도로 위로 보이던 문제 수정 (2026-08-07, 같은 날 후속 요청)

"일산점, 인천점도 지도가 도로 위에 찍혀있다"는 피드백. 수원・대전과 같은 원인(임의 추정 offset)이라, 이번엔 지오코딩 서비스(OpenStreetMap Nominatim, 무료・키 불필요)로 직접 좌표를 조회하는 방식으로 전환.
- **일산점**: "중앙로"만으로 검색하면 이 도로가 마두동・장항동・백석동・정발산동까지 여러 동에 걸친 긴 도로라 정발산역과 무관한 지점이 섞여 나옴 — 대신 정발산역 바로 앞 실제 상업 건물인 "라페스타"를 이름으로 검색해 실제 건물(라페스타 D동) 좌표를 찾아 적용. 정발산역에서 약 430m(도보 5~6분)로 기존 안내 문구와도 부합.
- **인천점**: 부평역 인근 실제 오피스텔(e편한세상 시티 부평역)의 도로명주소를 지오코딩해 도로 구간 좌표를 확인한 뒤, 도로 중앙이 아니라 인접 건물 쪽으로 소폭 이동.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과.

## 오늘(2026-08-07) 진행 요약 + 다음 진행 예정

**오늘 완료한 것** (이 파일 위쪽에 시간순으로 상세 기록됨, 아래는 목차):
1. GitHub 첫 커밋・푸시(`main` 브랜치) + Vercel 프로덕션 배포
2. AI 툴 로고(Claude・ChatGPT・Gemini・Figma) 4개 실제 이미지 적용
3. 오시는 길(본원)・지점 4곳 주소 확정 + 지도 좌표 정합성 검증(WebSearch로 실제 좌표 대조, 강남・대전 오류 발견해 수정)
4. 카카오톡 채널 ID 임의 지정("되는 것처럼 보이지만 실제로는 안 되는" 상태)
5. 지점 지도 좌표가 역・관공서・도로 위로 찍히던 문제를 3차례에 걸쳐 수정 → 최종적으로 5곳 전부 실제 건물(또는 검증된 인근 지점) 좌표로 안착(강남: 오피스 밀집가, 일산: 라페스타 상업건물, 수원・대전: 실제 아파트・오피스텔, 인천: 오피스텔 인근)
6. **DB 연결 1단계(계정) 착수**: Supabase 프로젝트에 `academy` 전용 스키마 설계(SQL 마이그레이션 작성), `AuthContext.jsx`를 mock 배열 → Supabase Auth로 전면 교체, 계정 시딩 스크립트 작성 — **아직 사용자의 Supabase 대시보드 설정 3가지(SQL 실행・스키마 노출・이메일 확인 끄기) 완료 대기 중이라 실제 동작 미검증**

**다음에 진행할 것** (우선순위 순):
1. **(진행 중) DB 연결 1단계 마무리**: 사용자가 위 대시보드 설정 3가지를 완료하면 → 시딩 스크립트 실행 → 로그인/회원가입 실제 동작 확인
2. **DB 연결 2단계**: 지원(Application) 데이터를 Supabase로 이전(1단계에 이어서 자연스러운 다음 순서 — 계정이 있어야 지원 데이터의 FK가 의미 있음)
3. **DB 연결 3~6단계**: 상담(정규・비회원) → 문의 게시판 → 커뮤니티 게시판 → 학원 일정 순으로 이전(진행 순서는 이전에 합의됨)
4. 개인정보처리방침 페이지(`/privacy`, 현재 placeholder) — 보류 중
5. 전체 브라우저 QA — 보류 중
- 이번 대화에서 만든 변경사항은 마지막 GitHub 푸시(2026-08-07 이전 대화, 커밋 `43f1aab`) 이후 전부 로컬에만 있고 아직 커밋・푸시・배포되지 않음. 사용자 요청으로 지금 커밋・푸시・배포 진행.

### 커밋・푸시・배포 완료
- GitHub: 커밋 `2d7c0df` (`feat: Supabase Auth 연동 착수 + AI 툴 로고/오시는 길 좌표 정리`) → `main` 브랜치 푸시 완료.
- Vercel 환경변수 3개 신규 등록(Production) — 이번에 처음 등록됨:
  - `VITE_SUPABASE_URL`・`VITE_SUPABASE_ANON_KEY`: 등록해두지 않으면 Supabase 클라이언트가 프로덕션에서 초기화에 실패해 사이트 전체가 깨질 수 있어 배포 전 필수로 먼저 등록.
  - `VITE_KAKAO_MAP_APP_KEY`: 이전 배포 때 발견했던 "프로덕션에 카카오맵 키가 없어 지도가 placeholder로 보이는" 문제를 이번에 함께 해결(로컬 `.env.local`에 있던 실제 키 값을 그대로 등록).
- `vercel deploy --prod` 실행 → https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).
- **주의**: 배포된 사이트에서 로그인/회원가입을 시도하면, 아직 Supabase 대시보드 설정 3가지(SQL 실행・스키마 노출・이메일 확인 끄기)와 계정 시딩이 안 되어 있어 에러가 날 수 있음 — 위 "사용자가 할 일" 완료 후 정상 동작 예정.

## DB 연결 1단계(계정) 최종 확인 완료 (2026-08-10)

사용자가 Supabase 대시보드 설정 3가지를 전부 완료. 시딩 스크립트를 처음 실행했을 때 `permission denied for schema academy` 에러 발생 — **원인**: 새로 만든 스키마는 `public`과 달리 `anon`/`authenticated` 역할에 기본 접근 권한이 없음(RLS는 "어떤 행을 볼 수 있는지"만 걸러줄 뿐, 그 전에 스키마/테이블 자체에 대한 GRANT가 먼저 필요). `supabase/migrations/0002_academy_grants.sql` 신규 작성(academy 스키마 USAGE + 테이블 CRUD 권한 + 앞으로 추가될 테이블에도 자동 적용되는 default privileges)해서 사용자가 SQL Editor에서 실행.
- 시딩 스크립트(`scripts/seed-accounts.mjs`)도 재실행에 안전하도록 수정 — 첫 시도에서 이미 auth 계정까지는 만들어졌던 상태라, "이미 가입됨" 에러가 나면 로그인해서 id를 다시 가져오고 프로필은 `insert` 대신 `upsert`로 저장하도록 변경.
- 재실행 결과 9개 계정 전부 정상 생성(`[OK]`). 이어서 관리자(admin@example.com)・멘토(jisu@example.com) 두 계정으로 실제 로그인 → 프로필 조회까지 end-to-end 테스트 성공(멘토 계정의 `mentor_id: "mentor-1"`까지 정확히 확인) — **DB 연결 1단계(계정) 완전히 동작 확인 완료**.
- 아직 커밋・푸시하지 않음(`0002_academy_grants.sql` 추가분). dev 서버(`localhost:5173`)에서 `/login`으로 직접 로그인・회원가입 테스트 권장(테스트 계정 비밀번호는 전부 `123456`).

## 상담 분야 "기타" 추가 + 이유나 계정 데이터 재연결 + 멘토 상태 직접 수정 (2026-08-10)

1. **상담 분야에 "기타" 추가 — 완료**: `data/mentors.js`의 `CONSULTATION_CATEGORIES`에 추가. 특정 멘토의 전문분야로는 지정하지 않음(어떤 멘토와도 매칭되지 않는 진짜 catch-all 분야라, 관리자가 "상담 매칭 모니터링"에서 내용을 보고 판단해 수동 배정 — 분야 일치 멘토가 없으니 부하 순으로만 정렬됨, 기존 로직 그대로 동작).
2. **이유나 계정에 미리 연결해둔 합격/멘토 배정이 안 보이던 문제 — 원인 확인 후 수정**: DB 연결 1단계에서 계정을 Supabase Auth로 옮기며 예고했던 대로, 이유나가 실제 Supabase 계정(새 UUID)으로 로그인하게 되면서 `data/applications.js`・`data/consultations.js`에 옛 mock id(`'user-yuna'`)로 심어둔 합격/상담 시드가 더 이상 본인 것으로 연결되지 않아 마이페이지에서 안 보였던 것. 이유나 계정으로 실제 로그인해서 Supabase가 발급한 진짜 id를 확인한 뒤, 두 파일의 `userId`/`studentId`를 그 id로 교체 — 이제 로그인하면 합격 상태・은채 멘토 배정이 정상적으로 보임. **"나의 지원현황을 볼 수 있는 페이지"는 이미 마이페이지(`/mypage`)에 있음** — 상단 프로필 배지(합격자/대기자/지원자)와 "지원 현황" 탭이 정확히 이 역할을 하고, 이번 수정으로 이유나 계정에서도 정상적으로 데이터가 뜨게 됨. 새로운 페이지를 만들 필요는 없었고, 연결이 끊어져 있던 게 원인.
3. **멘토 대시보드에서 상담 상태를 직접 되돌릴 수 있도록 — 완료**: 기존엔 "수락"(matched→in_progress)・"완료 처리"(in_progress→completed) 버튼으로 앞으로만 진행 가능해서, 잘못 눌렀을 때 되돌릴 방법이 없었음. `ConsultationContext.jsx`에 `setConsultationStatus(id, status)`를 추가해 matched・in_progress・completed 세 상태 사이를 자유롭게 오갈 수 있게 하고(요청 대기 상태로는 못 가게 막음 — 배정 해제는 기존 "거절" 버튼의 역할이라 겹치면 mentorId가 남은 채 대기 상태가 되는 모순이 생김), `MentorDashboard.jsx`의 각 상담 항목에 "상태 직접 변경" 드롭다운을 추가.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/consultation`에 "기타" 분야가 보이는지, yuna@example.com(비밀번호 123456)으로 로그인해 `/mypage`에서 합격 상태・은채 멘토 배정이 뜨는지, jisu@example.com으로 로그인해 `/mentor`에서 상태 드롭다운으로 되돌리기가 되는지 직접 확인 필요. 아직 커밋・푸시하지 않음.

## AI 인트로 이미지 교체 + Gemini 로고 크기 보정 (2026-08-10, 같은 날 후속 요청)

1. **"실무에서 쓰는 AI 툴" 인트로 이미지 교체 — 완료**: 사용자가 추가한 `reference/ai2.jpg`(736×736 정사각형, AI・인간 손 악수 이미지)를 컨테이너 비율(400×300, 4:3)에 맞춰 위아래를 살짝 잘라내 크롭(핵심 요소인 악수・"AI" 텍스트・주변 아이콘은 전부 보이도록). `data/aiTools.js`가 참조하는 경로(`/images/ai-agent-intro.jpg`)는 그대로 두고 파일 내용만 교체해 코드 변경 없이 반영.
2. **Gemini 로고만 다른 로고보다 작아 보이던 문제 — 완료**: 스크린샷으로 확인해보니 Claude・ChatGPT・Figma 로고는 아이콘이 정사각형 캔버스 가장자리까지 꽉 채우는데, Gemini만 원본 크롭 시 별 모양 주변에 여백이 많이 남아 같은 200×200 파일이어도 실제 아이콘이 훨씬 작게 보였던 것. `reference/제미나이 아이콘.jpg` 원본에서 별 모양 주변 여백을 줄여 다시 크롭(다른 로고들과 비슷한 여백 비율로) → `public/images/ai-logos/gemini.png` 교체.
- 이미지 작업은 기존과 동일하게 `npm install --no-save sharp` → 크롭 → `npm uninstall sharp`로 제거(흔적 없음 확인). `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/course`의 AI 인트로 이미지와 Gemini 로고 크기 직접 확인 필요. 아직 커밋・푸시하지 않음.

## DB 연결 2단계(지원) 착수: 테이블 생성 + 시드 데이터 (2026-08-10)

1. **`academy.applications` 테이블 + RLS — 완료**: `supabase/migrations/0003_academy_applications.sql` 작성・실행(user_id는 실제 로그인 계정만 연결하고, 정원을 채우는 더미 지원자는 user_id 없이 name만 저장하는 구조). 첫 시딩 시도에서 `new row violates row-level security policy` 발생 — 원인은 `applications_insert_own` 정책이 `auth.uid() = user_id`만 허용해 user_id가 null인 더미 지원자를 등록할 방법이 없었던 것. `0004_academy_applications_dummy_insert.sql`로 `user_id is null` 전용 INSERT 정책을 추가해 해결(Postgres RLS는 같은 command에 정책이 여러 개면 OR로 합쳐짐).
2. **시드 데이터 등록 — 완료**: `scripts/seed-applications.mjs` 작성 후 실행 — 이유나 계정으로 로그인해 실제 합격 지원 1건 등록, 이어서 계정 없는 더미 지원자 18명(무작위 이름) 등록. 최종 확인 결과 `course-ui-ux` 과정 confirmed 상태 19건으로 정확히 맞음(기존 mock의 "정원 20석 중 19석 확정" 시나리오와 동일).
3. **다음 남은 작업**: `ApplicationContext.jsx`를 mock 배열 → Supabase 비동기 호출로 전환하는 작업은 아직 안 함(이번엔 테이블・시드까지만 진행). `MyPage`・`Admin`・`CourseDetail`의 인터페이스는 그대로 유지될 예정이라 큰 수정 없이 붙을 것으로 예상.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과.

## 오늘(2026-08-10)까지 진행 내역 커밋・푸시・배포
- GitHub: 커밋 `dcfe5d2` → `main` 브랜치 푸시 완료.
- Vercel: `vercel deploy --prod` 실행 — 처음 두 번은 일시적 네트워크 오류(`Not authorized`, `fetch failed`)로 실패, 세 번째 재시도에서 성공. https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).
- 배포 대상에 포함된 것: 상담 분야 "기타" 추가, 이유나 계정 재연결, 멘토 상태 직접 수정 드롭다운, AI 인트로 이미지・Gemini 로고 교체, DB 연결 2단계 테이블/시드용 SQL 마이그레이션 파일 3개(실제 DB 반영은 Supabase 대시보드에서 별도로 이미 완료됨 — 이 커밋에 포함된 SQL 파일은 기록용).

## DB 연결 2단계(지원) 완료: ApplicationContext.jsx 전환 (2026-08-10, 같은 날 후속 요청)

1. **비로그인 방문자용 SELECT 정책 추가**: 과정 소개 페이지의 "정원 대비 확정" 배지는 로그인 여부와 무관하게 항상 보여야 하는데, 0003의 select 정책이 `authenticated` 전용이라 비로그인 상태에서는 지원 데이터가 하나도 안 보여 카운트가 항상 0으로 뜰 뻔했음 — `0005_academy_applications_anon_select.sql`로 `anon` 역할용 select 정책 추가.
2. **`ApplicationContext.jsx`를 mock 배열 → Supabase로 전환 — 완료**: `AuthContext.jsx`와 동일한 패턴 — 실제 비동기인 부분(최초 조회, `applyToCourse`・`cancelApplication`・`adminUpdateApplicationStatus`)만 Supabase 호출로 바꾸고, 나머지 조회 함수(`getCourseWithCapacity`・`getMyApplications`・`getAllApplications`・`hasActiveApplication`)는 로컬 state를 읽는 동기 함수로 그대로 유지 — 그래서 `MyPage`・`Admin`・`CourseDetail`은 코드 수정이 전혀 필요 없었음. 더 이상 쓰이지 않는 `data/applications.js`(mock 시드 배열)는 삭제.
3. **end-to-end 테스트 3가지 전부 성공**: ① 비로그인(anon) 상태로 정원 카운트 조회 → 19건 정상 조회 ② 김민지 계정으로 로그인해 실제 지원 생성 → 성공 ③ 본인 지원 내역 재조회 → 방금 만든 지원 건이 정확히 조회됨. 테스트로 만든 지원 데이터는 확인 후 바로 삭제해 김민지 계정을 원래의 백지 상태로 복원.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/course`(정원 배지), 로그인해서 실제 지원 → `/mypage`에 반영되는지, `/admin`에서 상태 변경이 되는지 직접 확인 권장. 아직 커밋・푸시하지 않음.

## 헤더 메뉴 개편 + 과정 소개 탭 분리 (2026-08-10, 같은 날 후속 요청)

1. **소통공간 메뉴에서 "멘토 상담" 제거, "공지사항"을 학원안내→소통공간으로 이동 — 완료**: `Header.jsx`의 `NAV_GROUPS` 수정. "멘토 상담"(`/consultation`)은 메뉴에서만 빠졌을 뿐 페이지・기능은 그대로라, `/mentors`・과정 소개・`BottomCta` 등 기존 곳곳의 "상담 신청하기" 링크로는 여전히 접근 가능.
2. **과정 소개(`/course`) 탭 분리 — 완료**: "국비교육과정 외 다른 과정 안내도 필요할 듯"이라는 요청으로, 상단에 4개 과정(국비 과정 포함) 탭을 추가해 전부 볼 수 있게 함. 국비 과정 탭은 기존 내용(모집요강・커리큘럼・지원 플로우 등) 그대로 유지. 나머지 3개 과정(UX 리서치 심화・프로덕트 디자인 부트캠프・디자인 시스템 마스터)은 여태 페이지 맨 아래 요약 카드로만 존재했던 것을 각자 전용 탭으로 승격 — `data/otherCourseDetails.js` 신규 작성해 과정별 "이런 걸 배워요"(스킬 태그)・간단한 커리큘럼 개요(국비 과정보다는 단계 수를 줄인 요약형)를 추가하고, 일정(진행기간・다음 기수 모집 예정)과 "모집이 시작되면 공지사항・학원 일정에서 안내" 문구를 표시(아직 모집 중이 아니라 지원 플로우는 없음). 기존에 페이지 맨 아래 있던 "다른 교육과정" 카드 그리드 섹션은 탭으로 대체되어 중복이라 제거.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/course`의 탭 4개 전환, 헤더 "소통공간"에 멘토상담이 빠지고 공지사항이 들어왔는지 직접 확인 필요.
- 커밋 `045a83b` → GitHub `main` 푸시 완료, Vercel 배포 완료(https://ui-ux-course-site.vercel.app, 200 OK 확인).

## DB 연결 3단계(상담) 완료: 정규 상담 + 비회원 콜백 (2026-08-11)

1・2단계(계정・지원)에 이어 상담 기능을 Supabase로 전환. "정규 상담"(로그인 회원, `ConsultationContext`)과 "비회원 상담"(로그인 없이 이름/연락처만 남기는 콜백 신청, `CallbackContext`)은 서로 다른 테이블로 분리.

1. **테이블 2개 + RLS — 완료**: `supabase/migrations/0006_academy_consultations.sql`(`academy.consultations` — 2단계 `applications`와 동일하게 신청 시점 이름을 `name` 컬럼에 직접 저장해, 관리자 화면에 UUID 대신 바로 이름이 뜨도록 함) · `0007_academy_callback_requests.sql`(`academy.callback_requests` — 비회원 기능이라 INSERT 정책에 `anon` 역할도 포함). 둘 다 사용자가 SQL Editor에서 실행 완료.
2. **`ConsultationContext.jsx`・`CallbackContext.jsx`를 mock → Supabase로 전환 — 완료**: `ApplicationContext.jsx`와 동일한 패턴(최초 조회 + mutation만 비동기 Supabase 호출, 나머지 조회 함수는 로컬 state를 읽는 동기 함수 유지) — `ConsultationRequest`・`MentorDashboard`・`Admin`・`MyPage`・`CourseDetail` 전부 코드 수정 없이 그대로 붙음. 더 이상 쓰이지 않는 `data/consultations.js`・`data/callbackRequests.js`(mock 시드 배열) 삭제.
3. **시드 데이터 등록 — 완료**: `scripts/seed-consultations.mjs`(실제 계정 4명으로 정규 상담 4건 — 기존 mock 시나리오와 동일하게 지수(mentor-1) 2건 진행 중, 은채(mentor-3)는 이유나 1건만) · `scripts/seed-callback-requests.mjs`(비회원 콜백 2건) 실행 완료.
4. **e2e 테스트 8단계 + 오탐 하나 조사 — 완료**: 신청(requested)→관리자 배정(matched)→멘토 수락(in_progress)→완료 처리(completed)→직접 되돌리기(matched)→거절(배정 해제, requested) 전부 정상 동작 확인. 테스트 중 "비회원(anon) 콜백 신청이 RLS에 막힌다"는 현상을 발견했었는데, 원인은 실제 버그가 아니라 **테스트 스크립트가 `.select().single()`로 insert 직후 결과를 되받으려 한 것** — Postgres RLS는 `RETURNING`이 있으면 INSERT 통과와 별개로 그 행에 대한 SELECT 권한도 필요한데, anon은 select 정책이 없어(의도된 설계 — 콜백 목록 조회는 관리자만 함) 여기서 막힌 것. 실제 앱 코드(`CallbackContext.jsx`)는 `.select()` 없이 insert만 하므로 이 문제와 무관함을 앱 코드와 동일한 방식으로 재현 테스트해 확인. GRANT・RLS 정책・PERMISSIVE 여부까지 전부 정상이었음(사용자가 SQL Editor로 `pg_policies`・`information_schema.role_table_grants`・`pg_policy` 세 차례 조회해 함께 확인).
5. **테스트 흔적 정리 — 완료**: `academy.consultations`・`academy.callback_requests` 둘 다 삭제 정책을 두지 않아(2단계 `applications`와 동일한 설계 — 앱에 삭제 기능이 없음) 스크립트로는 지울 수 없어, 사용자가 SQL Editor에서 직접 `delete` 실행. 최종 조회로 시드 데이터만 정확히 남은 것 확인(상담 4건, 콜백 2건).
- (같은 날 후속 요청) **메인 히어로 슬라이더 높이 확대 — 완료**: `HeroSlider.css`의 `.hero-slider__slide { min-height: 320px }` → `500px`로 변경("너무 가로로 긴 비율"이라는 피드백).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 `/consultation` 신청 → `/mypage`에 반영, `jisu@example.com`으로 `/mentor`에서 수락/완료/되돌리기, `/admin`에서 정규・비회원 상담 수동 배정, 메인 페이지 히어로 슬라이더 높이 직접 확인 권장. 아직 커밋・푸시하지 않음.

## 메인 히어로 1번 슬라이드에 애니메이션 gif 적용 (2026-08-11, 같은 날 후속 요청)

"메인비주얼01(비디오)"라는 이름으로 파일을 넣어뒀다는 요청 — 확인해보니 실제로는 mp4가 아니라 **움직이는 gif 파일**(9MB, 128프레임)이었음. gif는 `<video>` 태그 없이 기존처럼 CSS `background-image`에 URL만 넣어도 브라우저가 자동 재생하므로 별도 video 처리 로직 없이 그대로 적용 가능.

1. **gif 파일 적용 — 완료**: `reference/메인비주얼01(비디오).gif`를 `public/images/hero/slide-1.gif`로 복사(내용 변경 없이 그대로 — 이미 완성된 애니메이션이라 크롭・리사이즈 불필요), `data/heroSlides.js`의 1번 슬라이드 `image`를 `slide-1.jpg` → `slide-1.gif`로 교체. gif 프레임 딜레이를 읽어야 해서 이번에도 `npm install --no-save sharp`(gif metadata 조회 후 즉시 `npm uninstall sharp`로 제거, 흔적 없음)로 확인한 결과 총 재생 길이 **8.0초**(128프레임).
2. **슬라이드 전환 속도를 gif 길이에 맞춤 — 완료**: 기존 `HeroSlider.jsx`는 슬라이드 종류와 무관하게 5초마다 무조건 다음으로 넘기는 고정 `setInterval` 방식이라, gif가 8초짜리면 다 재생되기 전에 다음 슬라이드로 넘어가 버리는 문제가 있었음. `heroSlides.js`에 슬라이드별 `duration`(ms) 필드를 추가할 수 있게 하고(1번 슬라이드만 `8000`, 나머지는 기존처럼 기본값 5000 사용), `HeroSlider.jsx`는 고정 `setInterval` 대신 슬라이드가 바뀔 때마다 그 슬라이드의 `duration`만큼 새로 예약하는 `setTimeout` 방식으로 교체.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 메인 페이지 1번 슬라이드가 gif로 재생되는지, 8초간 머물다 다음 슬라이드로 넘어가는지, 나머지 슬라이드는 기존처럼 5초 간격인지 직접 확인 필요. **참고**: gif 파일 자체가 9MB로 꽤 무거워서(480×270 해상도인데도) 첫 로딩이 느리게 느껴질 수 있음 — 필요하면 다음에 압축이나 mp4 재인코딩을 검토할 수 있음. 아직 커밋・푸시하지 않음.

## 히어로 1번 슬라이드 gif → mp4로 교체 + 문구 위치 상단 정렬 (2026-08-11, 같은 날 후속 요청)

방금 적용한 gif가 480×270 원본 해상도를 가로 1400px 넘는 배너에 3배 가까이 늘려서 뿌옇게 보인다는 피드백("CCTV 수준") — 사용자가 같은 장면을 1280×720 mp4로 다시 내보내 `reference/`에 추가.

1. **문구(eyebrow・제목・버튼) 위치를 화면 상단으로 — 완료**: 스크린샷에 빨간 박스로 표시해준 위치가 기존 세로 중앙 정렬보다 위쪽이라, `HeroSlider.css`의 `.hero-slider__slide`를 `justify-content: center` → `flex-start`로 변경.
2. **gif → mp4 교체 — 완료**: `reference/메인비주얼01(비디오).mp4`(ffprobe 없이 mp4 `moov/mvhd`・`tkhd` 박스를 직접 파싱하는 임시 스크립트로 1280×720, 8.0초, 2.8MB 확인)를 `public/videos/hero-slide-1.mp4`로 저장, 더 이상 쓰지 않는 `public/images/hero/slide-1.gif` 삭제. `data/heroSlides.js`의 1번 슬라이드에 `video` 필드를 추가하고 `image`는 `<video>`의 `poster`(로딩 중 정지 프레임)로 재사용하도록 의미를 바꿈(원래 있던 `slide-1.jpg`로 되돌림).
3. **`HeroSlider.jsx`에 `<video>` 렌더링 추가 — 완료**: `slide.video`가 있으면 CSS `background-image` 대신 `autoPlay muted loop playsInline` `<video>` 엘리먼트 + 어두운 오버레이 div를 렌더링. `position: absolute`인 video/overlay는 기본적으로 정적 배치된 텍스트보다 위에 그려지는 CSS 스태킹 규칙 때문에, 텍스트(`eyebrow`・`title`・`cta`)에 `position: relative; z-index: 2`를 명시로 추가해 영상에 안 가리도록 처리. 전환 속도(8초)는 이전 gif 적용 때 이미 `duration` 필드로 처리해둬서 그대로 유지(우연히 mp4도 동일하게 8.0초).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 메인 1번 슬라이드가 영상(720p, 이전보다 선명한지)으로 재생되는지, 문구가 상단에 위치하는지, 텍스트가 영상에 가리지 않는지 직접 확인 필요. 아직 커밋・푸시하지 않음.

## 히어로 4번 슬라이드도 영상으로 교체 + 문구 변경 (2026-08-11, 같은 날 후속 요청)

1. **영상 적용 — 완료**: `reference/메인비주얼04(비디오).mp4`(1280×720, 8.0초, 5.7MB — 1번과 동일한 스펙)를 `public/videos/hero-slide-4.mp4`로 저장, `data/heroSlides.js` 4번 슬라이드에 `video`・`duration: 8000` 추가. `HeroSlider.jsx`의 영상 렌더링 로직을 1번 슬라이드 전용이 아니라 `slide.video` 유무로 범용 분기해뒀던 덕분에 컴포넌트 코드 수정 없이 데이터만 추가해서 바로 적용됨.
2. **문구 교체 — 완료**: "AI로 더 큰 결과물을 만든다"・"무궁한 가능성" 키워드로 3안(A: 혼자선 못 만들 결과물・B: 스킬 강조・C: 가능성 강조 직설형)을 제시해 A안 채택. eyebrow `AI 툴 실무 활용` → `AI로 넓어지는 가능성`, title `AI 프로토타이핑으로\n포트폴리오를 완성합니다.` → `혼자서는 못 만들 결과물을,\nAI와 함께라면 만들 수 있습니다.`로 변경(CTA・링크는 기존 그대로 유지 — "수료생 포트폴리오 보기" → `/portfolio`).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 4번 슬라이드 영상 재생과 새 문구 확인 필요. 아직 커밋・푸시하지 않음.

## 히어로 2번 슬라이드도 영상으로 교체 (2026-08-11, 같은 날 후속 요청)

`reference/메인비주얼02(비디오).mp4`(1280×720, 8.0초, 1.9MB — 1・4번과 동일한 스펙)를 `public/videos/hero-slide-2.mp4`로 저장, `data/heroSlides.js` 2번 슬라이드에 `video`・`duration: 8000` 추가. 컴포넌트가 이미 범용이라 코드 수정 없이 데이터만 추가. 문구는 이번엔 교체 요청이 없어 기존 그대로 유지("국민취업지원제도로\nAI UI/UX 디자인 과정을 시작하세요.").
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 2번 슬라이드 영상 재생 확인 필요. 이제 5개 슬라이드 중 1・2・4번이 영상, 3・5번만 정지 이미지. 아직 커밋・푸시하지 않음.

## 방금 그 영상은 3번 슬라이드용이었음 — 2번에서 빼서 3번으로 재배치 + 문구 변경 (2026-08-11, 같은 날 후속 요청)

직전에 2번 슬라이드에 넣은 영상이 사실 3번 슬라이드용이었다는 정정. 파일을 옮기고 2번은 원래 이미지 상태로 되돌림.

1. **영상 재배치 — 완료**: `public/videos/hero-slide-2.mp4` → `hero-slide-3.mp4`로 이름 변경(파일 이동, 재복사 아님). `data/heroSlides.js`에서 2번 슬라이드의 `video`・`duration` 필드 제거(이미지로 복귀), 3번 슬라이드에 동일하게 추가.
2. **3번 슬라이드 문구 교체 — 완료**: "UX・UI 디자인・사용자 경험・편의성" 키워드로 3안(A: 사용자 경험까지 고려・B: 편의성 중심・C: 키워드 나열형) 제시, A안 채택. eyebrow `현업 멘토가 1:1로` → `사용자를 이해하는 디자인`, title `막히는 지점마다\n현업 디자이너가 직접 봐드립니다.` → `사용자 경험(UX)까지 고려한\nUI 디자인을 배웁니다.`로 변경(멘토링 주제에서 UX/UI 설계 역량 주제로 전환됨 — CTA・링크는 요청받지 않아 기존 그대로 유지: "강사진 소개 보기" → `/instructors`, 주제와는 다소 느슨하게 연결되지만 학원 소개 사이트 맥락에서는 무리 없음).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 2번이 이미지로 복귀했는지, 3번이 영상+새 문구로 나오는지 확인 필요. 아직 커밋・푸시하지 않음.

## 2번 슬라이드 영상 적용 + 5번 슬라이드 삭제 + 인디케이터를 클릭 가능한 점(dot)으로 교체 (2026-08-11, 같은 날 후속 요청)

1. **2번 슬라이드 영상 적용 — 완료**: `reference/메인비주얼02(비디오).mp4`(진짜 2번용, 1280×720, 8.0초, 1.5MB)를 `public/videos/hero-slide-2.mp4`로 저장, `data/heroSlides.js` 2번에 `video`・`duration: 8000` 추가.
2. **5번 슬라이드 삭제 — 완료**: `heroSlides.js` 배열에서 5번 항목(수료 후 취업・`/reviews`) 전체 제거, 더 이상 쓰이지 않는 `public/images/hero/slide-5.jpg` 삭제. `heroSlides`를 참조하는 곳이 `HeroSlider.jsx` 하나뿐인 걸 확인해 다른 곳에 영향 없음. 이제 슬라이드 4개(1・2・3・4)이며 1・2・3・4 전부 영상.
3. **"1 / 5" 텍스트 카운터 → 클릭 가능한 점(dot) 인디케이터로 교체 — 완료**: 사용자가 참고 이미지로 보여준 "활성 슬라이드는 넓은 pill, 나머지는 작은 원" 스타일을 적용. `HeroSlider.jsx`에 `hero-slider__dots`(각 점은 `goTo(i)`를 호출하는 버튼, `role="tablist"`/`role="tab"`/`aria-selected`로 접근성 처리) 추가, 기존 `hero-slider__count` span 제거. 위치는 기존과 동일하게 우측 하단 컨트롤 바(재생/정지 버튼 옆) 유지 — 참고 이미지처럼 이미지 아래 별도 흰색 바로 분리하지는 않음(요청받지 않아 기존 레이아웃 유지, 필요하면 조정 가능).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 2번 슬라이드 영상, 슬라이드가 4개로 줄었는지, 점 인디케이터를 클릭해서 슬라이드 이동이 되는지, 현재 슬라이드 점이 넓은 pill로 표시되는지 확인 필요. 아직 커밋・푸시하지 않음.

## 영상 위아래 잘림 수정 — object-fit: cover → contain (2026-08-11, 같은 날 후속 요청)

영상이 너무 크게 확대돼서 위아래가 잘린다는 피드백. 원인은 영상 원본 비율이 16:9(1280×720)인데, 배너 컨테이너는 그보다 훨씬 넓은 비율(가로 최대 1440px, 세로 500px)이라 `object-fit: cover`가 컨테이너 폭에 맞춰 영상을 과도하게 확대하면서 위아래를 크게 잘라내고 있었음.
- `HeroSlider.css`의 `.hero-slider__video`를 `object-fit: cover` → `contain`으로 변경(영상 전체 프레임이 잘리지 않고 다 보이도록) + `background: #1c2321`(브랜드 톤 다크 그린) 추가 — contain을 쓰면 영상 좌우로 남는 여백(letterbox)이 생기는데, 그 여백을 흰 배경 대신 다른 슬라이드들과 톤이 맞는 어두운 색으로 채움.
- `npm run build` 통과. dev 서버(`localhost:5173`)에서 영상 슬라이드(1・2・3・4번) 전체가 잘리지 않고 다 보이는지, 좌우 여백 색이 자연스러운지 확인 필요. 아직 커밋・푸시하지 않음.

## 영상 밝기 보정 + 슬라이드 전환 시 영상 처음부터 재생 + 자동 전환을 영상 종료 시점 기준으로 변경 (2026-08-11, 같은 날 후속 요청)

1. **영상 밝기 보정 — 완료**: 영상이 전체적으로 어둡다는 피드백. ffmpeg 없이 픽셀 단위로 원본을 재인코딩하기보다, `HeroSlider.css`의 `.hero-slider__video`에 `filter: brightness(1.2)`를 추가해 브라우저 렌더링 단계에서 밝기만 보정(원본 파일은 그대로 두고 화면에 표시될 때만 밝게). 너무 밝거나 계속 어둡게 느껴지면 배율(현재 1.2)을 조정하면 됨.
2. **슬라이드 전환 시 영상이 항상 처음부터 재생 — 완료**: 기존엔 5개(현재 4개) 영상이 전부 `autoPlay loop`로 마운트되자마자 백그라운드에서 계속 재생되고 있어서, 화살표/점 클릭으로 다시 돌아온 슬라이드는 영상이 중간부터 보였음. `videoRefs`(각 슬라이드 `<video>`에 대한 ref 배열)를 추가하고, `index`가 바뀔 때마다 새로 활성화된 슬라이드의 영상만 `currentTime = 0`으로 되돌려 재생하고 나머지는 `pause()`하는 effect를 추가. `<video>`의 `autoPlay`・`loop` 속성은 제거하고 재생 제어를 전부 이 effect로 일원화. 정지(❚❚) 버튼을 누르면 영상도 같이 멈추도록 함.
3. **자동 전환 기준을 duration(ms) → 영상 종료 시점(ended)으로 변경 — 완료**: 지금까지는 영상 길이를 `duration` 필드에 수동으로 맞춰 넣어야 했는데(영상이 바뀔 때마다 다시 재보고 값 수정 필요), 이제 영상이 있는 슬라이드는 `<video>`의 `ended` 이벤트가 발생하는 시점에 자동으로 다음 슬라이드로 넘어가도록 변경(영상이 없는 슬라이드만 기존처럼 `duration` 타이머 사용, 기본값 5000ms). `heroSlides.js`의 `duration: 8000`은 4개 슬라이드 전부 영상이라 더 이상 쓰이지 않아 전부 제거하고 주석도 갱신.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 영상이 이전보다 밝게 보이는지, 화살표・점으로 슬라이드를 오갈 때 영상이 항상 처음부터 시작되는지, 정지 버튼이 영상도 같이 멈추는지, 영상이 끝나는 시점에 정확히 다음 슬라이드로 넘어가는지 직접 확인 필요. 아직 커밋・푸시하지 않음.

## 헤더 배경 살짝 색 입힘 + 플로팅 상담 버튼 말풍선 힌트 + 테마 색상 의견 (2026-08-11, 같은 날 후속 요청)

1. **상단 헤더 배경 — 완료**: 순백(`var(--color-card)`, `#ffffff`)이라 페이지 배경(`--color-bg`, `#f7f8f6`)과 거의 구분이 안 되던 문제. `Header.css`의 `.site-header` 배경을 브랜드 그린을 옅게 섞은 `#eef5f1`로 변경.
2. **우측 하단 플로팅 상담 버튼에 말풍선 힌트 추가 — 완료**: 버튼이 눈에 잘 안 띈다는 피드백으로, "상담하기" 말풍선이 8초 주기로 한 번씩(약 2초간) 나타났다 사라지도록 CSS `@keyframes` 애니메이션 추가(JS 상태 없이 순수 CSS로 무한 반복 — 늘 떠있으면 거슬리니 간헐적으로만 노출). 버튼 왼쪽에 꼬리 달린 흰색 말풍선으로 표시, 메뉴가 열려있을 때는 `.floating-consult[open]` 선택자로 숨김.
3. **테마 색상 2가지로 바꾸는 것에 대한 의견 — 제시만 함(구현 안 함)**: 완전히 새 색상을 추가하기보다, 이미 사이트 곳곳(플로팅 버튼, 헤더 드롭다운 상단 바)에 포인트로 쓰이고 있는 골드/앰버(`#f5b83d`)를 정식 보조 색상으로 승격시켜 "딥그린 + 골드" 2색 조합으로 다듬는 방향을 추천함 — 이미 검증된 색이라 리스크・비용이 적음. 완전 새 색(블루/틸 등)은 더 신선하지만 그린 톤이 이미 배지・버튼・그라디언트 전반에 쓰이고 있어 손볼 범위가 훨씬 큼. 사용자 결정 대기 중.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 헤더 배경 톤, 플로팅 버튼의 말풍선이 주기적으로 나타나는지 확인 필요. 아직 커밋・푸시하지 않음.

## 테마 색상 "딥그린 + 골드" 2색 적용 (2026-08-11, 같은 날 후속 요청)

직전에 제시한 의견(완전 새 색보다 기존 골드 포인트를 정식 보조색으로 승격)대로 진행해달라는 요청.

1. **`--color-accent`・`--color-accent-dark` 토큰 추가 — 완료**: `index.css`에 골드(`#f5b83d`・`#c17f16`) 공식 토큰 추가 + 재사용 가능한 `.btn--accent` 유틸리티 클래스 추가(플로팅 상담 버튼에 이미 쓰이던 것과 동일한 그라디언트 스타일, 텍스트는 골드 배경에 대비가 좋은 다크 컬러).
2. **적용 범위는 "마케팅성 CTA"로 한정 — 완료**: 지원・로그인・제출 같은 기능적 버튼은 그대로 그린 유지(사이트 전체의 기본 액션 색은 안 건드림), "여기를 눌러주세요"형 강조 버튼 2곳만 골드로 교체:
   - `BottomCta`(페이지 하단 "지금 상담 신청하기" 배너) — 기존엔 그린 배경 위에 흰 버튼이었는데, `btn--primary` → `btn--accent`로 교체해 흰색 대신 실제 색이 있는 골드 버튼으로.
   - `HeroSlider`의 각 슬라이드 CTA 버튼 — 기존 반투명 유리 버튼에서 골드 채움 버튼으로 교체, 배너에서 가장 눈에 띄는 자리라 duotone 효과가 가장 잘 드러남.
3. **나머지는 그대로 — 의도적으로 손 안 댐**: 배지・폼 버튼・관리자 화면 등 그린을 쓰는 나머지 UI는 전부 유지. 리스크를 낮추기 위해 이번엔 딱 2곳만 우선 적용, 반응 보고 확대할지 결정하면 됨.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 메인 히어로 슬라이더 CTA 버튼과 페이지 하단 "지금 상담 신청하기" 버튼이 골드로 바뀌었는지, 다른 페이지의 초록 버튼들은 그대로인지 확인 필요. 아직 커밋・푸시하지 않음.

## 오늘(2026-08-11) 진행 내역 커밋・푸시・배포

- GitHub: 커밋 `aacf2cf`(`feat: DB 연결 3단계(상담) + 히어로 영상 슬라이더 + UI 개선`) → `main` 브랜치 푸시 완료. `.claude/settings.json`(로컬 권한 설정)은 개인 로컬 도구 설정이라 제외.
- Vercel: `vercel` CLI가 PATH에 없어 `npx vercel deploy --prod`로 실행 — 첫 시도는 `Not authorized`, 두 번째는 24.8MB 업로드 후 `fetch failed`로 실패(과거에도 반복된 일시적 네트워크 오류 패턴), 세 번째 재시도에서 성공. https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).
- 배포 대상: DB 연결 3단계(상담/비회원 콜백 Supabase 전환 + 마이그레이션 2개 + 시드 스크립트), 히어로 슬라이더 영상화(1~4번) + 점 인디케이터 + 상단 정렬 + 밝기 보정 + 재생 로직 개선, 헤더 배경 톤, 플로팅 상담 버튼 말풍선, 딥그린+골드 2색 테마(히어로 CTA・하단 상담 배너).

## 헤더 메뉴 중앙 정렬 + 홈 팝업 이미지 제거 + 실적 배너 카운트업 반복 재생 (2026-08-11, 같은 날 후속 요청)

1. **메인 메뉴 중앙 정렬 — 완료**: `Header.css`의 `.site-header__nav`(로고와 로그인/마이페이지 사이 `flex:1` 영역)에 `justify-content: center` 추가해, 메뉴가 로고 옆에 붙어 왼쪽으로 쏠려 있던 것을 가운데로 이동.
2. **홈 진입 팝업(`HomePromoModal`)에서 이미지 제거 — 완료**: 상단 사진 영역(`home-promo-modal__media` — 이미지/그라디언트 fallback + "국비지원 · N기 모집중" 뱃지)을 통째로 제거, 제목・요약・마감 카운트다운・CTA만 남은 텍스트 중심 팝업으로. 더 이상 쓰이지 않는 관련 CSS(`.home-promo-modal__media`・`__media img`・`__media-fallback`・`__tag`)도 함께 정리.
3. **실적 배너("60+ 누적 수료생" 등) 카운트업 애니메이션이 팝업에 가려 안 보이던 문제 — 완료**: 기존엔 배너가 처음 화면에 들어올 때 딱 한 번만 애니메이션이 재생되고 끝났는데, 하필 그 타이밍에 홈 팝업이 화면을 덮고 있어서 애니메이션 과정을 볼 방법이 없었음. `AchievementBanner.jsx`를 "한 번만 재생"에서 "화면에 보이는 동안 6초마다 반복 재생"으로 변경 — `IntersectionObserver`는 계속 보이는 상태를 추적하도록 유지하고(연결 해제 안 함), 보이는 동안 6초 주기 타이머로 `cycle` 값을 증가시켜 `CountUpValue`에 `key={cycle}`을 줘서 매번 강제로 리마운트・재생되게 함(화면 밖으로 나가면 타이머 정지).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 헤더 메뉴가 중앙에 있는지, 홈 진입 팝업에 이미지가 안 뜨는지, 실적 배너 숫자가 몇 초마다 한 번씩 0부터 다시 올라가는 애니메이션을 반복하는지 확인 필요. 아직 커밋・푸시하지 않음.

## 상담 버튼 말풍선 위치・호버 개선 + 국비지원 공식 링크 추가 + 홈 화면 "뜬금없는 지원 버튼" 재배치 (2026-08-11, 같은 날 후속 요청)

1. **플로팅 상담 버튼 말풍선 위치/동작 개선 — 완료**: 버튼 왼쪽에 나란히 뜨던 것을 `bottom`・`right` 오프셋으로 버튼 왼쪽 대각선 위로 옮기고, 말풍선 꼬리도 오른쪽을 가리키던 것에서 아래(버튼 방향)를 가리키도록 변경. `.floating-consult:hover .floating-consult__bubble { animation: none; opacity: 1; }` 추가 — 마우스를 올리고 있는 동안은 8초 주기 애니메이션과 무관하게 계속 떠 있음(단순히 `animation-play-state: paused`만 쓰면 마침 사라지는 타이밍에 멈춰버릴 수 있어서, `animation: none`으로 완전히 꺼서 opacity:1이 확실히 적용되게 함).
2. **국비지원 안내 페이지에 공식 정부 사이트 링크 추가 — 완료**: "과정 지원하기" 버튼만 있고 국비지원 제도 자체에 대한 외부 공식 안내가 없다는 피드백. WebFetch로 실제 사이트인지 먼저 확인(고용노동부・한국고용정보원이 운영하는 "고용24", `work24.go.kr` — 국민취업지원제도 안내 섹션 실재 확인) 후, `Funding.jsx` 하단 안내 문단에 `https://www.work24.go.kr` 외부 링크(`target="_blank"`) 추가.
3. **홈 화면에서 맥락 없이 떠 있던 "지원하기" 버튼 + 마감 카운트다운을 국비지원 배너로 이동 — 완료**: `Home.jsx`에서 `AchievementBanner`와 `JourneyGraph` 사이에 아무 설명 없이 버튼+카운트다운만 놓여있던 `home__capacity` 블록을 제거하고, 같은 페이지 상단의 `FundingBanner`(국민취업지원제도로 수강료 최대 100% 지원 안내 배너)로 이동 — 이제 "이 배너가 무엇에 대한 지원인지" 맥락이 있는 곳에 붙어서 자연스러움. `FundingBanner`가 `course` prop을 받아 마감 카운트다운을 표시하도록 수정, "지원하기" 버튼은 오늘 만든 골드 강조 버튼(`btn--accent`)으로(마케팅성 CTA라 방금 도입한 2색 테마 규칙과도 맞음), 기존 "국비지원 조회하기" 링크는 그대로 유지해 배너 안에 카운트다운・지원하기(과정 신청)・국비지원 조회하기(안내 페이지) 세 요소가 나란히. `Home.css`의 이제 안 쓰는 `.home__capacity` 규칙도 제거.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 플로팅 버튼에 마우스를 올렸을 때 말풍선이 계속 떠 있는지(대각선 위 위치도 확인), `/funding` 페이지 하단의 고용24 링크, 홈 화면 상단 국비지원 배너에 카운트다운・지원하기・국비지원 조회하기가 자연스럽게 붙어있는지 확인 필요. 아직 커밋・푸시하지 않음.

## 여정 그래프 순서 변경 + 모바일 정렬/스와이프/겹침 수정 (2026-08-11, 같은 날 후속 요청)

로컬 서버가 갑자기 안 켜진다는 신고 — 확인해보니 백그라운드 dev 서버 프로세스가 어느 시점에 종료돼 있었음(원인 특정은 안 됨, 재현되면 알려달라고 안내). `npm run dev` 재실행으로 `localhost:5173` 정상화.

1. **홈 화면 "여정 그래프"(JourneyGraph) 순서 변경 — 완료**: "멘토 매칭이 제일 먼저고, 상담・과정 선택을 멘토가 도와주는 흐름이 맞다"는 피드백. `journeySteps.js`의 STEP 1・2 내용을 맞바꿈(곡선 위 좌표・above/below 배치는 그대로 두고 텍스트만 교체) — 1단계가 "멘토 매칭"(상담 신청 시 담당 멘토 먼저 배정, 과정 선택도 멘토와 상담), 2단계가 "과정 지원"(멘토와 상담한 내용을 바탕으로 온라인 지원)으로.
2. **모바일에서 이 여정 그래프가 텍스트만 나열되던 문제 — 완료**: 기존엔 768px 이하에서 SVG 곡선을 통째로 숨기기만 해서(`display:none`) 4단계가 곡선・점 없이 텍스트만 세로로 나열됐음. `JourneyGraph.css` 모바일 미디어쿼리에 각 단계 앞에 점(`::before`)과, 점들을 잇는 세로선(`::after`, 마지막 단계 제외)을 CSS만으로 추가해 데스크톱의 "단계가 이어진다"는 느낌을 세로 타임라인으로 재현. 이 부분만 텍스트 정렬을 좌측으로 바꿈(타임라인 UI 특성상 선・점이 왼쪽에 오고 텍스트가 오른쪽에 붙는 게 정상적인 형태라, 3번 항목의 "가운데 정렬" 원칙과는 별개로 의도적으로 유지).
3. **모바일 전반 왼쪽 정렬 — `FundingBanner`(홈 상단 국비지원 배너) 확인 후 수정**: 모바일 미디어�쿼리(`flex-direction:column` 전환 시) `align-items: flex-start`로 아이콘・문구・카운트다운・버튼이 전부 왼쪽으로 쏠려 있던 걸 발견해 `align-items: center; text-align: center`로 수정. `Login`・`ConsultationRequest` 등 폼 페이지들은 이미 중앙 정렬돼 있는 것 확인. **다만 사이트 전체 페이지를 모바일 화면으로 직접 훑어볼 방법이 없어(브라우저 자동화 도구 없음), 코드상 왼쪽 정렬 흔적이 뚜렷했던 이 배너 위주로 수정함 — 다른 페이지에서도 왼쪽 쏠림이 보이면 어느 페이지인지 알려주시면 바로 확인하겠습니다.**
4. **모바일 히어로 슬라이더 버튼이 영상을 가리던 문제 — 완료**: 좁은 화면에서는 16:9 영상이 `object-fit: contain`으로 세로 폭이 훨씬 작아져 화면 중앙에 떠 있는데, 오늘 앞서 상단으로 옮긴 문구・버튼이 하필 그 영역과 겹쳐서 버튼이 영상을 가리고 있었음. `HeroSlider.css` 모바일 미디어쿼리에서만 `justify-content: flex-end`로 바꿔 문구・버튼을 화면 하단(영상이 끝난 여백 쪽)으로 내림 — 데스크톱은 기존 상단 정렬 그대로 유지.
5. **모바일에서 스와이프로 슬라이드 전환 — 완료**: 기존엔 작은 화살표 버튼만으로 이동 가능했음. `HeroSlider.jsx`에 `onTouchStart`/`onTouchEnd`로 좌우 스와이프를 감지해(40px 이상 이동 시) 이전/다음 슬라이드로 넘어가도록 추가.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. 모바일 폭(≤640px)에서 직접 확인 필요: 여정 그래프 순서・세로선, 국비지원 배너 중앙 정렬, 히어로 슬라이더 버튼이 영상과 안 겹치는지, 좌우 스와이프로 슬라이드가 넘어가는지. 아직 커밋・푸시하지 않음.

## 학원비(수강료) 구체적 안내 추가 (2026-08-11, 같은 날 후속 요청)

"학원비 정보를 어디서도 찾아볼 수 없다"는 지적에 동의 의견을 드린 뒤, 실제 반영.
1. **국비 과정 — 총 교육비 명시**: `courseDetail.js`의 모집요강 "수강료" 항목과 `fundingInfo.js`의 `/funding` 페이지 수강료 안내 둘 다에 **"총 교육비 6,800,000원(20주 과정 기준)"**을 추가(기존엔 "국비지원 대상자 100%"처럼 상대적 설명만 있고 구체적 총액이 어디에도 없었음). 국비지원으로 실제 자부담금은 별도(개인별 한도에 따라 상이)라는 기존 설명은 그대로 유지.
2. **다른 과정 3개 — 신규 수강료 추가**: 지금까지 전혀 비용 언급이 없었던 UX 리서치 심화(360만원)・프로덕트 디자인 부트캠프(420만원)・디자인 시스템 마스터(340만원)에 각 과정 기간에 비례한 placeholder 총 교육비를 `courses.js`에 추가, `/course` 탭의 "일정" 섹션을 "일정 및 수강료"로 확장해 표시.
- 전부 실제 가격 확정 전까지 쓰는 placeholder라는 점을 코드 주석에 명시(사이트의 다른 수치들과 동일한 관례).
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과.
- 커밋 `4d899cd` -> GitHub main 푸시 완료(헤더/홈 UI 개선, 여정 그래프 개편, 학원비 안내 전부 포함 - 이전 세션의 미커밋 작업과 이번 학원비 추가분을 한 번에 커밋). Vercel 배포는 첫 시도 fetch failed(일시적 네트워크 오류)로 실패, 재시도에서 성공 - https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).

## 국비지원 배너 "지원하기" 버튼 제거 (2026-08-11, 같은 날 후속 요청)

**참고**: 이 시점에 다른 세션이 동시에 같은 프로젝트에서 작업 중이었던 것으로 확인됨(위 "학원비 안내" 항목이 커밋 `4d899cd`/`257e5ac`로 이미 푸시・배포됨 — 이 세션이 만들어둔 미커밋 변경사항까지 함께 커밋한 것으로 로그에 명시돼 있음). `git fetch`로 확인한 결과 로컬 `main`과 `origin/main`이 정확히 일치해 충돌은 없었음.

방금 홈 상단 국비지원 배너에 추가했던 "지원하기" 버튼(카운트다운 옆 골드 버튼)을 없애달라는 요청 — `FundingBanner.jsx`에서 해당 `<Link className="btn btn--accent">` 제거, 카운트다운과 "국비지원 조회하기" 링크만 남김. 이제 안 쓰는 `.funding-banner__apply` CSS도 정리.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. 아직 커밋・푸시하지 않음.

## 모바일 여정 그래프: 정적 타임라인 → 스크롤 연동 진행 표시로 재작업 (2026-08-11, 같은 날 후속 요청)

직전 수정(세로선+점)이 그냥 고정된 장식용 선이라, "한 줄로 쭉 이어져서 각 단계별로 멈추는" 데스크톱 호버 진행 표시 기능 자체가 빠졌다는 지적. 데스크톱은 마우스를 얹은 단계까지 곡선이 채워지는 상호작용인데, 모바일엔 호버가 없으니 그 기능이 아예 빠진 장식으로만 남아있었음.

1. **스크롤 위치로 "도달한 단계"를 추적 — 완료**: `JourneyGraph.jsx`에 `mobileReachedStep` state 추가, 각 단계 DOM에 `IntersectionObserver`를 달아(`threshold: 0.5`) 스크롤하다 그 단계가 화면 절반 이상 들어오면 도달한 것으로 기록(`Math.max`로 누적 — 다시 위로 스크롤해도 이미 지나온 단계 표시는 유지). 기존 데스크톱 호버 로직(`hoveredStep`)은 완전히 별개 state라 서로 간섭 없음.
2. **선・점이 도달한 단계까지 순서대로 채워지도록 — 완료**: `JourneyGraph.css` 모바일 블록에서 점(`::before`)과 연결선(`::after`)의 기본색을 옅은 회색으로 바꾸고, `journey-graph__step--reached` 클래스(도달한 단계에 붙음)가 있을 때만 골드로 바뀌게 함. 색 전환에 `transition`을 줘서 스크롤하며 하나씩 "멈춰서 채워지는" 느낌이 나도록 함 — 이제 데스크톱의 호버 진행 표시와 같은 역할을 모바일에서는 스크롤이 대신함.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. 모바일 폭에서 페이지를 아래로 스크롤하며 여정 그래프 점・선이 단계마다 순서대로 골드로 채워지는지 확인 필요. 아직 커밋・푸시하지 않음.

## 모바일 히어로 영상 상단 여백 제거 + 학원비 의견 + 지원 마감일 5일 남게 조정 (2026-08-11, 같은 날 후속 요청)

1. **모바일 히어로 영상 위쪽 검은 여백 제거 — 완료**: `object-fit: contain`이 기본적으로 위아래 여백을 반씩 남기는데, 위쪽 여백은 아무 용도가 없어서 굳이 비워둘 이유가 없다는 피드백. `HeroSlider.css` 모바일 미디어쿼리에 `.hero-slider__video { object-position: center top }` 추가해 영상을 위로 붙이고, 남는 여백은 전부 아래쪽(문구・버튼 자리)으로 몰아줌.
2. **총 교육비 680만원이 너무 비싼 게 아니냐는 질문 — 의견만 제시(코드 변경 안 함)**: 이 금액은 오늘 다른 세션이 만든 placeholder 수치(실제 가격 확정 전)임을 먼저 밝힘. 실제 국비지원(K-디지털 트레이닝 등) 5~6개월짜리 풀타임 과정들도 총 교육비가 보통 500~900만원대로 책정되는 경우가 많아서, 20주(~5개월) 과정에 680만원은 비현실적으로 높은 수준은 아니라고 판단됨 — 다만 실제 숫자를 낮추고 싶다면 언제든 조정 가능하다고 안내.
3. **지원 마감 카운트다운은 있는데 실제 마감 날짜가 안 보이던 문제 — 완료**: `DeadlineCountdown.jsx`가 지금까지 "지원 마감까지 D-4 07:30:23"처럼 상대적인 D-day・시간만 보여주고 실제 달력 날짜(몇 월 며칠 마감인지)는 어디에도 안 나와 있었음. `formatDeadlineDate()`를 추가해 "2026.08.18 마감 · D-5 07:30:23"처럼 절대 날짜를 항상 함께 표시하도록 변경 — 이 컴포넌트를 쓰는 홈 배너・과정 상세 페이지・홈 팝업 세 곳 전부에 한 번에 적용됨(컴포넌트 하나만 고치면 됨).
4. **"13일 제출 기준 지원 5일 남음"으로 마감일 재조정 — 완료**: `courses.js`의 `applicationDeadline`을 `2026-08-15` → `2026-08-18`로 변경(8/13 기준 D-5가 되도록). 연결된 데이터 확인 결과: `schedule.js`의 "4기 모집" 일정은 `applicationStart`/`applicationDeadline`을 그대로 참조해서 자동으로 같이 이동함(직접 고칠 필요 없음), 다음 일정인 "4기 발대식(OT)"는 기존 8/24 그대로라 새 마감일과 6일 간격으로 여전히 자연스러움(모집 마감 후 개강까지 준비 기간). 다른 곳에 8/15를 하드코딩한 날짜 텍스트가 있는지 전체 검색했지만 없었음.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. dev 서버(`localhost:5173`)에서 모바일 폭으로 히어로 영상 위쪽 여백이 없어졌는지, 홈 배너・과정 상세・홈 팝업의 카운트다운에 실제 날짜(2026.08.18)가 함께 뜨는지, 오늘(8/11) 기준 D-7로 표시되는지(8/13엔 D-5) 확인 필요. 아직 커밋・푸시하지 않음.

## 모바일 메뉴를 아이콘 중심 드로어로 전면 개편 (2026-08-11, 같은 날 후속 요청)

"지금 모바일은 PC 버전 비율만 줄인 수준"이라는 지적 — 지금까지 모바일 헤더는 PC 메뉴(드롭다운 3개 + 마이페이지 + 로그인)를 그대로 여러 줄로 흘려보내는 방식이었음. 아이콘을 적극 활용한 진짜 모바일용 메뉴로 새로 설계.

1. **인라인 SVG 아이콘 세트 신규 작성 — 완료**: 외부 아이콘 라이브러리(react-icons 등) 추가 없이, `NavIcons.jsx`에 `currentColor` 기반 단순 라인 아이콘 7개(햄버거・닫기・사용자・책(교육과정)・말풍선(소통공간)・건물(학원안내)・로그아웃) 직접 작성 — CSS로 크기・색을 그대로 제어할 수 있고, 기존 프로젝트 관례(PNG 아이콘)와 달리 벡터라 어떤 크기에서도 선명함.
2. **햄버거 버튼 + 오른쪽 슬라이드 드로어 메뉴 — 완료**: `Header.jsx`에 `mobileOpen` state 추가. 모바일(≤640px)에서는 기존 PC 내비게이션 전체(`.site-header__nav`・`.site-header__mypage`・`.site-header__account`・`.site-header__login`)를 숨기고, 로고 + 햄버거 아이콘만 남긴 심플한 상단바로 교체. 햄버거를 누르면 오른쪽에서 드로어가 슬라이드 인(`transform: translateX`) — 백드롭 클릭으로도 닫힘, 열려있는 동안 배경 스크롤 방지(`document.body.style.overflow`).
3. **드로어 구성 — 완료**: 상단에 로고+닫기 버튼, 그 아래 "OO님 · 마이페이지"(또는 "로그인 · 마이페이지") 카드형 링크(사용자 아이콘), 이어서 교육과정/소통공간/학원안내 3개 그룹을 각각 아이콘+제목으로 구분하고 하위 링크는 왼쪽 세로선으로 묶어 들여쓰기(그룹 전체를 아코디언으로 접지 않고 펼쳐서 보여주는 방식 — 항목이 18개라도 스크롤 가능한 드로어 안에서는 한눈에 훑기 편함), 로그인 상태면 멘토/관리자 전용 링크와 로그아웃 버튼(로그아웃 아이콘), 비로그인이면 로그인 버튼을 맨 아래 고정 배치.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. 모바일 폭(≤640px)에서 직접 확인 필요: 헤더가 로고+햄버거만 남았는지, 햄버거를 누르면 드로어가 오른쪽에서 슬라이드 인 되는지, 백드롭 클릭・닫기 버튼・메뉴 항목 클릭 시 모두 드로어가 닫히는지, 로그인/비로그인 상태에 따라 하단 버튼이 바뀌는지. 아직 커밋・푸시하지 않음.

## 국비지원 배너 카운트다운 제거 + 히어로 영상 어두움 수정 + 커밋・푸시・배포 (2026-08-11, 같은 날 후속 요청)

1. **국비지원 배너의 마감 카운트다운 제거 — 완료**: 스크린샷으로 지목된 "2026.08.18 마감 · D-7 ..." 부분을 `FundingBanner.jsx`에서 제거, "국비지원 조회하기" 버튼만 남김(원래 형태로 복귀에 가까움). 더 이상 안 쓰는 `course` prop・`DeadlineCountdown` import 정리, `Home.jsx`의 `<FundingBanner course={course} />`도 `<FundingBanner />`로 되돌림, `.funding-banner__actions` 관련 CSS도 정리.
2. **히어로 영상들이 전부 "검은 필터 씌운 것처럼" 어두워 보이던 문제 — 완료**: 원인은 영상 전체에 균일하게 45% 어두운 그라디언트(`.hero-slider__video-overlay`)를 씌우고 있었던 것 — 문구 가독성을 위한 조치였지만 영상 전체를 뒤덮어서 과했음. 문구가 있는 쪽에만 어둡게 지도록 방향성 그라디언트로 교체(위쪽만 어둡고 아래로 갈수록 투명 — 데스크톱은 문구가 상단에 있으므로 그대로, 모바일은 문구가 하단에 있어 모바일 미디어쿼리에서 방향을 반대로 뒤집음), 덕분에 화면 대부분(영상 중앙~하단/상단)은 원본 밝기 그대로 밝게 보임. 밝기 보정 필터도 1.2 → 1.3으로 소폭 상향.
- `npm run lint` + `npm run build` 통과.
- 커밋 `20aecc8`(`feat: 모바일 메뉴 아이콘 드로어 개편 + 히어로 영상/여정 그래프 개선` — 오늘 이 세션에서 진행한 모바일 메뉴・여정 그래프・히어로 영상・지원 마감일・국비지원 배너 정리까지 전부 포함) → GitHub `main` 푸시 완료. `vercel deploy --prod`는 이번엔 재시도 없이 첫 시도에 바로 성공 — https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).

## 국비지원 배너 모바일 배치 정리 (2026-08-11, 같은 날 후속 요청)

배포 직후 스크린샷 — 모바일에서 아이콘이 왼쪽 위에 따로 떠 있고 문구는 3줄로 꺾이면서 전체적으로 안 어울려 보인다는 피드백. 원인은 아이콘+문구를 가로 한 줄(`.funding-banner__content`)로 두고 `text-align: center`만 준 것 — 아이콘은 행의 왼쪽 끝에 고정되고 문구만 자기 영역 안에서 센터 정렬되다 보니, 짧은 아이콘과 3줄짜리 문단이 나란히 있어 비대칭으로 보였음.
- `FundingBanner.css` 모바일 미디어쿼리에서 `.funding-banner__content`를 `flex-direction: column`으로 바꿔 아이콘을 문구 위로 올리고 전부 세로로 쌓아 가운데 정렬(아이콘도 2.25rem → 2.5rem로 살짝 키워서 독립된 배지처럼 보이게). 이제 아이콘 → 문구 → 버튼이 위에서 아래로 깔끔하게 쌓이는 카드형 레이아웃.
- `npm run build` 통과. 모바일 폭에서 국비지원 배너가 아이콘・문구・버튼이 전부 세로 중앙 정렬로 깔끔하게 보이는지 확인 필요. 아직 커밋・푸시하지 않음.

## 모바일 히어로 슬라이더: 영상-문구 사이 빈 공간 제거 + 화살표 골드로 재배치 (2026-08-11, 같은 날 후속 요청)

스크린샷으로 지목된, 영상과 문구 사이에 아무 역할 없이 크게 뜬 검은 여백 문제. 원인은 지금까지 모바일도 데스크톱과 같은 구조(영상을 `position: absolute`로 슬라이드 전체에 배경처럼 깔고 문구를 그 위에 겹쳐 올리는 방식)를 그대로 쓰면서, `min-height: 500px` 고정값과 `object-fit: contain`의 레터박스 여백이 겹쳐 필요 이상으로 큰 빈 공간을 만들고 있었던 것.

1. **영상을 배경 오버레이 → 카드 상단의 일반 이미지로 전환 — 완료**: 모바일 미디어쿼리에서 `.hero-slider__video`를 `position: static`으로 바꾸고 `aspect-ratio: 16/9`(원본 비율과 정확히 일치해 `object-fit: cover`로 바꿔도 잘림 없음), 문구는 그 아래 자연스러운 문서 흐름으로 바로 이어 붙게 됨(`min-height: 0`으로 슬라이드 높이도 내용에 맞게 줄어듦) — 더 이상 빈 공간이 생기지 않음. 영상은 좌우로 살짝 튀어나오게(`negative margin`) 카드 폭 전체를 꽉 채우도록 처리, 문구를 덮던 어두운 그라디언트(`__video-overlay`)는 이제 필요 없어져 모바일에서 숨김.
2. **화살표 버튼을 골드로 바꾸고 영상 아래쪽 경계에 걸치게 재배치 — 완료**: 기존엔 화면 한가운데 텅 빈 공간에 어두운 반투명 버튼으로 떠 있었음. `top` 값을 `calc((100vw - 3rem) * 0.5625)`로 계산(영상 폭의 9/16 = 영상 높이)해서 화살표 중심이 정확히 "영상이 끝나고 문구가 시작되는 경계선"에 오도록 이동, 배경도 사이트 골드 강조색으로 교체해 잘 보이게 함.
3. **장식용 골드 블롭(`::before`) 모바일에서 숨김 — 완료**: `position:absolute`인 이 장식 요소가, 영상이 더 이상 절대 배치 배경이 아니게 되면서(일반 문서 흐름 이미지) 오히려 영상 위에 떠 보일 수 있어 모바일에서만 비활성화.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. 모바일 폭에서 영상과 문구 사이 빈 공간이 없어졌는지, 화살표가 골드색으로 영상 하단 경계에 걸쳐 보이는지, 영상이 잘리지 않고 카드 폭 전체를 채우는지 확인 필요. 아직 커밋・푸시하지 않음.

## 모바일 여정 그래프 STEP이 뒤로 갈수록 오른쪽으로 밀려 잘리던 버그 수정 (2026-08-11, 같은 날 후속 요청)

사용자가 스크린샷에 빨간 펜으로 원하는 배치(곧은 세로줄+점, 잘림 없이 각 단계가 왼쪽에서 시작)를 직접 그려서 전달 — 작업 전 이해한 내용을 먼저 설명(직전 대화 참고)한 뒤 진행.

**원인**: 이전 "모바일 세로 타임라인" 작업 때 각 단계 박스를 `position: relative`로 바꾸면서, 데스크톱 곡선 좌표용 인라인 스타일(`style={{ left: '${step.x}%', top: '${step.y}%' }}`, x값은 25/47/69/95%)이 여전히 붙어있는 걸 놓쳤음. `position: relative`는 `absolute`와 마찬가지로 `left`/`top` 오프셋을 그대로 적용받는다(`static`만 무시함) — 그래서 STEP 04(x:95%)는 화면 폭의 95%만큼 오른쪽으로 밀려나 거의 다 화면 밖으로 잘려나가고 있었음. 이전에 `transform: none !important`는 정확히 처리했지만 `left`/`top`을 놓친 것.
1. **좌표 밀림 버그 수정 — 완료**: `JourneyGraph.css` 모바일 블록의 `.journey-graph__step`에 `left: 0 !important; top: 0 !important;` 추가(인라인 스타일은 외부 스타일시트의 `!important`로만 덮어쓸 수 있음) — 이제 4단계 전부 같은 왼쪽 위치에서 시작.
2. **선・점을 좀 더 또렷하게 — 완료**: 빨간 그림의 두꺼운 선・큰 점에 맞춰 점 크기(0.65rem→0.85rem), 연결선 두께(2px→3px), 텍스트와의 간격(padding-left 1.5rem→1.75rem)을 살짝 키움.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과.
- 커밋 `7dddffe`(`fix: 모바일 히어로 슬라이더/여정 그래프 레이아웃 버그 수정` — 국비지원 배너 모바일 정렬 + 히어로 슬라이더 빈 공간・화살표 + 여정 그래프 좌표 버그까지 전부 포함) → GitHub `main` 푸시 완료. `vercel deploy --prod` 첫 시도에 바로 성공 — https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).

## 푸터 정보 줄바꿈 수정 + 대표자 풀네임 + 국비지원 배너 로고 위치 이동 (2026-08-11, 같은 날 후속 요청)

1. **푸터 사업자 정보가 좁은 화면에서 항목 중간이 끊기듯 줄바꿈되던 문제 — 완료**: `Footer.jsx`의 각 줄("사업자(법인)명: ... 사업자등록번호: ...", "교육담당: ... 학원명: ... 학원등록번호: ..." 등)이 `&nbsp;&nbsp;`로만 구분된 긴 한 문장이라 좁은 화면에서 항목 중간 아무 데서나 줄바꿈되고 있었음. 각 "라벨 : 값" 항목을 개별 `<span>`으로 감싸고 `Footer.css`에서 `white-space: nowrap`을 줘서 항목 내부는 항상 한 줄로 유지되게 하고, `p`는 `display:flex; flex-wrap:wrap`으로 바꿔 화면이 좁으면 항목 단위로 통째로 다음 줄로 넘어가게 함(항목이 중간에서 잘리는 일은 없어짐).
2. **사업자 정보의 대표자 이름에 성이 빠져있던 문제 — 완료**: `contactInfo.js`의 `businessInfo.ceo`를 `'성훈'` → `'김성훈'`으로 수정(로그인 계정 표시용 이름 `'성훈'`은 별개라 그대로 둠 — mock 로그인 페르소나 이름과 사업자 정보란의 법적 대표자 표기는 서로 다른 용도).
3. **국비지원 배너 로고가 위에 혼자 떠 있던 문제 — 완료**: `FundingBanner.jsx`에서 태극 배지 아이콘을 문구 위 독립된 위치에서 빼서 "국비지원 조회하기" 버튼 텍스트 앞에 작게(1.25rem) 넣음 — 버튼 배경이 이미 흰색이라 아이콘의 기존 흰 원형 배경은 필요 없어져 제거. 더 이상 안 쓰는 모바일 전용 아이콘 스타일(세로 스택 등)도 정리.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과.

## 모바일 여정 그래프 재배치 요청 — 내일로 보류 (2026-08-11, 같은 날 후속 요청)

사용자가 여정 그래프 모바일 화면에 빨간 펜으로 그려서 재배치를 요청 — "선이 위에서 시작해 각 단계 도착 지점에 점이 있고 그 옆에 내용 설명이 있어야 한다"는 설명을 들었으나, 이는 이미 구현된 구조와 같아 보여서 정확히 어떤 차이를 말하는 것인지 두 차례 확인 질문(박스로 감싸는 것인지, 배포 전/후 스크린샷인지)을 했지만 서로 의도가 안 맞음 — **사용자가 내일 이어서 설명하기로 하고 오늘은 보류**. 다음 세션 시작 시 이 부분을 먼저 물어봐야 함(사용자가 직접 요청함).
- `npm run build` 통과(위 1~3번 항목 반영본). 커밋・푸시・배포는 아래에서 진행.
- 커밋 `3783038`(`fix: 푸터 정보 줄바꿈 + 대표자 풀네임 + 국비지원 배너 로고 위치`) → GitHub `main` 푸시 완료. `vercel deploy --prod` 첫 시도에 바로 성공 — https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK). 여정 그래프 모바일 배치 건은 미반영 상태로 다음 세션에 이어감(위 항목 참고, auto-memory에도 기록해둠).

## 모바일 여정 그래프 재배치 — 손그림 재설명으로 해결 (2026-08-12)

어제 보류됐던 건에 대해 새 스크린샷(선+점 옆에 상자 모양으로 "이 정도 위치·비율에 글이 와야 한다"는 손그림)을 받아 의도 확인 완료: 상자를 그리라는 게 아니라, 점(선)과 텍스트 사이 간격 그리고 텍스트가 화면 오른쪽 끝까지 안 닿고 일정 비율만큼 떨어져 있어야 한다는 뜻이었음.

- **`JourneyGraph.css` 모바일 블록 수정 — 완료**: `.journey-graph__step`의 `padding-left: 1.75rem`(고정 28px, 점과 거의 붙어있던 간격) → `max(2.25rem, 12%)`로, 우측은 기존에 아예 없던 `padding-right: 18%` 신규 추가(텍스트가 화면 끝까지 꽉 차 있던 것을 오른쪽에 여백을 두도록). 손그림에서 측정한 비율(선 위치 ~17%, 글 시작 ~28-29%, 글 끝 ~72-89%)에 맞춰 퍼센트 기반으로 설정 — 점(`::before`)·선(`::after`)은 절대 위치라 이 padding 변경에 영향받지 않고 그대로 유지됨.
- `npm run build` 통과. 모바일 폭에서 그림과 비율이 맞는지 확인 필요. 다르면 퍼센트 값(12%/18%)만 조정하면 됨. 아직 커밋・푸시하지 않음.

## 모바일 여정 그래프: "선으로 시작 → 점 도착" 도입선 추가 + 자동 색칠 재생으로 전면 교체 (2026-08-12, 같은 날 후속 요청)

앞서 적용한 좌우 여백 조정만으로는 부족하다는 피드백 — 정확한 요구사항 재확인: "시작이 선으로 시작하고 도착이 점이야. 각 도착점 옆에 STEP n의 내용이 있어야 함" + "PC 버전처럼 단계별로 색칠되면서 넘어가는 것을 자동으로 구현".

1. **첫 단계 점 위에 "도입부 선" 추가 — 완료**: 지금까지는 STEP 01 점이 아무 선 없이 툭 놓여있었음(점~점 사이만 선으로 연결). `JourneyGraph.css`에 `.journey-graph__chart::before`로 짧은 세로선(1.75rem)을 첫 점 위에 추가해 "선이 이어지다 점에 도착"하는 형태로 변경, `.journey-graph__chart`에 `padding-top: 1.75rem`로 공간 확보.
2. **모바일 색칠 진행 방식을 스크롤 연동 → 자동 재생 타이머로 전면 교체 — 완료**: 어제 구현한 "스크롤로 각 단계에 도달하면 채워지는" 방식은 사용자가 원한 "PC처럼 자동으로 단계별 색칠"이 아니었음. `JourneyGraph.jsx`에서 단계별 `IntersectionObserver`(`stepRefs`)를 제거하고, 그래프 전체(`chartRef`) 하나에만 `IntersectionObserver`를 걸어 화면에 보이는 동안 700ms 간격으로 STEP 1→2→3→4를 자동으로 순서대로 채우고, 다 채워지면 1.5초 멈췄다가 처음부터 반복 재생(`AchievementBanner`의 반복 재생 패턴과 같은 결). 화면 밖으로 나가면 타이머 정지하고 초기화. 도입부 선도 STEP 01이 채워지는 시점에 함께 골드로 바뀌도록 `journey-graph__chart--started` 클래스 연동.
3. **Playwright로 실제 렌더링 검증 — 완료**: 브라우저 자동화 도구가 없어 매번 "직접 확인해달라"고만 안내했는데, 이번엔 임시로 `npm install --no-save playwright`(기존 sharp와 동일한 흔적 안 남기는 임시 설치 관례) 후 헤드리스 크로미움으로 375px 모바일 뷰포트에서 실제 스크린샷 3장(0초・2초・3.5초 시점)을 찍어 자동 재생이 의도대로 순서대로 채워지는지 직접 눈으로 확인함 — 이후 `playwright` 즉시 제거, `git status`로 `package.json`・lockfile에 흔적 없음 확인.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. Playwright 스크린샷으로 자동 재생(0→2→3.5초 시점에 STEP이 순서대로 골드로 채워짐) 확인 완료. 아직 커밋・푸시하지 않음 — 실제 폰/브라우저에서도 한 번 확인해주시면 좋습니다.

## 여정 그래프 자동 재생을 "흐르듯" 부드럽게 + 도입선 길이 실측 기반으로 재조정 (2026-08-12, 같은 날 후속 요청)

방금 구현한 자동 재생이 "너무 뚝뚝 끊어진다"(인스턴트 색상 전환 방식이라 그랬음), "맨 처음 선이 다른 선보다 너무 짧다"는 피드백.

1. **각 구간(도입선 포함)을 위→아래로 차오르는 그라디언트 채움 애니메이션으로 교체 — 완료**: 기존엔 `background-color`를 grey↔gold로 즉시(0.4s) 전환하는 방식이라 매 단계가 "탁, 탁" 끊겨 보였음. `background-image: linear-gradient(gold, gold)` + `background-position: top` + `background-size: 100% 0%→100%` 조합으로, 구간이 "reached" 상태가 되는 순간부터 0.65초에 걸쳐 위에서 아래로 실제로 채워져 내려가는 애니메이션으로 교체(`JourneyGraph.css`의 도입선・구간 선 둘 다 동일 기법 적용). JS 타이머 간격(700ms)과 채움 시간(0.65s)이 거의 맞물려서, 한 구간이 다 채워질 때쯤 다음 구간이 이어받아 채워지기 시작 — 전체적으로 하나로 이어져 흐르는 느낌.
2. **도입선 길이를 실측 기반으로 조정 — 완료**: 기존 1.75rem(28px)은 눈대중 값이라 실제 단계 간 간격(내용 줄 수에 따라 다름)보다 훨씬 짧았음. Playwright로 375px 모바일 뷰포트에서 각 STEP 박스의 실제 `top` 좌표를 측정한 결과 단계 간 간격이 약 125~165px(≈8~10rem)로 확인돼, 도입선 길이(및 `.journey-graph__chart`의 `padding-top`)를 8.5rem으로 재조정 — 다른 구간 선들과 비슷한 길이로 보이도록 함.
3. **Playwright로 실제 애니메이션 동작 검증 — 완료**: 스크린샷뿐 아니라 특정 시점의 `getComputedStyle(el, '::after').backgroundSize` 값을 직접 읽어(20%→33%→51%→64%→79%로 650ms에 걸쳐 연속 증가) CSS 전환이 실제로 매끄럽게 진행되는지 수치로 확인. 확인 후 임시 설치했던 `playwright` 제거, 흔적 없음 확인.
- `npm run lint`(기존 무관 warning 7개만) + `npm run build` 통과. 아직 커밋・푸시하지 않음.

## 회원가입 → 커뮤니티 글쓰기 → 댓글 → 타 사용자 노출 여부 E2E 테스트 (2026-08-12, 같은 날 후속 요청)

Playwright로 실제 회원가입부터 커뮤니티 게시판 글쓰기・댓글・다른 사용자 노출까지 전체 플로우를 자동으로 구동해 검증(주소 검색 팝업은 `window.daum.Postcode`를 초기화 스크립트로 모킹해 외부 스크립트 로딩 없이 즉시 주소가 채워지도록 처리).

**확인된 것**
1. 회원가입(신규 이메일 생성 → 중복확인 → 비밀번호/생년월일/성별/휴대폰/주소 입력 → 제출) → `/mypage`로 정상 리다이렉트, 로그인 상태 확인. `AuthContext`가 이제 mock이 아니라 **실제 Supabase Auth**로 연결돼 있음을 코드에서 확인(`supabase.auth.signUp` + `profiles` 테이블 insert) — progress.md에 남아있던 "5단계까지는 mock" 설명은 이미 다른 세션에서 갱신된 상태였음.
2. `/community`에서 글쓰기 → 목록에 즉시 노출됨 확인.
3. 글 상세 진입 → 댓글 작성 → 상세 페이지에 즉시 노출됨 확인.

**중요하게 발견된 것 — 커뮤니티 게시판은 아직 저장이 안 됨**
4. **완전히 별도의 브라우저 세션(다른 사용자를 흉내)에서 같은 글이 "안 보임"** — `CommunityContext.jsx`를 확인해보니 `useState(initialCommunityPosts)`로 seed data를 메모리에 올려두고 쓰는 방식일 뿐, localStorage도 Supabase 연동도 없음(Auth와 반대로 이 부분만 아직 4~5단계 mock 상태로 남아있음).
5. **작성자 본인이 새로고침만 해도 방금 쓴 글이 사라짐**(같은 세션이어도) — 순수 인메모리 state라 페이지가 새로 로드되는 순간 항상 seed data 5개로 초기화됨.
- 즉, "다른 사람도 볼 수 있는지"는 **현재 구조에서는 안 됨** — UI/로직 자체(글쓰기・댓글 폼, 목록/상세 렌더링)는 정상 동작하지만, 실제 배포 환경에서 서로 다른 방문자가 서로의 글을 보려면 커뮤니티도 상담(3단계에서 이미 전환됨)처럼 Supabase 테이블로 옮기는 작업이 필요함. 버그라기보다는 아직 안 옮겨진 남은 범위.
- 테스트 중 생성된 계정(`test{timestamp}@example.com`)은 실제 Supabase Auth・`profiles` 테이블에 남아있음 — anon key만 있어 스크립트로 삭제 불가, 필요하면 Supabase 대시보드에서 직접 정리 필요.
- 콘솔에 `406` 응답 1건 관찰(Supabase `.single()` 조회 시 결과 없을 때 흔한 정상 동작 패턴으로 보이며, 플로우 자체는 끝까지 정상 동작함 — 확실한 문제인지는 추가 확인 필요).
- 테스트용 Playwright는 확인 후 즉시 제거, 흔적 없음.

## 5가지 요청 처리: DB 영속성 검증 / 헤더 드로어 설명 / 유나 멘토 지정 / 여정 그래프 겹침 수정 / 커뮤니티 Supabase 연결 착수 (2026-08-12, 같은 날 후속 요청)

### 1. 관리자 상태 변경 → DB 저장 → 재변경도 유지되는지 검증 — 완료(정상)
Supabase에 직접 로그인해(admin 계정) `applications.status`를 `confirmed→pending→waiting→(원상복구)`로, `consultations`의 멘토 배정을 `mentor-1/matched→in_progress→(원상복구)`로 연속 변경하며 매번 새로 조회해 확인. 두 테이블 다 이미 Supabase 연동돼 있어(`ApplicationContext`/`ConsultationContext`가 매 변경 후 `refetch()`로 DB에서 다시 읽어옴) **정상적으로 저장되고, 재변경해도 최신 값이 정확히 유지됨**을 확인(중간값이 남거나 되돌아가는 문제 없음). 테스트 후 원래 값으로 복구 완료, 실 데이터에 영향 없음.

### 2. 모바일 드로어의 "멘토 대시보드" 항목 — 설명
스크린샷에 표시된 위치(학원안내 그룹과 로그아웃 사이)는 `Header.jsx`의 `{currentUser?.role === 'mentor' && <Link to="/mentor">멘토 대시보드</Link>}` — **로그인한 사람이 누구인지 보여주는 신분 표시가 아니라, 멘토 role 계정에게만 보이는 전용 페이지 바로가기 링크**임(관리자 role이면 같은 자리에 "관리자" 링크가 대신 뜸). 현재 로그인한 사람의 신분(이름)은 이미 드로어 맨 위 "OO님 · 마이페이지" 카드에 별도로 표시되고 있어서, 이 항목과 역할이 겹치지 않음 — 의도된 정상 동작으로 확인, 코드 변경 없음.

### 3. 유나(yuna@example.com) 계정 멘토 지정 — 완료
`src/data/mentors.js`에 `mentor-4`(이유나, 주니어 멘토, 전문분야: 교육과정 지원・학습 방법/진로, 사진 없어 이니셜 그라디언트로 자동 대체)로 추가. Supabase 쓰기 작업이라 사용자 확인을 먼저 받은 뒤, 그녀 계정으로 실제 로그인해(RLS상 본인 프로필만 수정 가능) `profiles.role`을 `user→mentor`, `mentor_id`를 `mentor-4`로 실제 DB에 반영 완료(변경 전/후 재조회로 확인).

### 4. 모바일 여정 그래프 애니메이션 겹침 버그 수정 — 완료
원인: 도입선(첫 점 위 구간)과 STEP01 점 색상・STEP01→02 구간 채움이 전부 동일한 `mobileReachedStep>=1` 조건 하나로 묶여있어서, 타이머가 처음 tick할 때 셋이 전부 동시에 시작되고 있었음(도입선이 다 채워지기도 전에 다음 구간이 같이 채워지기 시작 = "끊기지 않고 이어지는" 게 아니라 "겹쳐서 동시에" 채워지는 상태). `JourneyGraph.jsx`에서 도입선 트리거를 `leadInStarted`라는 별도 state로 분리하고, `setInterval` 대신 각 구간이 정확히 이전 구간의 채움 시간(650ms, CSS transition 길이와 동일)만큼 지난 뒤에 시작하도록 `setTimeout`을 순차적으로 예약하는 방식으로 교체. Playwright로 `getComputedStyle(el, '::after'/'::before').backgroundSize`를 여러 시점에서 읽어 **도입선이 763ms에 100% 다 채워진 뒤에야 STEP01→02 구간이 0%에서 시작**하는 것을 수치로 확인(겹침 없음).

### 5. 커뮤니티 게시판 Supabase 연결 — 코드/마이그레이션 작성 완료, DB 반영은 사용자 실행 필요
지원(2단계)・상담(3단계)과 같은 패턴으로 4단계 진행.
- **`supabase/migrations/0008_academy_community.sql`(신규)**: `academy.community_posts`・`academy.community_comments` 테이블 생성(댓글은 별도 테이블로 분리, `post_id`로 FK). 커뮤니티는 로그인 없이도 전체 글이 보이는 게시판이라(Community 라우트가 `RequireAuth`로 안 감싸여 있음) select 정책에 `anon`도 포함시킴(다른 테이블은 `authenticated`만 허용했던 것과 다른 부분). 글・댓글 작성은 본인 명의만, 계정 없는 시드 글을 위한 null 전용 insert 정책도 추가(0004 `applications_insert_dummy`와 동일한 패턴).
- **`supabase/migrations/0009_academy_community_seed.sql`(신규)**: 기존 `src/data/communityPosts.js`에 있던 seed 글 5개・댓글 10개를 계정 없는 더미 작성자로 그대로 이관하는 INSERT문.
- **`src/context/CommunityContext.jsx` 전면 교체**: `useState(seed data)` 인메모리 방식 → `ApplicationContext`/`ConsultationContext`와 동일한 패턴(Supabase `select`로 초기 조회 + 각 mutation 후 `refetch()`)으로 전환. 글・댓글을 각각 조회한 뒤 `postId` 기준으로 묶어 기존 UI가 기대하는 `post.comments` 배열 형태로 합쳐주므로 `Community.jsx`・`CommunityPostDetail.jsx`는 코드 변경 없이 그대로 동작.
- **`src/data/communityPosts.js` 정리**: 이제 안 쓰는 `initialCommunityPosts` 배열 제거, `COMMUNITY_CATEGORIES`(카테고리 select/필터 UI용)만 남김.
- **아직 DB에 테이블이 없음을 직접 확인**(`community_posts` select 시도 → "Could not find the table" 에러) — **0008・0009 마이그레이션을 Supabase 대시보드 SQL 에디터에서 직접 실행해야 커뮤니티 글쓰기/조회가 다시 정상 동작함**. 실행 전까지는 목록이 항상 비어 보이고 글쓰기도 조용히 실패함(에러 UI 없음 — 기존 컨텍스트들과 동일한 수준의 에러 처리로, 별도 개선 요청 없으면 그대로 둠).
- `npm run lint`(기존 무관 warning 7개, `CommunityContext.jsx`도 같은 종류 1개 추가돼 총 7개 유지) + `npm run build` 통과.
- 아직 커밋・푸시하지 않음 — 마이그레이션 실행 후 실제로 글쓰기/댓글까지 동작 확인하고 커밋하는 게 안전함.

## 여정 그래프 간격 축소 / 스크롤-투-탑 / 유나 건 정정 / 커뮤니티 최종 검증 (2026-08-12, 같은 날 후속 요청)

### 1. 모바일 여정 그래프: 제목과 첫 점 사이 빈 공간 축소 — 완료
스크린샷에 빨간 박스+화살표로 표시된 만큼 콘텐츠를 위로 이동해달라는 요청 — 원인은 직전에 추가한 도입선(`padding-top`/`::before` height) 8.5rem이 실측 기반이긴 했지만 제목 바로 아래에 놓고 보니 지나치게 넓은 빈 공간을 만들고 있었음. `JourneyGraph.css`에서 8.5rem → 4.5rem으로 축소. Playwright 스크린샷으로 제목-STEP01 간격이 확연히 줄어든 것 확인.

### 2. 모바일에서 다른 메뉴 이동 시 스크롤 맨 위로 자동 이동 — 완료
react-router는 SPA 라우팅이라 브라우저 기본 스크롤 초기화가 없어서, 페이지 아래쪽에서 다른 메뉴로 이동해도 이전 스크롤 위치가 그대로 남아있던 문제. `src/components/ScrollToTop.jsx` 신규 추가(`useLocation`의 `pathname`이 바뀔 때마다 `window.scrollTo(0,0)`, `App.jsx` 최상단에 렌더링). Playwright로 스크롤을 2000px 내린 뒤 모바일 드로어 메뉴로 페이지 이동 → 정확히 0으로 돌아가는 것 확인(사이트 전역에 이미 있던 `scroll-behavior: smooth`(`index.css`) 덕분에 순간이동이 아니라 400ms 정도 부드럽게 스크롤됨 — 기존 관례와 자연스럽게 맞음).

### 3. 유나 계정 건 — 전달 오류 정정
지난 요청("이유나 계정에 멘토 지정해서 넣어줘")을 "유나 본인을 멘토로 만들어달라"로 잘못 이해해 `profiles.role='mentor'`로 바꿔버렸는데, 실제 의도는 "유나(지원자)에게 담당 멘토가 연결되도록 설정"이었음. DB를 직접 확인해보니 **유나는 이미 은채 멘토(`mentor-3`)와 `consultations` 테이블에 `status: in_progress`로 정상 연결되어 있었음**(이전 세션에서 이미 시드해둔 상태, `src/data/users.js` 주석에도 명시돼 있었음) — 즉 사용자가 원한 "담당 멘토 연결"은 애초에 이미 되어 있었고, 제가 추가로 한 조치(멘토 role 부여)만 불필요・잘못된 변경이었음.
- **되돌림**: 유나 계정으로 다시 로그인해 `profiles.role`을 `mentor→user`, `mentor_id`를 `mentor-4→null`로 원복(DB 재조회로 확인). `src/data/mentors.js`에 추가했던 `mentor-4`(이유나) 항목도 제거 — 최종적으로 이 파일은 이전 상태와 완전히 동일(git status에도 변경사항 없음으로 확인).
- 담당 멘토 연결 자체는 추가 조치 없이 이미 정상 상태.

### 4. 커뮤니티 Supabase 마이그레이션(0008, 0009) 실행 완료 후 재검증 — 전부 정상
사용자가 Supabase 대시보드에서 두 마이그레이션을 직접 실행 완료. Playwright로 전체 플로우 재검증:
- 마이그레이션으로 옮긴 seed 글(예: "포트폴리오 피드백 스터디...") 목록에 정상 노출 확인.
- 기존 로그인 계정(민지)으로 새 글 작성 → 목록에 즉시 노출.
- 글 상세 진입 → 댓글 작성 → 정상 노출.
- **완전히 별도의 브라우저 세션(다른 사용자)에서도 방금 쓴 글・댓글이 그대로 보임 확인** — 지난번 테스트 때 확인됐던 "다른 사용자에게 안 보이는" 문제가 Supabase 연결로 완전히 해결됨.
- 같은 세션에서 새로고침해도 글이 남아있음 확인(이전엔 새로고침만 해도 사라졌던 문제도 해결).
- 콘솔 에러 없음.
- `npm run lint`(기존 무관 warning 7개) + `npm run build` 통과. 테스트용 Playwright는 확인 후 즉시 제거.
- 아직 커밋・푸시하지 않음.

## 여정 그래프 최종 재설계: 실측 좌표 기반 연속선 + 텍스트 도착 색상 (2026-08-12, 같은 날 후속 요청)

지난 수정에서 여전히 "선이 따로 논다"는 피드백을 받고, 도입선 길이를 줄인 것도 "선은 원래대로, 옆의 내용만 옮겨달라는 뜻이었다"는 정정을 받음. 근본 원인은 지금까지 모바일 타임라인 선을 각 STEP마다 `padding`/`margin` 수치로 근사한 CSS 가상요소(`::before`/`::after`) 여러 개로 흉내내고 있었던 것 — 텍스트 줄 수에 따라 박스 높이가 미묘하게 달라지면 그 근사치가 실제 점 위치와 어긋나 이음매가 보일 수 있는 구조였음.

1. **선을 실측 좌표 기반의 진짜 연속선 하나로 재구성 — 완료**: `JourneyGraph.jsx`에 각 STEP 박스 ref(`stepRefs`)를 다시 추가하고, `getBoundingClientRect()`로 각 점의 실제 화면 좌표를 직접 측정해 배경선(`.journey-graph__mobile-track-base`)과 채움선(`.journey-graph__mobile-track-fill`) 두 개의 실제 DOM 요소(가상요소 아님)의 `top`・`height`를 인라인 스타일로 그린다. 세그먼트별 근사치가 아니라 하나의 실제 요소가 처음부터 끝까지 이어져 있어 구조적으로 끊김・이음매가 생길 수 없음. 도입선 길이도 STEP01→02 실측 간격을 그대로 재사용해 다른 구간과 항상 정확히 같음("선은 원래대로" 요청도 이 방식으로 자동 충족 — 하드코딩된 rem 값을 다시 조정할 필요 자체가 없어짐).
2. **채움 속도를 실제 거리에 비례한 고정 속도로 변경 — 완료**: 기존엔 구간마다 같은 시간(650ms)을 썼는데, 이번엔 `px당 속도(PX_PER_MS)`를 고정해 긴 구간은 오래, 짧은 구간은 짧게 걸리도록 해 "흐르는 속도"가 일정하게 느껴지도록 함. Playwright로 `getComputedStyle(fillEl).height`를 여러 시점에 읽어 62→172→282→392→427px로 완전히 연속적으로 증가하는 것을 수치로 확인(점프・끊김 없음).
3. **점(선)뿐 아니라 텍스트도 도착 시 색이 바뀌도록 추가 — 완료**: `.journey-graph__step--reached` 클래스(이미 존재)를 활용해 배지・제목・본문 색이 함께 골드로 강조되도록 CSS 추가(`JourneyGraph.css`, 모바일 미디어쿼리 안에서만 적용 — 데스크톱 호버 로직과 분리). Playwright로 STEP01→02→03→04 순서대로 점과 함께 제목 색이 정확히 골드로 바뀌는 것 확인(처음엔 마지막 STEP만 안 바뀐 것처럼 보였는데, 재확인해보니 실제로는 정상 동작 중 색 전환 애니메이션(0.4s)이 막 시작된 찰나(0ms)를 캡처한 테스트 스크립트의 타이밍 문제였고 300ms 후 재확인하니 정상적으로 골드로 전환 완료됨 — 실제 버그 아님).
4. **제목-그래프 사이 여백 축소는 선 길이가 아니라 바깥쪽 여백으로 처리 — 완료**: `.journey-graph`의 top padding과 `.journey-graph__title`의 margin-bottom을 모바일에서만 줄여서(선 내부 구조는 전혀 건드리지 않고) 제목과 그래프 사이 공간을 좁힘.
- `npm run lint`(기존 무관 warning 7개) + `npm run build` 통과. Playwright로 좌표 실측값・연속 채움 애니메이션・색상 전환까지 전부 수치로 검증 완료, 확인 후 즉시 제거.

## 오늘(2026-08-12) 전체 작업 커밋・푸시・배포

오늘 진행한 모든 작업(여정 그래프 자동 재생/실측 연속선 재설계, 모바일 메뉴 스크롤-투-탑, 커뮤니티 Supabase 연결, DB 영속성 검증 등)을 한 번에 커밋・푸시・배포 진행.
- 커밋 `7c42192`(`feat: 커뮤니티 Supabase 연결 + 여정 그래프 실측 좌표 재설계 + 스크롤-투-탑`) → GitHub `main` 푸시 완료.
- `vercel deploy --prod`: 첫 시도 `Not authorized`(기존에도 반복된 일시적 오류) → 재시도에서 성공. https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).
- 커뮤니티 Supabase 마이그레이션(0008・0009)은 이번 세션 중 사용자가 이미 대시보드에서 실행 완료, 배포된 사이트에도 그대로 반영됨.

## 채용연계 파트너사 카드에 회사 정보 보강 (2026-08-12, 같은 날 후속 요청)

"연매출액・사원수 같은 정보를 추가하면 좋겠다, 더 넣을 게 있으면 제안해달라"는 요청 — 기본 회사 정보(설립연도・사원수・연매출액・근무지) 외에 초봉/연봉 범위・근무형태/복지・인턴 전환 여부・홈페이지 링크도 제안했으나, 사용자가 "기본 회사 정보"만 선택.

1. **`data/partners.js`에 필드 추가 — 완료**: 6개 파트너사 전부에 `foundedYear`(설립연도)・`employeeCount`(사원수)・`annualRevenue`(연매출액)・`location`(근무지) 추가 — 업종・기존 소개 문구와 자연스럽게 어울리는 규모로 가상의 수치 작성(에이전시=소규모, 커머스/핀테크=대규모 등). 다른 placeholder 수치들과 동일하게 실제 파트너사 확정 시 교체 필요.
2. **`Partners.jsx`・`Partners.css` — 완료**: 회사명 아래, 소개글 위에 "설립 2019년 · 사원 45명 · 연매출 80억원 · 서울 강남구" 형태의 메타 정보 줄 추가(작은 글씨・톤다운 색상으로 소개글과 위계 구분).
- `npm run lint`(기존 무관 warning 7개) + `npm run build` 통과. Playwright로 데스크톱・모바일 스크린샷 확인(레이아웃 정상, 콘솔 에러 없음), 확인 후 즉시 제거. 아직 커밋・푸시하지 않음.

## 문의 게시판 Supabase 연결 + 커뮤니티 글/댓글 수정・삭제 기능 추가 (2026-08-12, 같은 날 후속 요청)

1. **문의 처리 방식 확인 — 완료(답변만)**: 회원이 문의를 남기면 `createInquiry`가 `reply: null` 상태로 목록에 추가되고, 본인은 `/inquiry`의 "내 문의 내역"에서만 볼 수 있음(다른 사람 문의는 비공개). 관리자가 `/admin` "문의 관리" 탭에서 1건짜리 답변을 등록하면 답변완료로 바뀜. 비회원은 이름・연락처・이메일로 남기고 나중에 재조회할 방법이 없어(로그인 계정이 없으므로) 담당자가 남긴 연락처로 직접 연락하는 방식.
2. **메인 메뉴 클릭 시 서브메뉴만 열리는 것에 대한 의견 — 제시만 함(구현 안 함)**: `NavDropdown.jsx`는 `<details>/<summary>`라 `summary`(메인 메뉴 라벨) 클릭은 원래 "펼치기/접기" 토글이지 링크가 아님 — 각 대분류(교육과정/소통공간/학원안내)마다 별도 허브 페이지를 만드는 건, 이미 하위 메뉴・푸터에 있는 링크를 다시 나열하는 정도라 실제 정보량 증가 없이 유지보수 대상만 늘어날 수 있어 신중한 편을 추천. 원한다면 "메뉴 라벨 클릭 시 그 그룹의 첫 하위 페이지로 바로 이동" 정도의 가벼운 대안도 가능.
3. **문의 게시판 Supabase 연결 — 코드/마이그레이션 완료, DB 반영은 사용자 실행 필요**: 커뮤니티(4단계)와 같은 패턴으로 진행.
   - **`supabase/migrations/0010_academy_inquiries.sql`(신규)**: `academy.inquiries` 테이블 생성(`user_id` nullable로 비회원 지원, `name`을 회원・비회원 모두 항상 저장해 관리자 화면에서 바로 이름 표시 가능하게 함 — 2・3단계와 동일한 관례). 조회는 로그인 회원만(마이페이지・관리자 전용 화면이라 커뮤니티처럼 `anon` select는 불필요), 등록은 `anon`도 가능(비회원 문의), 답변(수정)은 로그인 회원 전체 허용(기존 관례와 동일한 단순화).
   - **`supabase/migrations/0011_academy_inquiries_seed.sql`(신규)**: 기존 `src/data/inquiries.js`의 seed 문의 6건을 계정 없는 더미로 이관(원본의 가짜 userId는 실제 계정이 아니었어서, 이관하며 표시용 이름을 새로 부여).
   - **`src/context/InquiryContext.jsx` 전면 교체**: `useState(mock)` → Supabase(`select`/`insert`/`update` + `refetch()`) 패턴, 기존 함수 시그니처(`createInquiry`/`getMyInquiries`/`getAllInquiries`/`replyToInquiry`) 그대로 유지해 `InquiryBoard.jsx`・`Admin.jsx`・`InquiryList.jsx` 전부 코드 변경 없이 동작.
   - **`src/data/inquiries.js` 삭제**(다른 곳에서 더 이상 참조 안 함), **`Admin.jsx`의 `inquiryAuthorName`** 을 이제 항상 채워지는 `inquiry.name`을 우선 사용하도록 소폭 수정(기존엔 로그인 회원 문의일 때 `mockUsers` 배열에서 이름을 찾았는데, 실제 신규 가입 계정은 애초에 `mockUsers`에 없어 UUID가 그대로 노출되는 문제가 있었음 — 이번에 함께 해결됨).
   - **아직 DB에 테이블 없음 직접 확인**(`inquiries` select 시도 → "Could not find the table" 에러) — **0010・0011을 Supabase 대시보드 SQL 에디터에서 순서대로 실행해야** 문의 등록・답변이 다시 정상 동작함.
4. **커뮤니티 댓글/글 수정・삭제(본인 또는 관리자) — 코드/마이그레이션 완료, DB 반영은 사용자 실행 필요**:
   - **`supabase/migrations/0012_academy_community_edit_delete.sql`(신규)**: `community_posts`・`community_comments`에 update/delete 정책 추가 — "본인 소유(author_id 일치)" 정책과 "관리자(profiles.role='admin')" 정책을 각각 추가해 OR로 합쳐지도록 함(0004와 동일한 원리).
   - **`CommunityContext.jsx`**: `canModify(authorId)`(본인 또는 관리자 여부 판단) + `updatePost`・`deletePost`・`updateComment`・`deleteComment` 추가.
   - **`CommunityPostDetail.jsx`**: 글 제목 옆・각 댓글 옆에 `canModify`일 때만 "수정"/"삭제" 텍스트 버튼 노출. 수정은 인라인 폼으로 전환(제목+본문 또는 댓글 내용), 삭제는 확인창 후 즉시 반영(글 삭제 시 `/community`로 이동, 댓글은 `on delete cascade`로 글 삭제 시 함께 정리됨). `CommunityPostDetail.css`에 관련 스타일 추가.
   - **DB 미반영 상태 — 0012 마이그레이션 실행 전까지는 수정・삭제 버튼을 눌러도 RLS가 막아 실패함**(UI는 이미 배포 가능한 상태).
- `npm run lint`(기존 무관 warning 7개) + `npm run build` 통과. 아직 커밋・푸시하지 않음 — **0010・0011・0012 세 마이그레이션을 순서대로 실행해주셔야** 문의 게시판과 커뮤니티 수정/삭제가 실제로 동작합니다.

## 0010~0012 실행 후 재테스트 + 진짜 원인의 버그 발견・수정 + 학원 일정 Supabase 연결 (2026-08-12, 같은 날 후속 요청)

사용자가 0010・0011・0012를 대시보드에서 실행 완료 후 "게시물 삭제는 되는데 댓글 수정/삭제가 안 된다"고 재보고. Playwright로 재현하며 원인을 추적.

### 원인 파악 과정
1. **새로 쓴 댓글은 수정/삭제 정상 동작** — 로그인 사용자가 방금 작성한 자기 댓글로 테스트하면 문제없음.
2. **관리자로 시드(더미 작성자) 댓글을 열어보니 수정/삭제 버튼 자체가 안 보임** — RLS(0012)를 API로 직접 테스트해보니 관리자 권한으로 남의 댓글을 수정하는 것 자체는 100% 정상 동작(`community_comments_update_admin` 정책 정상). 즉 백엔드는 문제없고 **프론트엔드가 관리자 로그인 상태를 인식 못 하는 순간이 있었음**.
3. **진짜 원인 발견**: `ApplicationContext`・`ConsultationContext`・`CallbackContext`・`InquiryContext`・`CommunityContext` 전부 `useEffect(() => { refetch() }, [refetch])` 형태로, **앱이 처음 켜질 때 딱 한 번만 데이터를 조회**하고 있었음. `refetch`는 `useCallback(..., [])`라 참조가 절대 안 바뀌니 이 effect는 사실상 "최초 마운트 1회"만 실행됨. 문제는 Supabase 로그인 세션 복원이 비동기라서, 이 최초 조회가 **세션 복원이 끝나기 전에** 먼저 실행되면 RLS(`select to authenticated`)가 막아서 빈 배열을 리턴하는데, 그 이후 세션이 복원돼도 **다시 조회하는 트리거가 없어 빈 배열이 그대로 굳어버림**. 마이페이지・관리자 대시보드 등 화면에 로그인 상태로 들어가도 데이터가 안 보이거나 늦게 보이는 문제가 잠재적으로 여러 화면에 있었던 것 — 문의 게시판에서 유독 도드라져 보인 이유는 방금 이관한 기능이라 처음으로 제대로 테스트해봤기 때문.
4. **`CallbackContext.jsx`는 아예 `useAuth`를 쓰지도 않고 있었음**(콜백 신청 자체는 비회원도 가능해서 필요 없었는데, 조회는 관리자 전용이라 원래는 필요했음) — 이번에 추가.

### 수정 내용
- **5개 Context 전부**(`ApplicationContext`・`ConsultationContext`・`CallbackContext`・`InquiryContext`・`CommunityContext`) — 최초 조회 `useEffect`의 의존성 배열에 `currentUser`를 추가(`[refetch, currentUser]`)해서, 로그인 세션이 나중에 복원되거나 로그인/로그아웃이 일어날 때마다 자동으로 재조회되도록 수정. `CallbackContext.jsx`는 `useAuth` import・`currentUser` 추적 자체를 새로 추가.
- **Playwright로 전체 플로우 재검증 — 6개 항목 전부 통과**: 관리자 화면에 남의 댓글 수정/삭제 버튼 노출 → 관리자가 남의 댓글 삭제 → 회원 문의 등록 후 "내 문의 내역" 노출 → 새로고침 후 유지 → 관리자 화면에 방금 등록한 문의가 보임 → 관리자 답변 등록 후 반영, 전부 정상.

### 학원 일정(Schedule) Supabase 연결 — 다음 미완료 항목 진행
1. **`supabase/migrations/0013_academy_schedule_events.sql`(신규)**: `academy.schedule_events` 테이블 생성. `/schedule` 페이지가 로그인 없이 보이는 공개 페이지라 select는 `anon`도 포함, 나머지는 로그인 회원 전체 허용(관리자 전용 화면이지만 다른 관리 테이블과 동일한 단순화).
2. **`supabase/migrations/0014_academy_schedule_events_seed.sql`(신규)**: 기존 `src/data/schedule.js`가 `courses.js` 필드로 계산해두던 일정 20건(1~4기 모집・수업・수료식 등)을 실제 계산된 날짜값 그대로 DB에 이관. 관리자가 어떤 일정이든(계산되어 생성된 것 포함) 수정・삭제할 수 있는 기존 UI 동작과 맞추기 위해, 계산값 그대로를 "일반 데이터"로 취급해 전부 DB로 옮기는 방식을 택함(Community・Inquiry와 동일한 접근).
3. **`src/context/ScheduleContext.jsx` 전면 교체**: `useState(seed)` → Supabase 패턴, 함수 시그니처(`getAllEvents`/`addEvent`/`updateEvent`/`deleteEvent`) 그대로 유지해 `Admin.jsx`・`Schedule.jsx` 코드 변경 없이 동작.
4. **`src/data/schedule.js` 삭제**(더 이상 참조하는 곳 없음), `src/data/aboutContent.js`의 관련 주석도 새 출처(Supabase 테이블)를 가리키도록 갱신.
- **아직 DB에 테이블 없음 직접 확인**(`schedule_events` select → "Could not find the table" 에러) — **0013・0014를 순서대로 실행해야** `/schedule` 페이지와 관리자의 일정 추가/수정/삭제가 실제로 동작함(실행 전까지는 일정이 안 보이거나 추가해도 사라짐).
- `npm run lint`(기존 무관 warning 7개) + `npm run build` 통과. 아직 커밋・푸시하지 않음.

## 0013~0014 실행 후 학원 일정 재테스트 + 전체 커밋・푸시・배포 (2026-08-12, 같은 날 후속 요청)

사용자가 0013・0014 실행 완료 후 Playwright로 전체 플로우 재검증: 시드 일정이 `/schedule` 공개 페이지에 노출 → 관리자가 일정 추가 후 목록 노출 → 새로고침 후에도 유지 → 공개 페이지에도 반영 → 수정 반영 → 삭제 반영, 전부 통과(첫 시도에서 9월 날짜로 테스트해 "공개 페이지에 안 보임"으로 오판했었는데, `/schedule`이 월별 캘린더라 기본 화면이 2026년 8월로 고정돼 있어서였을 뿐 — 8월 날짜로 재확인해 실제 버그 아님을 확인).

오늘 하루 동안 진행한 전체 작업(문의 게시판・커뮤니티 수정삭제・여러 Context의 세션-레이스 버그 수정・학원 일정 Supabase 연결 등)을 한 번에 커밋・푸시・배포.
- 커밋 `96a59b1`(`feat: 문의 게시판・학원 일정 Supabase 연결 + 커뮤니티 수정/삭제 + 세션 레이스 버그 수정`) → GitHub `main` 푸시 완료.
- `vercel deploy --prod`: 첫 시도 `fetch failed`(일시적 네트워크 오류, 기존에도 반복됨) → 재시도에서 성공. https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).

## 문서 갱신: 기능명세서・DB스키마・프로젝트일정표 (2026-08-12, 같은 날 후속 요청)

남은 미완료 목록(공지사항 등 관리자 CRUD 확장 vs 문서 갱신) 중 무엇이 "완성도를 위해 필요한지" 의견을 드림 — CRUD 확장은 정적 콘텐츠라 실제로 자주 안 바뀌고 이미 검증한 패턴 반복이라 비용 대비 가치가 낮다고 판단해 보류를 추천, 문서 갱신은 비용이 적고 발표자료 등에서 실제 구현과 안 맞으면 헷갈릴 수 있어 추천 → 사용자가 문서 갱신만 진행하기로 결정.

- **`docs/기능명세서.md`**: "백엔드(Node.js+Express+SQLite) 미구현, 전부 mock" 전제를 걷어내고 실제 Supabase 연동 상태로 전면 갱신. 인증 방식(Supabase Auth), 페이지별 기능 표를 실제 라우트 전체(26개, 기존엔 10개만 기재)로 확장, 핵심 비즈니스 로직에 커뮤니티・문의・학원 일정・콜백 4개 섹션 추가, "알려진 제약 사항"을 "백엔드 없음"에서 "관리자가 직접 관리 못 하는 정적 콘텐츠 목록"・"RLS 단순화 범위"로 교체.
- **`docs/DB스키마.md`**: 기획 단계의 가상 스키마(User/Course/Mentor 등)를 걷어내고, 실제 마이그레이션(`supabase/migrations/0001~0014`) 기준 8개 테이블(profiles/applications/consultations/callback_requests/community_posts/community_comments/inquiries/schedule_events)의 실제 컬럼・RLS 정책을 문서화. `Course`・`Mentor`는 DB 테이블이 아니라 `src/data/*.js`의 정적 배열이며 문자열 id로만 느슨하게 연결된다는 점을 명시(실제 FK 아님 — 헷갈리기 쉬운 부분이라 강조).
- **`docs/프로젝트일정표.md`**: "8단계: 백엔드(Supabase) 연동" 진행 상황 추가(마이그레이션 번호별 체크리스트), "향후 계획"에서 이미 완료된 문의 게시판・백엔드 항목 제거하고 남은 것만(관리자 CRUD 확장・발표자료・기획 산출물 정리)으로 정리, 오늘 발견한 "로그인 세션-조회 레이스 컨디션" 버그도 6단계 버그 목록과 같은 형식으로 기록.
- 순수 마크다운 문서 변경이라 코드/빌드에 영향 없음.
- 커밋 `3751a5c`(`docs: 기능명세서・DB스키마・프로젝트일정표를 실제 Supabase 연동 상태로 갱신`) → GitHub `main` 푸시 완료. `vercel deploy --prod` 첫 시도에 바로 성공 — https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).

## 결함 발견 및 수정・보완 보고서 작성 (2026-08-12, 같은 날 후속 요청)

"기능 테스트를 수행해 결함을 찾아 수정보완하고 수정・보완사항을 보고서로 완성하시오"라는 과제 요구사항 — 실제로 이 프로젝트를 진행하며 발견・수정한 결함들을 이 `progress.md` 전체 이력에서 추출해 정식 보고서 형식으로 재구성.

- **`docs/결함발견및수정보고서.md`(신규)** 작성: 2026-07-31~2026-08-12 기간 동안 실제로 발견・수정한 결함 29건(D-01~D-30, 조사 중 하나가 다른 결함과 동일 원인으로 판명돼 통합됨)을 발견일・영역・심각도・원인・조치가 포함된 표로 정리하고, 그 중 가장 중요한 5건(정원 초과 버그, Supabase RLS 권한 설정 결함 2건, 모바일 여정 그래프 좌표 밀림, 오늘 발견한 로그인 세션-데이터 조회 레이스 컨디션)은 증상・조사 과정・원인・조치・검증까지 상세 서술. "버그처럼 보였지만 조사 결과 결함이 아니었던" 오탐 6건도 별도 섹션으로 정리(정확한 원인 규명 없이 성급하게 고치지 않았다는 근거 자료로 활용 가능). 심각도별(Critical 7・High 5・Medium 11・Low 6)・영역별 통계표도 포함.
- 커밋 `b11c38a`(`docs: 결함 발견 및 수정・보완 보고서 작성`) → GitHub `main` 푸시 완료(순수 문서라 별도 배포 없음).

## 마이페이지 개선 3건: 커뮤니티 활동 내역 + 배지 명칭 + 요약 카드 단순화 (2026-08-12, 같은 날 후속 요청)

스크린샷으로 마이페이지 요약 카드가 8개나 되어 지저분해 보인다는 피드백 + 커뮤니티 활동도 볼 수 있게 해달라는 요청 + 기본 배지 명칭 변경 요청.

1. **커뮤니티 글・댓글 현황 — 완료**: `CommunityContext.jsx`에 `getMyPosts()`・`getMyComments()` 추가(댓글은 게시글 안에 중첩돼 있어, 마이페이지에서 목록으로 보여주려고 어느 글의 댓글인지(`postId`・`postTitle`)까지 붙여서 평탄화). 마이페이지에 "커뮤니티" 탭을 새로 추가해 내가 쓴 글・댓글을 각각 목록으로 보여주고, 글 제목을 누르면 해당 글 상세로 이동.
2. **"지원자" → "일반회원" — 완료**: `applicantIdentity()`의 기본값(지원/대기 이력이 전혀 없을 때 표시되는 배지)을 "지원자"에서 "일반회원"으로 변경. 실제 합격/대기 이력이 있을 때 뜨는 "합격자"/"대기자" 배지는 그대로 유지(지원 결과를 나타내는 라벨이라 성격이 다름).
3. **요약 카드 8개 → 3개로 단순화 — 완료**: 지원 합격/대기중/취소, 상담 진행중/배정대기/완료, 문의 답변대기/답변완료로 흩어져 있던 카드 8개를, "지원 현황"・"상담 현황"・"문의 현황" 카드 3개로 묶어 각 카드 안에 관련 수치를 한 줄에 모아 보여주는 방식으로 교체.
- `npm run lint`(기존 무관 warning 7개) + `npm run build` 통과. Playwright로 배지 문구("일반회원") 확인, 상태 카드 3개로 줄어든 것 확인, 새 글 작성 후 마이페이지 커뮤니티 탭에 노출되는 것까지 end-to-end 확인.
- **테스트 중 남은 데이터 정리**: 이번 세션 테스트로 만들어진 커뮤니티 글 2건은 삭제 완료. 문의 테스트 데이터 2건은 `inquiries` 테이블에 삭제(DELETE) 권한 정책이 아예 없어(원래 기능 요구사항에 없었음) 삭제하지 못하고 남아있음 — 내용상 무해하지만("문의테스트 [타임스탬프]") 신경 쓰이시면 삭제 정책을 추가하거나 Supabase 대시보드에서 직접 지워주시면 됩니다.
- 커밋 `2da3cc9`(`feat: 마이페이지 커뮤니티 활동 내역 추가 + 요약 카드 단순화`) → GitHub `main` 푸시 완료. `vercel deploy --prod` 첫 시도에 바로 성공 — https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).

## 결함 보고서를 사람이 쓴 것처럼 재작성 (2026-08-12, 같은 날 후속 요청)

"너무 AI가 정리한 것 같다"는 피드백 — 결함 ID(D-01~D-30) 체계와 심각도별/영역별 통계표, "증상・원인・조치・검증"이 매번 똑같이 반복되는 항목별 템플릿을 걷어내고, 실제로 겪은 순서대로 이야기하듯 산문체로 재작성. 특히 여정 그래프(같은 컴포넌트에서 네 번 연속 다른 성격의 버그가 나온 것)와 세션 레이스 컨디션(사소한 신고로 시작해 다섯 개 영역 공통 구조 문제로 밝혀진 과정)은 조사 흐름을 그대로 서술. 자잘한 UI 수정 17건은 표 하나로 압축, 오탐 5건과 결론은 그대로 유지하되 문체만 자연스럽게 정리. 143줄 → 85줄로 축소.
- 순수 문서 변경, 코드/빌드 영향 없음.

## 페르소나・정보구조설계도・사용자시나리오 문서 신규 작성 (2026-08-12, 같은 날 후속 요청)

기획 초기(2단계・3단계)에 "페르소나 3명 확정", "IA 확정"이라고만 짧게 기록돼 있었을 뿐 실제 문서 파일은 만들어진 적이 없었음(이전 감사에서도 "원본 산출물이 프로젝트 폴더에 없음"으로 확인됐던 부분). 결함 보고서와 같은 톤(서술형, 표 최소화)으로 세 문서를 새로 작성.

1. **`docs/페르소나.md`**: 기존에 정해져 있던 3명(지원자 김민지・멘토 지수・관리자 성훈)에 실제 사이트가 지원하는 비회원 문의/전화상담 흐름을 반영해 "예비 지원자(비회원)" 1명을 추가해 총 4명. 각자 실제로 사용하는 페이지・기능까지 연결해서 서술(예: 지수는 멘토 대시보드에서 배정 수락/거절/완료, 성훈은 관리자 대시보드 각 탭).
2. **`docs/정보구조설계도.md`**: `App.jsx`의 실제 라우트 26개 전체를 트리로 정리하고(교육과정/소통공간/학원안내 3개 그룹 + 회원/멘토/관리자 전용 영역), "왜 이렇게 나눴는지"(헤더 항목이 많아져 3그룹으로 재편했던 실제 히스토리, 로그인 여부에 따라 열람/작성 권한이 다른 이유, 마이페이지를 활동 허브로 묶은 이유)를 설계 근거로 함께 서술.
3. **`docs/사용자시나리오.md`**: 페르소나 4명이 실제 화면을 순서대로 이동하는 흐름 4개를 서술형으로 작성. 작성 과정에서 실제 코드 대조 중 두 가지 부정확한 부분을 발견해 수정함 — ① 상담 분야 카테고리 이름이 "국비지원・수강료"가 아니라 실제로는 "교육과정 지원"이었음, ② 플로팅 상담 버튼의 "전화 상담"이 실제 콜백 신청(DB 저장)을 만드는 게 아니라 `tel:` 링크로 즉시 통화 연결만 하는 것이었음(`createCallbackRequest` 함수가 실제로는 어느 화면에서도 호출되지 않고 있어, 관리자/멘토 화면의 "비회원 상담 관리"가 사실상 아직 아무도 채우지 못하는 빈 기능이라는 것도 확인 — 별도로 다룰 만한 사항이라 여기서는 시나리오 문구만 정확하게 고침).
- 순수 문서 신규 작성, 코드/빌드 영향 없음.

## 결함 보고서 PPT 분량으로 축소 + 페르소나・시나리오 정확도 재검증 (2026-08-12, 같은 날 후속 요청)

"여전히 너무 길다, PPT 1~2페이지 분량으로" + "페르소나/시나리오가 실제 완성된 페이지와 맞는지, 이유나 계정은 왜 없는지" 두 가지 피드백.

1. **`docs/결함발견및수정보고서.md` 대폭 축소 — 완료**: "몇 번 재발했다" 같은 진행 과정 서술, 오탐 사례, 상세 조사 흐름을 전부 빼고 "핵심 기능 / 화면・레이아웃 / 백엔드・데이터" 3개 그룹에 문제→조치 한 줄씩만 남기는 방식으로 재작성. 85줄 → 27줄.
2. **이유나 계정 상태 실제 DB 재조회 — 완료**: 관리자 계정으로 로그인해 `profiles`/`applications`/`consultations`를 직접 조회. 이유나는 지원 "합격"(confirmed) + 은채 멘토와 상담 "진행중"(in_progress) 상태였고, **김민지도 사실 "빈 계정"이 아니라 이미 지원 "심사중"(pending) + 지수 멘토와 상담 "진행중" 상태로 시딩돼 있었음**(시나리오 문서가 마치 완전히 처음부터 시작하는 것처럼 서술했던 부분과 실제 시드 상태가 다름을 확인).
3. **`docs/페르소나.md`에 "이유나 — 합격 후 재학 회원" 추가**: 지원 단계가 끝난 뒤에도 상담・커뮤니티를 계속 쓰는, 기존 4명이 다루지 못했던 사용 단계를 대표하는 페르소나로 추가(총 5명). 문서 끝에 **"실제 테스트 계정 상태" 참고 표**를 새로 추가해 각 페르소나-계정-실제 DB 상태를 한 번에 확인할 수 있게 함 — 시드 데이터라 테스트를 거듭하며 값이 바뀔 수 있다는 점도 명시.
4. **`docs/사용자시나리오.md`에 이유나 시나리오 추가 + 안내 문구 보강**: "김민지・지수・성훈 시나리오는 전형적인 신규 사용자 흐름을 보여주는 것이고, 이유나 시나리오는 실제 테스트 계정 상태를 그대로 따라간다"는 설명을 문서 서두에 명시해, 시나리오가 "완성된 페이지의 100% 재현"이 아니라 "이 유형이 겪을 법한 흐름"이라는 성격을 분명히 함. 성훈 시나리오의 "비회원 상담 관리" 부분에도, 실제로는 콜백 신청 폼이 아직 연결 안 돼 있어 이 섹션에 뜰 내용이 없다는 점을 정직하게 추가.
- 순수 문서 변경, 코드/빌드 영향 없음.

## 비회원 콜백 신청 폼 연결 (2026-08-12, 같은 날 후속 요청)

전날 시나리오 문서를 쓰다가 발견한 것 — 관리자/멘토 화면에 "비회원 상담 관리"는 이미 있는데, 정작 비회원이 콜백을 신청하는 화면이 없어 그 관리 기능이 채워질 일이 없었던 문제를 해결.

1. **`PhoneConsultModal.jsx`에 콜백 신청 폼 추가 — 완료**: 기존엔 전화번호 목록(`tel:` 링크)만 있던 "전화 상담" 모달에, 구분선 아래로 "지금 통화하기 어려우신가요? 번호를 남겨주시면 저희가 먼저 연락드립니다" 폼(이름・연락처・문의 내용 선택)을 추가. 이미 만들어져 있던 `CallbackContext.createCallbackRequest`를 그대로 연결(로그인 여부 무관하게 제출 가능), 제출 후 "접수됐습니다" 안내로 전환. `PhoneConsultModal.css`에 구분선・폼 스타일 추가.
2. **Playwright로 전체 플로우 검증 — 완료**: 비로그인 상태로 플로팅 버튼 → 전화 상담 → 콜백 신청 폼 제출 → 관리자로 로그인해 "상담 관리" 탭(비회원 신청 관리 섹션)에 방금 신청이 노출되는지 → 연락완료 처리 버튼 존재까지 4단계 전부 통과.
3. **정리 중 추가로 발견한 사실**: `callback_requests` 테이블에 이미 시드 데이터 2건("이수진", "박현우")이 있었음 — 이전에 "관리 화면에 뜰 내용이 아예 없다"고 문서에 적었던 게 정확히는 틀렸음(신청 UI가 없었을 뿐, 시드 데이터로 인해 화면 자체는 비어있지 않았음). `docs/사용자시나리오.md`・`docs/페르소나.md`・`docs/프로젝트일정표.md`의 관련 문구를 전부 지금 상태(폼 연결 완료)에 맞게 수정.
4. **테스트 데이터**: 이번 테스트로 만든 콜백 신청 2건("콜백테스트 [타임스탬프]")은 `callback_requests`에 삭제(DELETE) 권한 정책이 없어(다른 테이블과 동일한 패턴) 삭제하지 못하고 남아있음 — 내용상 무해함.
- `npm run lint`(기존 무관 warning 7개) + `npm run build` 통과.
- 커밋 `cb029e6`(`feat: 비회원 콜백 신청 폼 연결`) → GitHub `main` 푸시 완료. `vercel deploy --prod` 첫 시도에 바로 성공 — https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK).

## 웹 파비콘을 메인 로고로 변경 (2026-08-12, 같은 날 후속 요청)

기존엔 Vite 기본 `favicon.svg`를 그대로 쓰고 있었는데, 헤더에 쓰이는 메인 로고(`public/logo-mark.png`, "U"+커서+"X" 마크)로 교체.

1. **정사각형 크롭 방식 비교 후 결정 — 완료**: 원본이 316×200 비율이라 그대로는 파비콘에 못 씀. 여백 없이 꽉 채우는 방식(중앙 크롭)과 로고 전체가 잘리지 않게 여백을 채우는 방식(레터박스) 둘 다 만들어 64px로 비교한 결과, 파비콘은 실제로 아주 작게(16~32px) 보이는 용도라 크롭해서 큼직하게 보이는 쪽이 훨씬 또렷해서 채택. 반대로 애플 홈 화면 아이콘(더 크게 보임)은 로고가 하나도 안 잘리는 여백 버전으로 따로 만듦.
2. **`sharp`로 이미지 처리 — 완료**(기존 관례대로 `npm install --no-save sharp`로 임시 설치 후 처리, 완료 후 `npm uninstall sharp`로 제거): `public/favicon.png`(32×32, 중앙 크롭), `public/apple-touch-icon.png`(180×180, 레터박스) 생성.
3. **`index.html` 갱신 — 완료**: `<link rel="icon">`을 새 PNG로 교체, `<link rel="apple-touch-icon">` 신규 추가. 더 이상 안 쓰는 `favicon.svg` 삭제.
- `npm run build` 통과, `dist/`에 두 파일 포함 확인, 로컬 서버에서 두 파일 200 응답 확인.
- 커밋 `ee5bbf6`(`feat: 파비콘을 메인 로고로 교체`) → GitHub `main` 푸시 완료. `vercel deploy --prod` 첫 시도에 바로 성공 — https://ui-ux-course-site.vercel.app 배포 완료, 파비콘 응답까지 확인(200 OK).

## 파비콘이 너무 확대돼서 잘려 보이는 문제 수정 (2026-08-12, 같은 날 후속 요청)

방금 적용한 "꽉 채운 크롭" 버전이 실제로 보니 너무 확대돼서 로고 일부가 잘린 것처럼 보인다는 피드백. 애플 터치 아이콘과 동일하게 로고 전체가 잘리지 않는 여백(레터박스) 버전으로 `favicon.png`를 다시 생성 — 크기는 조금 작아 보여도 잘리는 것보다 낫다고 판단.
- `npm run build` 통과.
- 커밋 `211fa31`(`fix: 파비콘이 너무 확대되어 잘려 보이던 문제 수정`) → GitHub `main` 푸시 완료. `vercel deploy --prod` 첫 시도에 바로 성공 — https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK). 브라우저가 파비콘을 강하게 캐싱하는 편이라, 탭에 바로 안 바뀌어 보이면 강력 새로고침(Ctrl+Shift+R) 필요할 수 있음.

## 파비콘 배경 투명 처리 (2026-08-12, 같은 날 후속 요청)

파비콘 배경이 흰색으로 보인다는 피드백 — 원본 `logo-mark.png` 자체가 투명이 아니라 옅은 크림색 텍스처 배경을 포함하고 있어서였음. `sharp`로 픽셀 단위로 배경색(약 254,254,252)과의 색상 거리를 계산해, 배경에 가까울수록 투명・로고 색상(파랑/네이비)에 가까울수록 불투명하게 알파 채널을 새로 계산하는 방식으로 배경 제거(단순 임계값이 아니라 거리 기반 그라데이션이라 경계가 부드럽게 처리됨, 흰 테두리 아티팩트 없음). 이 배경 제거된 버전으로 `favicon.png`(32×32)・`apple-touch-icon.png`(180×180) 재생성.
- `npm run build` 통과. 임시 설치한 `sharp`는 확인 후 제거.
- 커밋 `5fb4b44`(`fix: 파비콘 배경을 투명하게 처리`) → GitHub `main` 푸시 완료. `vercel deploy --prod` 첫 시도에 바로 성공 — https://ui-ux-course-site.vercel.app 배포 완료, 접속 확인(200 OK). 이전과 마찬가지로 브라우저 캐시 때문에 탭 아이콘이 바로 안 바뀌면 강력 새로고침 필요할 수 있음.
