프로젝트 루트에 있는 progress.md를 먼저 읽고 전체 기획 내용(주제, 사용자 유형, 핵심 로직, DB 스키마, 디자인 가이드, 페이지 목록)을 파악해줘.

지금까지 1~4단계(기획/리서치/디자인/웹퍼블리싱)는 완료된 상태고, 첨부된 순수 HTML/CSS 파일들(index.html, course.html, login.html, mypage.html, admin.html, styles/)이 그 결과물이야.

이제 이 프로젝트를 React + Vite로 전환하면서 5단계(게시판 핵심기능 구현)를 진행하려고 해. 아래 방식으로 작업해줘.

**개발 스타일**
- 기능 구현을 먼저 하고 스타일링은 나중에
- 폴더 구조는 pages/ 와 components/ 를 분리
- mock data는 별도 data.js 파일에 관리
- 상태관리는 Context API 사용
- 라우팅은 react-router-dom 사용
- 단계별로 하나씩 구현하고, 각 단계마다 무엇을 했는지 설명해줘
- 옵션이 여러 개일 때는 구조화된 리스트로 제시해주면 내가 빠르게 결정할게

**진행 순서**
1. React + Vite 프로젝트 세팅, 기존 HTML/CSS의 시맨틱 구조와 디자인 토큰(색상/폰트)을 CSS 변수 그대로 가져와서 컴포넌트 구조로 분리
2. 라우팅 구성 (홈/과정상세/로그인/마이페이지/멘토대시보드/관리자)
3. mock data 기반으로 지원(Application) CRUD 구현 — 정원 초과 시 자동 대기 로직 포함
4. 관리자 상태 변경 시 대기자 자동 승격 로직 구현
5. 멘토 상담 신청 + 자동 매칭 로직 구현 (분야 일치 → 부하 최소 순 배정)
6. 회원가입/로그인 및 role 기반 권한 분기 (지원자/멘토/관리자)

한 번에 다 만들지 말고, 1번부터 순서대로 진행하면서 각 단계 완료 후 나한테 확인받고 다음으로 넘어가줘.
